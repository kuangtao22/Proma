# 画布截图问题只读调查

## 已确认的界面缺陷

截图顶部标题精确对应 AgentCanvasOrchestrationCard.tsx:40。其目标摘要在 :41 同时使用 block 与 line-clamp-1；实际构建 CSS index-DfubVb8c.css 中 .line-clamp-1 位于偏移 58345，.block 位于 58675，后者覆盖 display:-webkit-box。外层 max-height 只能限制整体卡片区域，无法让摘要单行截断，因此 2008 字目标文本在折叠头部也展开，挤占聊天区域。

隔离 Electron 43 实测：相同600px宽度与真实构建CSS，原类组合 display=block、height=1512px；仅在隔离DOM移除 block 后 display=flow-root、height=24px，断言通过，退出0。源码和真实业务未修改。测试 /private/tmp/proma-goal-clamp-repro.cjs，首次沙箱启动退出134，使用授权的隔离隐藏窗口成功。此最小反例只证明CSS冲突，不代表整个组件修复验收。

## 画布与执行上下文

- 截图右侧「车本本｜小红书视频·AI管线V2」对应 5f287297；其当前图revision49，20节点10关系，全部8个video无入边，包含7个镜头与成片占位。
- V2 S1 artifact-66c3e41b 具有已采用视频与首尾帧 literal 资产输入，确实可以生成；没有图入边不等于没有模型输入，但无法从图还原完整制作依赖，也不满足用户要求连上首尾帧的操作。
- 截图顶部属于另一张「车本本｜马小本IP短片·正式制作0914」a0aed363；其委托current.status=cancelled，goal长2008字符，仍保留1/5阶段统计。多画布关联本身合法，展示应明确状态和所属画布。
- 22:31:29 canvas_get_context.activeCanvasId=a0aed363，而截图所见为V2；22:43之后原聊天Agent将当前画布认作0914并在那里新建S1。可见画布与操作目标发生不一致，需核对可见上下文传递及旧默认/最近记录。
- canvas-tool-provider.ts:1279读取binding.lastActiveCanvasId；SidePanel.tsx:1624-1636异步markActive并在catch仅打印错误。发送与此更新是否有竞态、或实例是否版本不同，尚未实测，不能声称已确定唯一上下文根因。

## 后续生成归因错误

原会话在截图后主动生成了新S1，并称canvas-output解析错输入。两份正式media/runs快照的比较显示：
- 6f136802 输入首帧hash195740d6，尾帧hashecef5792；尾帧等于0914选中B1节点artifact-d4511ddd的实际采用素材。
- 492c6933 首帧仍hash195740d6，提示词逐字相同，尾帧换为hashac0a162b。
- 这是换了尾帧的两次生成，不能据此证明canvas-output绑定机制错误。0914最终保留指向旧B1的图关系，实际配置采用另一个literal尾帧，视觉关系与生成输入仍不一致。
- 仅看到原Agent的sampled评审存档，未重新观看视频，不对作品视觉质量作结论。

## 修复顺序与影响

1. 消除摘要互斥display类，补长目标收起/展开的真实布局回归；取消/历史委托用紧凑明确状态展示。无需新增依赖或运行时计算。
2. 将用户看到的画布身份与发送/工具上下文可靠对齐；保留多关联与明确跨图操作，不能只用标题推断或全局替换默认。
3. 用实际生成的素材身份核对图关系，区分动态节点输入与固定快照；先复用正确资产，避免为补连线再次收费生成。

本轮只读核查、隔离静态渲染与项目记录；未改业务代码、真实画布、模型配置，未发送指令或触发媒体生成。

2026-09-16 接续：用户确认后的源码修复及验收已完成，详见 `.omx/reports/2026-09-16-canvas-context-fixes.md`。活动身份根因进一步确认为误用已关联时 no-op 的 link 合同；真实业务连线与素材仍未自动迁移。
