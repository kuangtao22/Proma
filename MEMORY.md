# MEMORY.md

## 项目

- 名称：Proma
- 仓库：`Proma-git`
- 技术栈：Bun workspace、TypeScript、Electron 43、React、Jotai、Vite、Tailwind CSS、Radix UI、Pi Agent Runtime
- 初始化日期：2026-08-18

## 长期约束

- 2026-09-08 发布时 `github.com` Git HTTPS 持续超时，但 `api.github.com` 可用；可通过 Git Data API 上传同一 blob/tree/commit，保留作者、提交者、时区和原始 message，逐层比对本地与远端 SHA 后以 `force: false` 快进分支。Windows 按该完整 SHA 预检成功后，再创建同 SHA 的轻量标签；本次 API 建标签已实际触发 `push.tags` Release 工作流。为什么这样处理：网络传输故障不应导致重写历史或绕过预检；对用户的影响是仍发布原验证提交。沿用系统 keyring 的仓库账户，仅注入子进程环境，不保存凭据值；本次临时工具位于 `/private/tmp/proma-bone11-remote.ts`、`/private/tmp/proma-bone11-sync-api.ts`。
- 2026-09-07 推送时发现本机 GitHub 多账户的活动身份可能与目标仓库不匹配：`taokoplay` 对 `kuangtao22/Proma` 只有读权限，HTTPS push 会返回 403。先用 `gh auth status` 确认已有登录；优先从系统 keyring 取得已有 `kuangtao22` 账户凭据，仅注入本次 Git 子进程的 `GH_TOKEN`，清空该次 Git 的默认 credential helper 并使用 `gh auth git-credential`，无需切换全局活动账户。此前临时切换账户后恢复的方式也可用，但单次子进程方案避免干扰并行任务。凭据不输出、不记录值、不写入文件，也不修改全局 Git 凭据配置。
- 用户于 2026-09-07 补充 Canvas 详情应在点击外部区域时关闭；外部点击复用原关闭入口，保留未保存内容的确认，确认期间暂停再次关闭。详情内部及其 Portal 下拉菜单不触发关闭，拖动卡片、平移和缩放不视为外部点击；只在详情挂载期间订阅点击，不增加高频监听或全图扫描。
- 用户于 2026-09-07 将 Canvas 详情交互确定为固定在所属卡片下方：详情不可独立拖动，移动卡片才改变其画布位置，与卡片共同平移、缩放，超出可视范围由画布裁剪。此要求替代此前独立屏幕浮窗/偏移方案；仍允许按节点调整详情尺寸。实现复用 ViewportPortal 和所属节点瞬时位置订阅，视口手势不逐帧重绘详情正文，旧屏幕尺寸须在首次视图更新前一次换算为世界尺寸，不新增图节点、连线或持久化位置写入。
- 用户要求画布图片、视频首次成功生成时默认选中第一份素材；这只初始化空节点的默认版本，后续生成保留已有选择，默认选中不等于视觉验收，也不自动续跑下游。2026-09-07 主线先接入图片，2026-09-08 视频沿用同一规则，并允许读取空节点时为当前配置对应的已有候选补选；按候选完成时间选首份有效主视频，不能按历史扫描顺序或用旧配置结果覆盖用户新配置。
- 默认使用中文沟通；代码、命令和变量名使用英文。
- 本地存储优先，使用配置文件与 `JSON`/`JSONL`，不引入本地数据库。
- 渲染进程状态统一使用 Jotai。
- 功能变化涉及文档时，修改 `AGENTS.md` 与 `README.md` 前需获得用户允许。
- Electron 正式应用使用官方基线加 Bone 构建号的 SemVer；正式标签、应用版本和更新元数据必须一致，提交说明使用中文。
- 凭据只记录存放位置，不记录具体值。
- 全量测试使用 `bun test --isolate`，避免历史测试中的 `mock.module` 跨文件污染；定向测试仍可直接指定测试文件。
- Windows 发布先按完整提交 SHA 运行独立预检，通过后再发布同一提交；原生编译准备与行为测试分别限时并保留完整诊断。操作顺序、失败判断和验收边界见 `docs/windows-release-runbook.md`（2026-09-07）；这是发布操作规则，自动预检门禁尚未接入。这样减少反复发版，保留真实回归约束且不增加客户端运行时开销。
- Canvas 性能证据要区分生产 JobManager 扫盘测试、隔离 Electron 的真实组件交互和已安装应用实跑；V8 内存使用 CDP 强制 GC 后的 heap 采样，不能用固定的粗粒度 `performance.memory` 声称没有泄漏。这样既能验证优化，也避免把 fixture 或单平台结果当成完整交付。
- 用户要求 Canvas 整理按真实层级、分支和关联聚拢；共用参考素材不能混同执行依赖，也不能凭标题补造业务连线。2026-09-07 已授权并实现 ELK 独立 Worker 整理，保留显式触发与原子位置提交；原画布未自动重排。方案与验收分别见 `.omx/plans/2026-09-07-canvas-relational-layout.md`、`.omx/reports/2026-09-07-canvas-relational-layout.md`。大图验收还必须检查整理后原视口有内容：单参考源的巨大星形图仅用 layered 会拉成超高单列，应聚拢同源末端流程；Worker 完成或帧率正常不能替代几何与可见性验证。

## 架构决策

- 2026-09-10：实际会话日志显示 Agent 工具链正常注入并执行；“能做吗”这一轮只回复能力说明，是模型没有把已有具体目标识别为执行信号，下一轮“继续”才开始调用工具。系统提示现明确：已有目标后出现“能做吗/可以吗/继续”应立即调用首个读取、查询或检查工具，只有缺关键参数、付费或不可逆边界才暂停确认。为什么这样处理：减少用户必须重复催促的一轮对话，同时保留真实风险确认；只增加提示词文本，不增加运行时监听、轮询或资源开销。

- 2026-09-10：LTX 首尾帧节点的 `/object_info` schema 实际可解析，误报来自两处边界：资源目录把“schema 已发现但没有本地执行合同”标成 `unsupported`，以及 `VHS_VideoCombine` 的合法 MIME 格式键被严格 JSON key 校验降级。现允许 object_info 元数据保留 MIME/格式键但继续限制深度、数量、长度和危险键；VHS 输出通过明确的 `gifs` 视频 history 合同接入。资源与 Agent 摘要改为区分 `schemaAvailable`、`support: unknown` 和真正 `unsupported`，仍不会把未知自定义节点自动放行执行。为什么这样处理：解决现有 LTX 工作流无法打开/分析的问题，同时保留上传、路径、输出和动态输入的 fail-closed 边界；对用户的影响是已有 LTX 工作流可被发现和校验，缺少本地适配的节点仍会明确显示为“已发现/只读”，不会静默生成。性能只增加一次有界 schema 元数据解析，无新增轮询或远端请求。

- 2026-09-10：官网 `upstream/main` 已更新至 `4c871eb4`（正式标签 `v0.19.52`），当前 fork 与官网共同基线相差 637/30 个提交，直接合并会删除约 872 个 Canvas、媒体、运维和发布专有文件并破坏接口。已筛选合入与 fork 边界兼容的模型窗口、侧栏、Skill、Vault、Slack、Markdown、模型选择器和 Exa Search 修复；跳过官网移除旧搜索/生图工具及 Copilot/MCP 大架构提交。为什么这样处理：上游功能必须进入现有 fork 接缝，不能用官方删除替换本地业务边界。对用户的影响是保留 Canvas Agent 模型失效恢复、媒体和运维能力，同时获得独立官方体验修复；性能只增加对应界面和本地逻辑，无新增常驻任务或远端轮询。

- 2026-09-09：历史 Canvas Agent session 绑定的渠道或模型被停用时，LOAD 对账现将节点标记为 `AGENT_SESSION_UNAVAILABLE`，复用已有“重建会话”入口使用当前启用模型；运行前仍 fail closed，并返回要求重建会话的可操作提示。为什么这样处理：不能静默把旧任务改派到另一渠道，但也不能让失效配置一直伪装成可用节点。对用户的影响是旧节点会明确显示恢复入口，重建后保留节点、连线和旧会话记录；对账只增加每个 Agent 一次模型目录校验，无轮询、网络请求或新增依赖。

- 2026-09-09：补充兼容没有历史事务凭据的旧 Canvas Agent 节点：只要其 session 仍有完整 Canvas 归属且模型失效，LOAD 同样派生可重建问题；缺少 session 或归属证据仍保持兼容路径，不凭空阻断。为什么这样处理：旧数据不能依赖新事务文件才能获得模型失效诊断。对用户的影响是截图中的旧节点也能显示“重建会话”，无需删除画布或手工重建全部节点；每个旧 Agent 仅增加一次本地模型可用性检查。

- 2026-09-09：GPT Image 2 的一次真实请求约 32.5 秒完成；原 `canvas_get_task` 仅等待 30 秒，正常成功会先被 Agent 看到 `running` 超时，造成“没有候选图片”的假性失败。图片任务等待上限现统一为 60 秒，重试回执和 Agent 续行提示复用同一常量，等待仍在 IPC 锁外且有界。为什么这样处理：等待窗口应覆盖已观测的正常服务延迟，避免误报和重复付费重试，同时不能变成无限阻塞。对用户的影响是 GPT Image 2 在 60 秒内完成时会直接返回真实终态，超过上限仍需沿同一 job 查询，不改变生成、候选采用或资源开销边界。

- 2026-09-09：用户要求生图模型选择器显示当前供应商名称和模型名称，已选值与下拉项保持一致；供应商名称从当前渠道目录读取，不能用重复的模型 ID 或历史任务名称冒充，模型 ID 保留为悬停信息。展示名称不进入生成路由、持久化配置或历史快照，不增加网络请求和依赖。88 项相关测试、全仓类型检查、主进程构建及隔离 Electron 宽窄菜单验证通过，未提交真实生成；当前客户端界面操作未响应，未确认后台空闲，故未强制重启，新增主进程字段待下次启动生效。

- 2026-09-09：模型切换渠道后，Canvas 生图底部“重试生成”始终复用旧任务快照，导致反复出现快照不一致且没有按新配置生成的入口。现主按钮始终先保存当前草稿再新建任务，失败、取消、中断后显示“按当前配置生成”；历史详情保留并明确标注“按原配置重试”，模型或渠道变更时继续拒绝旧快照并给出重新生成提示。为什么这样处理：用户修改当前配置与重放历史执行是不同操作，不能为消除错误而静默改派旧任务渠道。对用户的影响是换渠道或改参数后可直接生成，当前模型不可用、配置冲突和运行中仍受原校验约束，已有素材及任务历史不改写；不新增监听、网络调用或依赖。330 项相关回归、全仓类型检查、主进程构建与隔离 Electron 宽窄窗口真实点击验证通过，确认旧渠道失败后的主按钮零历史重试调用；开发实例空闲后已重启，未提交真实付费生成，未验证新渠道生图服务可用性。

- 2026-09-09：用户反馈图片重试后“无法执行下去”，现场实际为 GPT Image 2 渠道返回 503 `No available compatible accounts`，不是 ComfyUI 或节点配置错误；工具此前把异常包装成普通结果，Pi 因而记录成功并导致任务只显示“没有产生可验证图片”。现保留单次生成的终止语义，通过 Pi 原生结果钩子将完整、匹配调用身份的失败标记为错误，让任务详情沿用真实诊断。提交或重试后 Agent 须有界等待同一新任务，等待期间释放画布锁，结束后复验权限与节点身份；错误、取消和任务消失均回收监听，渠道故障不触发反复付费重试。为什么这样处理：提交回执不能替代生成终态，读取失败也不能误判为节点内容损坏。对用户的影响是后续失败可见、不会只回复“已提交”，已有素材与失败历史保持不变；仅复用任务事件，无后台轮询或新依赖。436 项相关回归、全仓类型检查、主进程及 Agent runtime 构建通过；完整构建另在原生 Agent Island 的 Swift 缓存写入处受沙箱限制。已确认开发实例空闲后重启并恢复原会话画布，默认 Skill 已同步到 1.0.19；未再次付费生成或证明渠道恢复，不能将测试通过等同真实生图成功。

- 2026-09-09：用户授权优化画布节点“审核并修复”。审核读取现逐节点保留结果和脱敏失败诊断，明确缺失节点、未返回关系与数据完整性；异步读取后复验权限、停止和图版本，删除或覆盖遇到版本冲突不再自动换基线重试。为什么这样处理：读取失败不能当成内容不合格，旧审核结论也不能覆盖用户刚修改的节点。修复沿用本任务授权、已有媒体策略和原工作流预算，优先原地更新，必要替换先验证新节点、迁移全部关系再回收旧节点；默认最多两轮是 Agent 编排约束，不是新增 Host 审核状态机，音视频元数据仍不代表内容验收。对用户的影响是局部故障不再中断全量审核，也降低误删与重复生成风险；保留原分页、32 节点和响应预算，无新增依赖、后台轮询或远端生成。359 项相关回归、全仓类型检查及主进程构建通过；开发客户端当时仍在生图，因此未重启，新工具逻辑待下次启动生效，未以真实模型对话验证整套自动修复。

- 2026-09-09：图片候选列表可查但精确预览报 `CANVAS_IMAGE_VERSION_LIST_FAILED`，根因是 IPC 向图片 Registry 传递 JobManager 裸方法，导致实例 `this` 丢失；独立列表使用成员调用，因此正常。装配改为保留接收者的闭包，底层异常仅记本机日志，Agent 仍收到固定阶段、错误码和中文提示。为什么这样处理：预览失败不代表生成或素材失败，不能靠重新生图或提前采用绕过；不增加扫描、网络请求或依赖。回归夹具须保留实例调用约束，箭头函数 mock 会掩盖此类问题；同候选先列表再预览、归属拒绝及零生成/零采用断言已通过。333 项相关测试、全仓类型检查和主进程构建通过，开发客户端已重启并恢复原画布；未发起真实 Agent 对话重试或生成。

- 2026-09-08：用户要求图片生成按钮与视频运行按钮一样始终位于工作台底部。图片工作台现复用音视频的容器布局，预览与配置各自滚动，生成、取消和重试操作放在配置滚动区之外；错误信息限高可滚动，配置冲突时恢复按钮与主操作同排。为什么这样处理：外层整页滚动配合 sticky 无法保证长参数和窄面板下的底部位置；纯布局调整不改变生成、保存或采用逻辑，也不增加监听、网络请求或依赖。24 项相关测试、全仓类型检查和独立 Electron 滚动点击验证通过，覆盖 840×560、430×320 及扣除标题栏后的 360×276 长错误状态；未提交真实生成任务。

- 2026-09-08：视频生成成功后此前只挂接候选，详情仍要手动预览。现为空节点默认采用当前配置对应的首份有效主视频及完整 bundle，复用资产复验、CAS 和可恢复传播；以默认来源区分初始化与明确验收，工作流恢复仅认可用户或 Agent 明确采用。详情优先预览当前版本，手动查看历史后保持选择，默认读取失败可见且不随刷新重复重试；切换目标和关闭回收预览授权，返回空节点也能重新初始化。为什么这样处理：生成完成即可看到结果，同时保留用户选择和下游验收边界。只增加首次选择事务和按需本地预览，无新增远端生成、下载、轮询或依赖。161 项定向测试、全仓类型检查及隔离 Electron 交互通过，开发实例已重启；原画布的人工点击复验因 Mac 锁屏尚未完成，不能将隔离素材验证视为原视频实测。

- 2026-09-08：用户再次反馈视频详情过大，根因是详情首次默认宽高先按屏幕面积计算再除以画布 zoom，低倍展开时外壳膨胀而正文仍缩小；仅调小默认数值不能解决。默认尺寸现按画布世界坐标定义，首次视口约束只减小尺寸，与卡片共同缩放。旧内存缓存无法区分自动记录与用户手动调整，因此用当前节点的“恢复默认大小”显式纠正，不批量覆盖其它自定义尺寸；恢复不重挂载正文、不清空参数或播放状态。为什么这样处理：尺寸和内容必须共享同一坐标系，缓存纠正也须保留用户意图。验证须覆盖真实 Overlay、ViewportPortal 和 Jotai 的首次展开、关闭重开及手势，不能只挂固定尺寸的媒体正文；本轮相关测试、类型检查、宽窄交互及原视频播放通过，无新增远端请求或逐帧状态更新。

- 2026-09-08：视频详情的 `MEDIA_FILE_BUSY` 可由同进程详情读取、StrictMode 和后台进度读取争用同一模块文件锁触发，并不表示视频未下载。媒体模块的 LOAD/CAS 在进程内按完整目标串行，出队后复验授权，失败释放后继并清理队列；跨进程仍保留原文件锁，不同模块继续并行。为什么这样处理：合法并发应等待，不应随机失败，也不能靠移除文件锁破坏 CAS。视频详情默认缩至 840×560，保留左右分栏和用户调整尺寸；真实开发客户端已读取并播放原本地 MP4 至 3.042 秒，文件完整性和独立 Electron 解码也通过，未重新生成或下载。

- 2026-09-08：Agent 历史图片采用的 replay 查询曾将节点、Job、素材等字段一并传给仅接受 projectId/canvasId/batchId 的严格接口，导致 `CANVAS_IMAGE_CANDIDATE_BATCH_INPUT_INVALID`；调用必须显式构造三字段，继续使用原稳定 operationId，不能复用首次自动采用的 job 批次，否则会妨碍再次选择旧版本。为什么这样处理：重放同一操作与用户重新发起采用是不同意图，回归须穿透真实参数解析器。现场两张图片及缩略图有效，重开原画布可显示；历史 `image-unavailable` 和保存失败未能复现，因此补充图片读取阶段、固定错误码及中文诊断，并在本机控制台保留保存失败信息，不能把文件存在或重开成功当成已证明历史根因。

- 2026-09-08：用户要求音视频工作台采用与图片工作台一致的左右布局：左侧预览、运行状态和历史版本，右侧工作流、服务器及输入参数，两侧独立滚动，保存和运行操作固定在右侧底部。为什么这样处理：长参数表单不应把预览和主操作推到页面之外；根据工作台自身宽度切换分栏，共享输入表单也按容器宽度排列，窄面板保持可访问。预览仍由显式点击读取，保存、运行、导出与采用沿用原候选身份和权限；纯 CSS 容器查询不增加尺寸监听、轮询、远端请求或依赖。

- 2026-09-08：用户要求工作流图片输入的素材列表直接显示图片。图片与音视频工作流共用的表单现以缩略图、文件名和尺寸展示全部项目图片，支持按文件名搜索、键盘选择，并在收起后保留已选预览。为什么这样处理：UUID 文件名无法帮助辨认参考图，预览应复用已导入素材而不重新访问 ComfyUI。缩略图通过项目和完整素材引用读取，缺失时明确提示且不回退原图；仅可见行读取，离屏、关闭和项目切换释放 Blob，不新增远端请求或目录轮询。100 张目录的宽窄窗口交互验证覆盖滚动到末尾、重名身份、搜索、预览错误和迟到响应回收，未提交真实生成任务。

- 2026-09-08：用户纠正媒体生成不能无条件逐次审批，按时计费的 ComfyUI 实例在等待确认期间仍会产生费用。媒体配置现提供全局“每次确认 / Agent 自主执行”，旧配置默认每次确认；自主模式覆盖已装配媒体能力的普通 Agent 和 Canvas 运行控制，并授权当前任务所需的选型、生成、检查采用及无匹配时根据服务器实际模型/节点创建并保存本地工作流，不再反复询问。为什么这样处理：工作流发现、创建和运行若使用互相冲突的审批规则，所谓自动执行仍会中断。授权保存后通过同一主进程 Store 的事件释放当前等待的媒体审批，后续调用读取最新策略；切回逐次确认恢复新调用的审批，已准备/提交的任务内容与远端身份不变。计划模式、停止、任务预算、作用域和防重复提交仍生效，非媒体强制审批不受此设置影响；独立定时任务产品的 Canvas scope 装配没有在本轮扩张。实现沿用 JSON 原子写和 CAS，只在待审批期间订阅变化，不新增配置轮询或远端请求。真实 Store、Pi 参数校验、工具策略与审批服务的隔离集成已覆盖自动放行、恢复逐次确认和自主保存，uploads/submits 均为零；宽窄窗口验证标题在上、授权在下，保存失败可见且恢复权威设置。本条替代此前所有无匹配都必须再取得一次对话确认的约定，该要求现在只适用于“每次确认”模式。

- 2026-09-08：用户确认 ComfyUI 新任务优先检查绑定服务器的工作流，选中后直接使用，执行快照由 Host 自动缓存，不要求手动导入、创建或发布。完整目录无匹配时先说明缺口并询问，用户同意后根据服务器实际模型、节点和接口生成并另存本地模板；目录读取失败、分页未完、筛选无结果和转换失败都不能等同无匹配。设置页统一称“本地工作流”，隐藏内部快照与旧项目分类，但保留原任务引用；本地模板跨项目复用，素材和执行参数仍项目隔离。为什么这样处理：已有远端工作流应直接服务任务，新建模板只在用户需要时发生；内容去重和按需读取避免重复模板及新增轮询。新建许可是 Agent 对话确认及工具意图声明，不增加重复保存审批，也不宣称 Host 能独立证明自然语言确认；显式指定连接仍遵守原覆盖默认绑定的合同。605 项相关测试、全仓类型检查、Electron 构建和隔离宽窄窗口验证通过；真实存储/工具参数/表单集成使用模拟远端传输，未进行真实模型对话或提交生成。本条落实并替代此前“仅建议远端直接使用”的记录。

- 2026-09-08：用户要求所有 Canvas 节点首次运行和已有内容重跑时都有加载动效。统一卡片标题栏承接排队/运行反馈，Agent、图片、音频、视频、文档和原型共用；媒体进度与既有运行事实合并，结束后停止，原有内容保留，系统减少动态效果时保留静态状态图标。为什么这样处理：运行反馈应由任务状态驱动，不能依赖空卡片占位或某一种节点专属 UI；CSS 动画不增加逐帧 React 更新、网络请求或后台轮询。已在真实浏览器与隔离 Electron 验证动画帧变化、终态收口、宽窄窗口和减少动态效果，并通过 1000 节点性能回归。

- 2026-09-08：用户进一步询问为何区分公私有、为何不能直接使用 ComfyUI 服务器工作流。分析结论与建议：远端工作流可作为直接选择来源，本地缓存、字段映射和任务运行快照由系统自动维护，不应要求用户先导入或发布模板；本地另存模板只作为可选复用功能。现有必须登记本地工作流的步骤来自运行接口对本地 ID/版本的依赖。任务素材与输入继续项目隔离，UI 工作流仍需转换为 API 执行图，取消导入步骤本身不能解决自定义节点转换问题。本轮仅解释与建议，尚未实施此方案。

- 2026-09-08：核查用户截图发现，全局工作流管理与 Agent 导入尚未完全统一：新导入的项目草稿也被设置页归入“旧项目历史”，不能将其解释为仅存量兼容。此处公共/私有表示本机跨项目复用范围，不是 ComfyUI 账号的公开权限。后续统一应延续已确认的全局模板方向，同时保留项目素材、任务参数和执行快照的隔离，避免将项目内容直接扩散到其它项目。本轮仅核查并解释现状，未迁移工作流或改变功能。

- 2026-09-08：用户反馈音视频卡片创建修复后仍无法绑定工作流，实际会话的 `canvas_update_media_config` 传入 `outputs: [{ key: '92.video', mediaKind: 'video' }]`，缺少必填 `role/order`；共享解析器的 `CANVAS_MEDIA_OUTPUT_INVALID` 又被保存入口包装为无详情的 `CANVAS_MEDIA_SAVE_INPUT_INVALID`。这与前一轮卡片创建时的内容类型拒绝不同，也未到 ComfyUI 远端生成阶段。工具原先的 `Type.Unknown()` 数组已替换为完整输入/输出 schema，保存错误按受信字段位置提示，持久化解析保持原错误码；输出角色仍须明确选择，不能替多输出工作流猜主输出。为什么这样处理：Agent 必须看得到真实接口字段，失败后才能有依据地纠正参数；对用户的影响是可在原卡片保存工作流与部分输入，并看到具体缺失字段。183 项关联测试、全仓类型检查、Electron 构建及独立复查通过；现场参数补齐后可解析，真实 Service/Store 的隔离草稿往返和未创建运行已验证。当前开发客户端已重启加载修复，本轮未手工改动原卡片、未进行真实模型工具调用或视频生成，未重新发布安装包。诊断仍为最多 128 项的单次解析，无新增依赖、网络请求或后台任务。

- 2026-09-08：音视频先建卡片的回归必须穿透产物服务与真实内容 Store，并至少覆盖临时磁盘 helper；只 mock 内容准备会漏掉底层类型、空正文和重复准备合同不一致。此次 `CANVAS_CONTENT_KIND_INVALID` 在本地创建阶段发生，与 ComfyUI 连接及工作流转换无关；补齐音视频内容类型、允许空正文并按媒体状态复验重放，保留未知类型、非空正文和损坏配置的拒绝规则。为什么这样处理：已支持的上层工具不能被旧存储校验拦截，重试也不能覆盖原配置。对用户的影响是音视频卡片可先创建再填写参数；265 项相关测试、全仓类型检查及 Electron 构建通过，验证只使用隔离数据，未提交真实生成。无新增依赖、网络请求或后台任务。

- 2026-09-08：用户确认 Canvas 生成先创建或复用目标图片、音频、视频卡片，再分析工作流并绑定精确版本与服务器，按真实节点输入展示参数；允许保存已完成部分，缺失字段保持待配置且不自动补回模板默认值，完整校验后才从原卡片运行。分析失败由 Agent 将有界诊断写回原卡片，运行准备失败由 Host 持久化可信字段错误，未知异常只保留稳定中文提示；进度、错误与候选留在原卡片。音视频的纯诊断写入只能推进外层状态 revision，不能改变父工作流冻结的配置 revision，否则恢复或重试会误报配置变更。为什么这样处理：用户应先看到可编辑的生产对象，失败后继续补齐配置，不因预检失败丢失上下文或创建无效任务。沿用既有 LOAD/进度订阅与有界字段校验，不增加后台目录轮询。本轮相关 532 项测试、全仓类型检查、Electron 构建及隔离窗口交互验证通过，开发客户端已更新启动，未提交真实生成任务；此规则取代此前“图片不完整工作流输入只保留本地草稿”的约定。

- 2026-09-08：Canvas 图片工作流在 `resolveAvailableWorkflowSnapshot()` 创建运行快照前，必须本地校验输入 key 集合、必填字段、标量类型/范围/步长和 `MediaAssetRef` 的精确形状；失败统一抛 `MediaWorkflowValidationError`，通过 `INPUT_REQUIRED`、`INPUT_UNKNOWN`、`INPUT_TYPE_INVALID`、`INPUT_RANGE_INVALID` 携带可信 `nodeId/input` 与白名单中文文案，供画布持久化 preparation 错误。为什么这样处理：不完整草稿若先创建 Job，失败会滞后到运行准备阶段且难以恢复为可编辑状态；对用户的影响是点击生成立即看到具体字段错误，不产生无效 Job 或远端请求。性能上仅遍历最多 128 项输入和绑定，不读取素材、不调用网络。

- 2026-09-08：MiniMax Ref 的 `r2v` 两份快照与首尾帧节点 105 的实例 7 个输入/定义 11 个输入矛盾不是同一问题，Ref 均无子图。转换器现已支持服务端可证明且有界的 `COMFY_AUTOGROW_V3` 的 `prefix + max` 命名模板；仅在 schema 明确只有一个静态可序列化控件时兼容标量 `widgets_values`，不能把任意标量盲目包装成数组。普通 Ref 已离线完成转换与编译，AutoVideo Ref 可通过分析，但其节点 115 默认 `megapixels=0.98` 不符合服务端 `min=0.1/step=0.1`，仍须修正输入；首尾帧 7/11 定义矛盾继续明确阻断。为什么这样处理：序列化兼容缺口应修在转换边界，真实配置冲突不能靠猜测绕过。对用户的影响是可导入更多已有工作流，同时保留可定位的输入校验。本轮未修改原始工作流或提交生成。

- 2026-09-08：ComfyUI UI 转换应以服务端 schema 和可证明的前端序列化为依据，保留 `input_order`、`output_matchtypes`，区分上传辅助控件、已连线 widget 占位、动态分支与具名自增输入；子图只展开明确边界并按实例传递参数，接口不一致不得猜测。旧 schema 快照需按格式版本尝试一次迁移，失败保留旧内容且同实例不重复联网。设置详情保留原始 JSON 并展示有界问题；Agent 导入和准备失败抛带错误码、节点、字段及中文原因的可信错误，完整转换问题可分页读取。真实 LTX 首尾帧快照已离线通过转换与编译；MiniMax 的节点 105 存在实例 7 个输入与定义 11 个输入不一致，仍须明确阻断。本轮未提交生成任务。

- 2026-09-08：真实首尾帧工作流接入核查发现 UI 转换与 API 校验边界不一致：远端目录已有 LTXVPreprocess / LTXVAddGuide，最小 LTXVPreprocess API 图可通过静态校验，UI 图却因缺少本地名单被拒绝。LTX 样例同时涉及上传辅助控件、MarkdownNote、动态输入，MiniMax 样例还包含子图；不能将这些转换缺口解释为服务器缺节点，也不能保证导出 API 后整图可执行。后续应复用既有 API 节点校验并补齐可证明的 UI 序列化语义，避免逐节点盲加名单或要求用户重建工作流。本轮只读原始调用记录与资源快照并离线复现，未提交生成任务。

- 2026-09-08：画布视图更新必须让无变化输入保持原状态引用，不能仅在调用方返回空对象后仍发布新状态；否则工作台 Effect 依赖的回调随父渲染变化时，会形成持续更新并导致卡顿。判定须包含旧尺寸迁移结果，保留首次迁移和真实草稿变化；采用浅比较，避免深度遍历与无效 Map 复制。回归需覆盖重复通知的反馈链，不能只靠静态渲染判断没有循环。

- 2026-09-07：运维文件页在 StrictMode 重挂载或快速切换时，固定窗口级 SFTP owner 会与异步清理碰撞，真实主进程日志位于 `/private/tmp/proma-main-electron.log`，错误为 `SERVER_OPS_SFTP_OWNER_CLOSED`。文件 owner 必须按可信窗口和主机隔离；同一主机重开复用稳定 runtime key，但以页面对象代次拒绝旧等待者，并等待前一轮 utility 清理 ACK 后再读。`closeOwner` 必须向 IPC 返回真实清理 Promise，不能提前报告完成。这样切页不会误伤新请求，也避免每次重开累计新的 runtime owner 记录；仅增加内存索引和已有清理时的等待，不增加轮询。114 项相关测试、全仓类型检查及隔离 Electron 的真实 preload/IPC/utility/localhost SSH 验证通过，连续三次关闭后立即重读成功且最终远端 handle 为零；开发主进程已重建，未重启用户客户端、访问真实服务器或发布安装包。

- 2026-09-07：用户提供的远端 ComfyUI `http://100.124.186.117:8189/` 已完成真实服务层联调：0.30.0、RTX 3090 Ti，1787 个节点、26 个工作流，Assets API 未启用。图片、音频、视频上传与回收及 SDXL 512×512/6步生图成功，采样进度 1/6 至 6/6 可追踪，任务恢复未重复提交。该实例需要兼容 nullable 元数据、官方列表标记和 V3 COMBO；第三方不兼容 schema 必须保留为可发现但不可执行，不能阻断完整目录。公共工作流上传槽不能拿占位值匹配已有文件枚举，必须由真实上传回执填充。远端已有 UltraSharp 放大工作流存在 float/Half 精度错误，发现模型或工作流不代表其运行健康。报告与样张在 `.omx/qa/2026-09-07-comfy-live-verification.md` 及同名产物目录；本轮没有验证语音、音乐或视频扩散模型生成质量。

- 2026-09-07：用户要求普通 Agent 作为统一入口，高自由度操作画布并顺畅完成电影制作、整套 App 交互、运营计划、数据采集分析。后续验收必须覆盖任务拆解、资源发现、真实执行、产物传递、质量检查、失败恢复和最终交付，不能以工具已注册或组件测试通过替代完整业务验收。当前媒体分支只读审查表明四类场景仍有执行与产物衔接缺口，尚未完成真实端到端验证；此次审查没有扩展执行权限或改动运行逻辑。

- 2026-09-06：Canvas 跨节点生产使用持久 `CanvasWorkflowRun` 固定首次可达范围、节点结构身份、typed DAG 输入槽、完成事实、精确图片 `batchId/taskId`、媒体 `runId/outputKey` 与剩余预算；恢复只推进固定计划内未完成后继，新增后继必须另走显式重规划。媒体输入只能由 Host resolver 按 `targetInputKey + sourceNodeId + sourceOutputKey` 解析，并在提交前固化配置 revision、来源内容 hash 与解析值 hash，禁止从 `context.text` 或 Agent 正文猜台词和任务 ID。为什么这样处理：Canvas 边只证明节点关系，不能证明具体 ComfyUI 输入槽或多输出角色已采用，进程内状态也不能证明重启后应继续哪些任务；对用户的影响是图片、音频、视频和 Agent 工作流可跨重启继续，旧候选不会冒充本轮结果，采用单个音轨只释放依赖该音轨的后继。性能上每个工作流最多 32 个节点，journal 为有界 JSON，恢复只做 O(n+e) 校验与按需精确事实查询，不扫描媒体文件或远端目录。

- Canvas 终态归档的删除 CAS 必须使用稳定扫描读到的原始正文；同轮推进事务时改用实际写入且已确认可见的正文，不能把 parser 重建后的对象重新序列化当作磁盘证明。旧日志即使业务值完全相同，也可能因字段顺序不同而阻断 LOAD。恢复历史采用须逐条定位当前未决 operation，以完整图哈希和原历史输出证明已提交，再备份并仅补齐事务状态；修复一条历史事务或只打开客户端窗口不能作为原画布恢复验收。2026-09-07 的 S2-A 证明、备份位置与归档字段序现场见 `.omx/reports/2026-09-07-original-canvas-load-recovery.md`。
- Electron IPC 变更必须依次同步：共享类型与通道常量 -> 主进程处理 -> Preload 桥接 -> 渲染进程调用。
- Canvas 持久工作流的 operation 身份必须包含父会话与首次运行代次；同会话后来运行可读取、继续和取消原记录，但重新执行只能重放原代次。崩溃恢复不能仅凭子 Agent 空闲判定成功，必须核对原用户消息锚点、输出 UUID 和正文哈希；输出被后来运行覆盖时持久标记需重新规划，禁止驱动下游。这样可避免恢复时重复计费或采用错误输出，额外核验只发生在关键恢复状态。
- Canvas 不再作为左侧独立会话、顶部标签或独立主视图存在，只通过普通 Agent 的右侧“画布”工作区访问；项目级图事实仍按 `projectId + canvasId` 共享，视口、选区和详情状态按普通 `sessionId + canvasId` 隔离。首次打开 `legacy-design` 必须先在 workspace 写守卫内幂等初始化真实 Design 文档并进入 Canvas registry，再沿用普通 Agent、项目归属和画布存在性校验建立 binding，禁止用悬空关联或绕过校验兼容旧数据。
- Canvas 标签打开状态与 binding 解耦，默认隐藏且后台产物不得抢焦点；用户从顶部“打开画布”显式进入时，必须等待项目画布 metadata 与 binding 同时就绪，再按默认画布、第一个未归档画布、自动新建依次回退，并对同一入口的在途动作做单飞去重。
- Agent-Canvas 关联 IPC 只允许已授权主窗口操作同项目普通顶层 Agent 与已登记 Canvas；LIST 按实时会话/画布事实清理陈旧关联，事件使用 shared exact-key parser。Canvas、普通 Agent 或工作区主删除成功后的关联清理均为带精确事件的 best-effort 后置步骤，失败不得反向击穿主删除，内部会话不参与。
- Agent 消息携带 Canvas 节点引用时，主进程必须在飞书镜像、启动槽位、Orchestrator、started 事件或 Pi 注入等消息接管副作用前，基于 fresh 绑定、Canvas registry 与权威文档完成单次解析；引用字段缺失才表示无引用，字段存在时必须先是数组并严格解析，只有合法空数组可 bypass，queue-now 转换必须保留该字段的 presence，禁止把缺失归一化为显式 `undefined`。deferred 仅在 prepare 成功后移除队列投影并发布 started：空闲会话首次接管失败必须同步 reject 且移除本项；已异步接管的旧队首后来失效必须发布不含 cause 的稳定 failed 状态、终结该项并继续调度，Renderer 依据本地队列投影幂等恢复正文、附件、引用和旧 quoted selection，且旧选区只能在当前无新选区时补回。queue-now 只允许 Pi 注入阶段的 stale-active 降级排队。新发送缺省使用 latest 刷新到发送时 revision，历史消息重试必须通过显式发送字段使用 exact，不得从可选错误 UUID 推断；exact 的项目、Canvas、节点、类型与标题必须全部按权威文档重建，原 revision 当前无法证明时必须失效，禁止返回 Renderer 原对象或以当前版本替代。引用失效跨 IPC 只暴露稳定 `CANVAS_REFERENCE_INVALID` code/message，内部 cause 仅写主进程日志；prompt 摘要必须使用转义标签敏感字符的 JSON 数据编码，并在 I/O 前限制单条消息最多 32 个引用、编码后摘要最多 8192 字符。SDK JSONL 只保存发送时真实 revision 的 `_canvasNodeReferences` 快照，历史渲染不得回查当前节点替代。
- Agent 仅使用 Pi Agent Runtime；主进程通过独立 utility process 与 `agent-runtime-client.ts` 管理每个会话，禁止重新引入 Claude/Codex Agent SDK。
- Agent 自动标题统一复用 Pi ModelRuntime 的模型、渠道、认证、协议和代理解析；标题使用独立无工具、且不主动启用推理的会话，任何非中止失败均回退为首条用户消息，Chat 对话自身仍保留 Provider Adapter 路线。
- Pi runtime 与 `sharp` 等 external 依赖由 `sync-runtime-deps.ts` 在打包前同步，桌面构建同时包含自包含的 Proma CLI 与原生辅助模块。
- fork 保留 LAN Bridge 与 `apps/mobile`；开发 Renderer 统一使用 `127.0.0.1:5174`，LAN Bridge 配置与打包版共享。
- 所有桌面安装包入口统一通过 Electron workspace 的 `package:prepare` 构建 Electron、移动端并同步 runtime external 依赖，避免本地与 CI 打包步骤漂移。
- LAN Bridge 配对 PIN 仅在桌面设置界面显示，不注入移动网页、不写日志或明文文件；配对失败按客户端 IP 独立限速。
- fork 桌面安装包使用 `com.bone.proma.app` 作为系统应用标识，与官方版 `com.proma.app` 区分；保留 `Proma` 产品显示名和现有数据目录。
- 桌面版本前三段表示当前合入的官方版本，`bone.<构建号>` 表示 fork 发布修订；同一官方版本递增 Bone 构建号，合入新官方版本后重置为 1。
- 开发版与打包版统一使用 `~/.proma` 存储 Skills、模型、渠道、会话等业务数据；仅 Electron 内部 `userData` 保持开发实例隔离。
- 正式版身份固定为 `Proma` / `com.bone.proma.app`，直接启动的开发版固定显示为 `Proma Dev` / `com.bone.proma.dev`；两者隔离 Electron `userData` 和单实例锁，继续共享 `~/.proma` 业务配置。开发版在 Electron `ready` 前固定使用 `@proma/electron` Safe Storage 身份，禁止把展示名直接用于加密身份。
- 从纯 `0.17.42` 迁移到 `0.17.42-bone.5` 需要手动安装一次；后续 Bone 版本使用预发布 SemVer 正常递增更新，不开启全局降级。
- fork 的 LAN、移动端与后续公网能力必须保持为低耦合扩展层；每次合并官方 Proma 新版本后，通过协议契约、适配层和自动化兼容检查确认自有功能不回退，同时继续获得上游新功能与 Bug 修复。
- LAN 扩展固定按版本化协议、Proma Adapter、IPC、Preload 与 UI 分层；官方运行时服务只允许由 Adapter 接入，降低上游模块重构对移动端功能的影响。
- LAN Bridge 虽订阅全局 AgentEventBus，但普通 Agent 状态推送必须先通过用户可见性边界排除 Design/Canvas 内部会话；异步状态读取异常只能降级当前推送并记录日志，禁止击穿 Electron 主进程。
- LAN 设备认证由进程级唯一 `AuthService`/`DeviceStore` 管理；一次性 ticket 仅经 URL fragment 和内存传递。移动端长期设备凭证永久有效直至桌面撤销，桌面仅保存其 SHA-256 哈希；访问令牌固定 15 分钟，受设备版本与撤销状态约束，IP 只作审计元数据，不参与身份校验。
- 上游兼容检查每周临时合并最新正式 `v*` 标签后运行协议、接缝、测试、类型和构建验证；Workflow 保持只读，不 push、不创建 PR、不发布。
- LAN Bridge 停启复用进程级 HMAC；可信设备通过长期凭证自动签发新的 15 分钟访问令牌，Bridge 重启或 DHCP/IP 变化不会要求重新配对，重新扫码会轮换该设备的凭证和 Token 版本。
- 客户端必须先完成 `protocol.hello` 协商，业务层只信任 hello 返回的版本和能力；服务端升级主版本但保留旧协议范围时，旧移动端仍可配对。
- Bridge 启动按生命周期代次串行化；未认证连接从建立起严格 15 秒截止、单 IP 最多 3 个、入站消息最多 64 KiB，超限连接立即释放；外部会话 ID 在 Handler、Adapter 和配置路径三层校验。
- 手机端视觉与桌面端共享语义设计语言，但主题映射保留在 `apps/mobile` 内；默认通过 `prefers-color-scheme` 跟随系统明暗，不导入桌面 Renderer CSS，也不扩展 LAN 协议或持久化字段。
- 手机端 Agent 会话运行态由主进程四态快照与全局 LAN 推送共同驱动；实时事件必须同时更新会话列表和当前会话，重连时以列表快照恢复，星标只采用服务端确认值。
- fork 的标签发布工作流覆盖 macOS arm64/x64、Windows x64 与 Linux x64；Linux 同时提供 AppImage 和 deb，并与其他平台产物汇总到同一个 GitHub Release。
- Linux deb 打包依赖 `apps/electron/package.json` 的 `homepage` 元数据；发布合同测试必须覆盖该字段，避免 AppImage 成功但 deb 在 FPM 阶段失败。
- Electron workspace 使用带 scope 的包名，Linux 必须显式配置不含 `/` 的 `artifactName`，否则 deb 默认输出路径会被拆成不存在的子目录。
- 路径管理采用固定 `~/.proma-location.json` 定位文件与重启迁移模式：业务模块继续通过 `getConfigDir()` 解析当前数据根，复制、校验和 Proma-owned 绝对路径重写全部成功后才切换；项目文件迁移独立使用工作区锁，源目录始终保留。
- 数据根迁移计划在正常进程中记录的容量只作为预检值；隔离迁移进程可为 `pending/failed`、零进度且无复制 sidecar 的计划重建最终容量基线，兼容退出流程写入。已有复制进度或 sidecar 的断点继续严格拒绝源容量变化。
- Bone 发布说明以 `release-notes/bone/v<version>.md` 为唯一事实来源；发布前必须校验对应文件存在且非空，GitHub Release 正文与关于页“Proma 修改”历史共同使用该内容。
- macOS 发布优先使用 Developer ID Application 签名与 Apple 公证；证书未配置时允许发布明确标注的未签名 Bone 产物，并在 Release 说明中提供 `xattr` 解除隔离命令。GitHub Actions 不得在 step 自身的 `if` 中读取仅由该 step `env` 注入的证书变量，须先以独立步骤输出签名能力状态。
- 项目文件迁移复用 verified copier，并以活动数据根内的可恢复 journal 驱动 `会话路径 -> 工作区配置 -> projectRootPath` 三步幂等提交；复制、校验和提交失败均不删除源目录。
- 项目迁移准入同时覆盖 Agent in-flight generation、自动标题等 generation-owned 写入、Automation、active/linked worktree 与受守卫 IPC；旧 Agent 代际通过唯一 query token 精确关闭，不会误停同会话的新运行。
- 项目目录选择使用按窗口、用途和代次隔离的一次性 `selectionId`；重启遗留的 copying/verifying/failed journal 只能按已持久化 operation 继续或显式放弃，renderer 不可直接指定任意目标路径。
- 设计素材与 Agent 会话双向传递以项目归属和当前会话持久化消息为唯一授权事实：设计素材只填入目标会话 composer、不自动发送；Agent 图片只接受该 session 的 `tool_result.imageAttachments.localPath` 精确匹配，并继续执行 realpath、允许根和图片签名校验，禁止扫描目录。新版 SDK JSONL 在主进程落盘时从 Nano Banana 本地标记结构化附件字段，旧 `AgentMessage.events` 保持兼容。
- Design 到 Agent composer 的跨视图文件引用使用按 `sessionId` 隔离的 Renderer 内存 Jotai 一次性队列：生产者先入队再导航，AgentView 仅在 editor 明确插入成功后确认清理并提示，失败时保留队列；富文本模式插入 mention 节点，默认纯文本或 mention 扩展缺失时复用可发送的 `@file:` 协议。归档或删除时清理队列，禁止用可能早于监听器挂载的 window 事件传递。禁用的会话菜单按钮保持原生 disabled，并由可聚焦包装元素提供原因提示。
- Design 权威 `LOAD` 从 tmp/backup 提升恢复后必须广播 `cause: recovery`，让其它已打开窗口同步接管新 revision；启动清理只删除无当前 runtime promotion journal 引用的 `import-*`/`relink-*` staging，正式 assets 永不参与兜底清理。
- Design 的 `cause: recovery` 表示原 revision 序列已失去磁盘依据，Renderer 必须立即阻断旧基线写入并强制 LOAD，且无条件接管可能更低的恢复 revision；位置类 pending 可在恢复基线上重放但保持手动重试阻断，结构 pending 只能保留为显式远端冲突，历史、选区和 Inspector 状态全部清理。普通 canvas/asset/job 事件继续遵守 revision 单调规则。
- Design Renderer 在 400ms 自动保存边界压缩 viewport mutation，只保留最后一个 `set-viewport` 且保持其它 mutation 顺序；macOS 沙箱内执行 Electron 完整构建时，将 `CLANG_MODULE_CACHE_PATH` 与 `SWIFT_MODULE_CACHE_PATH` 指向 `/private/tmp` 可避免原生 helper 写入用户缓存失败。
- Design Renderer 的 recovery/dispose 会递增独立 SAVE 基线代次并主动把在途 batch 归还 pending；旧代次 SAVE 的 success/failure 回调必须完全无副作用，禁止覆盖恢复后的低 revision 或重复归还 mutation。
- Design Renderer 的权威 recovery LOAD 使用独立于普通/job LOAD 的请求代次；普通/job 刷新不得取消恢复。恢复期间收到的 job revision 只合并最高目标，恢复成功后单次对账；恢复失败时保留目标，待用户 retry 成功后再对账。
- Design 大画布节点已有持久化尺寸且禁止连线时，必须向 XYFlow 显式提供 `handles: []`，否则未测量 handle 会迫使所有节点首帧挂载。启用 `onlyRenderVisibleElements` 后，Fit View 必须传 `includeHiddenNodes: true`，让尚未挂载测量的离屏节点使用声明宽高参与边界计算，避免裁剪与测量形成循环依赖。浏览器 QA 基线使用 1000 节点在 1200px 验证可见 DOM 显著低于总量及拖动、平移、缩放，在 620px 验证 Inspector/Toolbar/禁用 Tooltip 与 light/dark 主题。
- Agent 图片导入必须把授权校验与实际消费绑定到同一稳定文件身份：Bridge 以 `O_NOFOLLOW` 打开 fd，并用打开后的 canonical 路径和 `dev/ino/size` 证明归属；Asset Service 只在图片处理队列内从该 fd 读取、复核读取前后身份并统一关闭。祖先目录或叶子在排队期间被替换时只能读取原授权 inode 或 fail closed，禁止再次按路径打开。
- Design `LOAD` 的媒体授权采用候选 lease 事务替换：先取得权威 snapshot 和新 token，成功后切换 sender/project 所有权再撤销旧 token；Store 或候选授权失败必须保留旧 URL，窗口销毁、显式 RELEASE 与注册 dispose 继续按当前所有权幂等清理。
- Design Job 启动恢复与退出中断按项目隔离，单项目路径、Store 或 journal 故障必须记录中文日志并继续其它项目；应用 `before-quit` 的 Design、Agent、浏览器、watcher 等清理步骤逐项隔离，任何单点失败不得跳过后续全局资源释放。
- Design 生图模型按项目保存在本机 `design-cache` 偏好中；新任务在主进程固化 profile 快照，并通过单次 Pi 运行扩展强制 Nano Banana 使用该模型。重试复制原快照，配置删除、停用、执行器或模型 ID 变化、凭据失效时明确阻断，普通 Chat/Agent 的全局 Nano Banana 模型不受影响。
- Design 生图模型的用户入口统一放在“模型配置”，但能力声明、执行协议与任务路由继续由 fork 自有 `image-generation-models.json` 和 executor 管理；GPT Image 2 profile 只引用现有渠道的 `channelId`/`modelId`，执行时通过 `channel-manager` 读取 Base URL 并解密 API Key，禁止扩展官方高频变化的 `ChannelModel` 或复制凭据。Nano Banana 暂留 legacy 工具凭据兼容。
- 生图目录已升级为 schema v2；GPT Image 2 通过稳定 Pi 图片工具 ID 分派到 OpenAI Images generations/edits，渠道 Base URL 与 API Key 仅存在于主进程单次运行路由，Renderer、IPC 和 Design journal 不暴露凭据。远程图片响应必须逐跳执行 HTTPS、DNS/IP、重定向、MIME 与大小校验后才能保存，禁止直接下载供应商返回 URL。
- Pi Runtime 的图片工具结果可能以 `user + tool_result` 消息返回；Design Job 收集输出时必须兼容该角色形态，同时继续校验工具 ID、错误状态、媒体类型和当前任务会话的附件归属，不能只扫描 `assistant/tool`。
- Design 终态任务删除仅允许 `failed/cancelled/interrupted`，采用 journal `deletionState: pending` 记录可恢复意图，再幂等清理任务节点、历史异常分组引用和 journal；运行中任务必须先取消，Renderer 普通结构编辑仍不得删除 job 节点。
- 所有 Design 生成和编辑必须经过内部 Pi Agent；Agent 可按 `自动/使用项目/不使用项目` 在授权范围内读取代码与创作上下文，最终提示词以真实图片工具入参为事实。普通 Agent 对话只在用户从对话发起时保留原始沟通，不承载 Design 执行过程。
- Agent 与 Design 通过稳定 `creativeTaskId` 关联同一创作任务，单次 `DesignJobRecord.id` 只表示一次执行尝试；重试沿用任务 ID，基于成功版本继续则创建新任务并使用 `parentAssetId` 建立版本关系。
- Design 内部执行会话必须从普通会话、搜索、归档、最近记录、托盘、状态岛、会话引用、项目记忆及 LAN/mobile 中排除；终态先把摘要、精确提示词、实际上下文引用、Thinking 和日志转存为 Design trace，再可恢复地回收会话。
- Design 任务详情按 `projectId + jobId` 缓存轻量历史，只有用户展开 Thinking 或执行日志时才读取 trace；删除按 `creativeTaskId` 聚合清理全部尝试和详情缓存，成功素材删除后同步回收来源任务。旧项目缺少 `traces` 子目录时，由 trace 写入边界幂等补齐后再原子提交。
- Design 素材删除必须在画布 revision 变化前依次检查节点引用、`parentAssetId` 子版本引用和创作上下文 Catalog 的视觉标准引用；任一引用存在时保留元数据、原图、缩略图与来源任务，Catalog 通过主进程显式依赖注入，不在素材服务内隐式创建。
- Electron 主进程输出为 CJS 且把 Pi SDK 标记为 external；`@earendil-works/pi-coding-agent` 仅提供 ESM import，因此主进程模块禁止顶层值导入该包。只需要类型时使用 `import type`，真实运行能力沿用动态 `import()`，纯 identity helper 应留在本地并用 CJS bundle 回归测试锁定不产生同步 `require()`。
- Common Bridge 与飞书 Bridge 的 Agent terminal 回调必须以启动时捕获的 `sessionId + workspaceId` 复核当前绑定、用户可见性和项目一致性；身份漂移只允许清理/关闭，禁止向外部聊天发送内部错误正文。飞书失效清理由统一幂等边界先摘除 binding/反向索引和全部运行态，再异步关闭 CardStream，关闭失败不得恢复引用。
- Bun 非 isolate 组合测试中的 `mock.module` 会跨文件覆盖；共享核心模块的测试替身必须提供组合消费者所需的完整导出合同，并复用同一可重置状态，禁止各测试文件注册互不兼容的局部 mock。
- Canvas Agent 对话只允许通过专用 GET/SEND/STOP IPC 访问：每次调用先对账 pending intent，再从权威 Canvas 文档解析 `node -> session` 并复核会话三字段独占归属；运行复用 Pi Agent runtime，但单次强制 `Read`、`Glob`、`Grep` 只读工具白名单。持久化 JSONL 仅在打开对话时读取，关闭或切换 Canvas 不停止运行；全局事件按最小 owner 路由，完整归属只进入 Canvas live/error/完成导航，半归属、混合归属和损坏归属 fail closed，禁止进入普通会话、未读、状态岛或 Agent tab。
- Renderer reload 只通过一次性 active Canvas run 快照恢复 busy session 的最小 owner；renderer/headless 的正常与异常 completion producer 均强制从权威 session 索引附带轻量 metadata。未知 completion 与 stream/title 共用有界 bootstrap gate：stream/title 使用 O(1) 环形缓冲，completion 按 session 独立有界保留并在重放时后于流事件，bootstrap 失败后全部 fail closed。owner/messages 统一按 open/running/completed/invalid 生命周期回收：open/running 保留完整权威消息，只有终态非保护缓存按单会话 500 条、20 个 session、总计 2000 条修剪；soft completion 保持 active invalid 阻断，只有 hard terminal 才转为 100 条 LRU tombstone，token 热路径只做 O(1) 分类查询。
- Canvas Agent 的 authority 在每次 bootstrap 时严格按当前 active-run snapshot 重建；Renderer optimistic token/generation 与 hard completion 后的 JSONL GET handoff generation 独立保存，空 snapshot 不撤销后两者，GET 晚回只凭 handoff 代次接管并由新 run、close、settled 或 invalid 精确清理。
- Canvas Agent 的 `run_started` 必须携带主进程权威轻量 session metadata；Renderer reload 先注册全部 lifecycle listener，再由可 dispose 的 coordinator 应用一次性 active-run snapshot 并重放期间非普通事件，StrictMode 旧 snapshot 无副作用，禁止用定时重拉或 owner cache fallback 修补竞态。bootstrap 期间的 `run_started` 与 completion 分别按 session 独立有界保留，并固定按启动、普通流、终态顺序重放，避免 token 洪峰淘汰 owner/generation 建立事实。
- Canvas 新节点从当前可视中心开始，按固定节点尺寸、间距和确定性方形环寻找首个不重叠位置；Agent 对话只允许明确节点点击打开，关闭时同步清空选区和对话身份但不停止 Agent，XYFlow 的迟到 selection change 只能恢复选区、不得隐式重开面板。
- Canvas 节点操作采用顶部悬浮工具栏管理全局添加与删除，节点右侧 `+` 创建并原子连线下游节点；删除只移除节点和关联边并保留底层对话。Canvas LOAD 对已提交 Agent 引用的会话缺失或归属异常返回运行时 `nodeIssues`，局部显示“会话不可用”并允许显式重建为空白 session；图文档、授权和未完成事务损坏仍整图 fail closed，UI 禁止暴露 IPC 前缀、内部 UUID、路径或堆栈。
- 原生 Canvas 根容器必须复用 `design-canvas` 主题作用域，确保 XYFlow 控件在深浅主题下使用统一变量；右侧 Agent 对话面板收窄画布时，顶部工具栏等绝对定位元素的 containing block 必须绑定收窄后的 Canvas surface，不能继续按完整工作区定位。
- Canvas 新节点落点必须按右侧面板收窄后的真实 Canvas surface 计算，不能按完整工作区宽度放置；面板打开后仅在目标节点被裁剪时执行一次保持 zoom 的 viewport 平移，完整可见时不得制造额外保存或反馈循环。
- Canvas 多类型节点目标改为顶部添加始终选择 Agent/生图/文档/原型，视频只显示“即将支持”；独立节点按全画布最右侧追加且不得修改 viewport/zoom，节点默认折叠并在卡片锚点内按需展开单一工作台。有连线才共享直接上游已提交上下文，无连线完全独立；上游变化只标记待更新，不自动产生模型或付费调用。
- 原生 Canvas 单选状态只能由真实 `onNodesChange` 用户交互与显式节点/空白点击同步，禁止再用 React Flow 派生 `onSelectionChange` 反写权威 `selectedNodeId`；后者会在受控 selected 与回调 identity 更新之间形成双向反馈，使多个节点的选中边框持续交替。
- Canvas Agent committed intent 的永久归属只包含节点身份、session 绑定、稳定展示身份和关系合同；`position` 是用户后续可编辑的画布状态，对账与坏会话重建必须保留当前位置，禁止再以初始落点判定归属损坏。
- Canvas 节点工作台尺寸只属于 Renderer 当前挂载期临时状态，不写入画布文档或 IPC；首次打开和窗口边界变化时必须收进 React Flow 可视区，右下角双向缩放按画布缩放比例换算，并保持 `nodrag nopan nowheel` 隔离。Agent 工作台的消息宿主必须是受高度约束的纵向 Flex 容器，确保消息区单独滚动且底部输入框不被内容挤出裁切。
- 官方基线已同步到 `v0.19.1`，Electron fork 版本重置为 `0.19.1-bone.1`；后续继续按官方三段版本加 Bone 构建号发布。
- 上游 Vault、Live Markdown、Terminal、OfficeCLI、双 Pane 与 MCP 安全能力通过现行 Renderer/IPC 边界接入；Canvas、DataRoot、LAN/mobile、稳定文件授权与迁移守卫继续保留。OfficeCLI 只能处理已授权 Buffer 生成的临时副本，禁止按原路径二次打开文件。
- 独立规划窗口继续保留；Todo Agent 从该窗口启动后，通过 `TODO_AGENT_SESSION_READY` 由主窗口接管会话，避免独立窗口承担 Agent 工作区。
- 普通 Renderer 的活跃会话快照、Agent Island 已读、Terminal 创建与 Vault 会话上下文必须排除 Design/Canvas 等内部会话，防止内部运行态混入普通 Agent 界面。

- 2026-09-07：画布连续生产中，时间或媒体次数耗尽属于可扩额恢复的等待，用户取消有独立语义；跨轮次续跑保留原工作流执行身份，当前会话权限仍需重新核验。提交回执未知只允许对账原操作，确认失败后才可创建新尝试；预算扩展和定向重试需要持久化幂等记录。为什么这样处理：长片、多轮设计等任务不能因期限或重启丢失已付费任务，也不能靠换一次调用刷新预算。对用户的影响是可持续推进已有流程，重复恢复不会重复消费，异常会保留可解释的任务事实。
- 2026-09-07：普通 Agent 负责代码、剪辑及采集分析，画布统一保存产物、关系、版本和采用事实。现有授权项目内的精确媒体资产可按需交给 Shell/专业 Skill，处理后的明确文件再导入为候选；首次正式采用必须验证所选文件存在且内容一致。ComfyUI 的已安装节点接口用于兼容性检查，资源输入与输出仍需明确合同，接口描述不能证明第三方节点实现安全。为什么这样处理：高自由度依赖完整的输入、执行和产物回流链，不能只增加调用入口。对用户的影响是电影、App、运营和分析可共享产物管理，但内置剪辑器、数据集引擎及真实远端验收仍需独立能力；性能上采用有界文件读取、串行素材校验和事件恢复，不增加全库扫描。

## 会话记录

- 2026-09-08：按用户要求将旧画布归档修复纳入 `0.19.31-bone.12`，本地提交 `b9ca9fcdab1a3164715caca10b74235eadd03b0f`。从 `.11` 隔离构建，仅纳入三条事务链修复、回归测试及版本说明，避免把并行开发内容带入安装包；359 pass、6 个 Windows 专属 skip、全仓类型检查、完整打包及 DMG 校验通过。实际 app.asar 服务与包内原生 helper 在旧数据副本上验证原始字段顺序、无尾换行、新事务归档和真实并发修改拒绝，图提交使用内存夹具，未启动新包完整 UI。安装包位于 `apps/electron/out/Proma-0.19.31-bone.12-macos-arm64.dmg`，证据见 `.omx/reports/2026-09-08-canvas-archive-release.md`。对用户的影响是后续安装此包可预防同类加载阻塞，现有画布与付费生成任务不变；没有新增 I/O 或轮询，仅在既有有界扫描内保留原文。此记录接续此前只恢复现场数据的处理；本轮未替换已安装 `.11`，未推送或发布 GitHub，macOS 包未签名及公证。

- 2026-09-08：用户反馈“续命 · App Store 商店视觉”旧画布打不开；实际安装的 `0.19.31-bone.11` 的 `app.asar` 仍把批事务 parser 结果重新序列化后归档，主工作区已有的原文 CAS 修复未包含于该安装包。现场只有一条已提交创建事务，active 与 archive 语义相同但资源字段顺序不同；用原操作节点重演完整图哈希后，备份全部 10 份 JSON，再通过生产原生归档器保存原文、精确读回和 CAS 移出 active。原画布在同一正式客户端重试后正常显示，图片详情可读取原提示词；revision 1、一个卡片、配置和两份失败创建的回收内容逐字节未变，没有生图。为什么这样处理：安装版本号不能证明修复代码已进入产物，历史读取故障也不能靠重建画布或忽略事务来处理。证据及备份见 `.omx/reports/2026-09-08-app-store-canvas-recovery.md`；52 项相关回归和全仓类型检查通过。本轮仅恢复当前数据，未替换安装包，正式版后续新建仍需发布已有预防修复；恢复仅处理一条事务，无新增客户端运行开销。

- 2026-09-08：按用户要求发布 [v0.19.31-bone.11](https://github.com/kuangtao22/Proma/releases/tag/v0.19.31-bone.11)，正式标签与主线提交均为 `973e780338f85f295d61e02cbbf731eb4797b899`。隔离检出验证 282 项相关测试及全仓类型检查通过，已清理该临时 worktree；[Windows 独立预检 34200080426](https://github.com/kuangtao22/Proma/actions/runs/34200080426) 实际 Checkout SHA 一致，53 pass、14 skip、0 fail，跳过项为既有 macOS 专属及非 Windows 编译器测试，Windows 原生行为与安装包上传通过。[正式 Release 34201237079](https://github.com/kuangtao22/Proma/actions/runs/34201237079) 六个任务全部成功，四个平台 15 个资产齐全且版本为最新正式 Release，说明与仓库文件字节一致，三份更新 YAML 的版本、引用、大小、SHA-512 格式和 GitHub 元数据 SHA-256 校验通过；CDN 首次读取短暂 TLS 超时后重试成功。验收脚本与结果在 `/private/tmp/proma-bone11-verify-release.ts`、`/private/tmp/proma-bone11-release-verification.json`。本轮未下载大型安装包重算 SHA-512，未做四平台安装启动或真实媒体生成；macOS 包未签名，安装步骤见发布说明。发布仅纳入固定提交，保留主工作区其他未提交改动和现有客户端。

- 2026-09-08：用户要求画布 ComfyUI 服务器与可用媒体模型放在同一弹层统一管理，取代独立服务器下拉占用工具栏的布局。统一入口命名为“画布媒体配置”，上方选择服务器、下方保留模型搜索和多选；两类选择仍独立保存，既有任务继承规则不变，也不增加请求或轮询。嵌套 Select 时父 Popover 的缩放会触发重复尺寸通知，此处保留淡入和平移并固定缩放比例，避免测量反馈。142 项相关测试、全仓类型检查及隔离 Electron 的双主题宽窄窗口真实交互通过，覆盖选择互不覆盖、失效绑定、取消和关闭重开；仅 Renderer 变更，复用当前开发服务热更新。

- 2026-09-08：用户确认优化画布 ComfyUI 绑定与远端工作流接入，已实现画布顶部选择服务器、空白新草稿继承、Agent 自动发现并分析候选以及导入当前项目不可变草稿，无需用户手写本地 JSON。画布绑定属于用户偏好，Agent 只可信读取；失效不自动改选，用户已编辑的节点和已准备任务保持原连接。匹配依据真实输入输出与语义角色，不能只按名称；API 图沿用既有执行校验，UI 图只转换已有明确静态 widget 合同，不确定自定义控件须提示具体原因。为什么这样处理：本地副本用于复现和恢复，不能成为手工录入前提，也不能让默认切换改派既有任务；复用目录快照、限制每页 6 个候选、大图分页精读和紧凑导入回执控制主进程与上下文开销，无新增依赖或后台扫描。相关回归、真实服务类的内存远端集成、全仓类型检查、Electron 构建和双主题宽窄窗口交互通过；开发客户端已重启，本轮没有访问真实远端或提交生成。验收及支持边界见 `.omx/reports/2026-09-08-canvas-comfyui-discovery.md`。

- 2026-09-08：用户询问是否必须手工创建本地工作流，并指出画布缺少 ComfyUI 服务器绑定，希望未指定工作流时查询服务端目录并分析匹配。建议画布选择运行服务器、新任务继承，具体工作流按素材角色和输出需求选择；本地不可变副本用于复现与恢复，不应成为用户手工录入的前置负担。匹配必须检查真实节点、输入输出及服务器资源，不能只按文件名判断；UI 格式转换与候选匹配仍需补齐。本轮只核查现有链路并提出方案，未修改运行逻辑、绑定真实服务器或启动生成。

- 2026-09-08：用户反馈工作流行内展开后难以收起，明确改为点击条目直接弹窗，以只读代码编辑器展示完整 JSON；此要求替代上一轮行内展开。关闭按钮、Esc 和遮罩均可关闭，关闭后恢复原条目焦点与列表位置，保留完整复制及 API 格式独立导入。复用现有编辑器与高亮能力，无新增依赖，只高亮可视区域并在关闭时释放编辑器，避免大工作流拖慢列表。24 项相关测试、全仓类型检查及隔离 Electron 真实组件交互通过，覆盖 1000 节点完整正文、只读、复制、关闭重开、迟到响应、失败重试、导入和深浅主题宽窄窗口截图；本轮补齐弹窗交互验收，未访问真实远端服务。

- 2026-09-08：工作流资源详情应能查看完整正文，不能把“查看”和“导入为可执行模板”绑定。详情现按需读取并预览完整 JSON，保留节点、连线及扩展字段，支持复制；UI/未知格式只读可见，API 格式才提供独立导入入口且继续严格校验。这样无需导入即可检查远端文件，不自动执行或保存工作流，复用已有缓存并忽略折叠/切换后的迟到响应。32 项相关测试和全仓类型检查通过；实机已确认更新后的设置页，详情展开验证因工具自动审批连续超时未完成。

- 2026-09-08：用户将资源快照的模型浏览改为左右布局：左侧按存放目录筛选，右侧显示对应模型，取代原来的目录下拉框；上方仍保留同一行的页签、搜索和同步按钮。两栏独立滚动，搜索和加载期间保留目录导航，连接地址或认证变化后清空旧目录，便于连续浏览且不增加远端请求或后台开销。18 项相关测试、全仓类型检查及真实客户端左右分栏视觉检查通过；补充的目录切换和搜索点击验证因工具自动审批连续超时未完成。

- 2026-09-08：资源快照也沿用媒体列表的紧凑工具栏：默认 Tabs 靠左，256px 搜索与同步/刷新按钮靠右同一行，模型目录筛选独立保留在下方；窄容器自动换行。只调整资源浏览器布局，查询、防抖、同步与快照语义不变；18 项相关测试和真实客户端布局检查通过。

- 2026-09-08：用户要求媒体配置点击编辑后采用“模型配置”的独立表单页：顶部为返回箭头和编辑标题，隐藏列表添加按钮、主导航页签和列表搜索；媒体模型、服务连接、公共工作流及对应新增页统一使用此结构。返回沿用取消草稿语义，保存期间禁用，媒体配置继续显式保存；连接资源快照仍保留在表单下方。只调整 Renderer 页面结构与草稿导航，无新增网络或后台开销；23 项相关测试、全仓类型检查和真实客户端服务连接编辑页视觉检查通过，未操作用户正在编辑的字段。

- 2026-09-08：用户进一步要求媒体配置的页签与搜索位于同一行：页签在左，搜索缩短为 256px，媒体模型类型筛选紧随搜索；三个列表保持一致，窄容器允许换行，错误提示单独占下一行，编辑草稿期间仍隐藏搜索。此约定取代此前页签与搜索分行的布局；仅改变界面排布，不增加请求或后台任务。

- 2026-09-08：用户指出媒体配置页签比例不协调，要求优先检查已有封装。该页继续复用 `ui/tabs.tsx` 的 Radix/shadcn Tabs，移除页面强制整行三等分、缩小字号和内边距的覆盖，采用左对齐、内容自适应的默认紧凑样式；`SettingsSegmentedControl` 用于少量设置值选择，不替代页面导航。只调整当前页，不修改共享 Tabs 默认样式或增加依赖；23 项相关测试、全仓类型检查及真实客户端布局检查通过。

- 2026-09-08：用户要求媒体配置沿用“模型配置”的页面层级：三个页签各自的标题和添加主按钮置于最上方，页签居中间层，搜索与类型筛选放在列表外，添加按钮使用相同主题主色。这样新增入口无需滚到列表底部，三个页面操作位置一致；只调整 Renderer 布局并保留切页键盘焦点，不增加 IPC、网络或后台开销。23 项相关测试及真实客户端的新增/取消、搜索空结果、连续方向键切页验证通过。

- 2026-09-08：媒体设置的 `mediaGetSettings/listMediaApiModelProfiles is not a function` 来自合并后 Renderer 已热更新、运行中的 Electron main/preload 仍为旧构建。合并涉及 IPC 或 runtime 的功能后，启动前必须同步重建 main、preload 与相关 utility/runtime，再退出旧进程并启动；仅重建 main 或刷新 Renderer 无法更新已加载的 preload。已完成五项构建并沿用 `PROMA_DEV_INSTANCE=main` 与 Renderer 5177 映射重启，真实窗口验证媒体模型读取现有 GPT Image 2，服务连接和公共工作流正常显示空列表，两个接口报错消失。日志位于 `/private/tmp/proma-main-electron-media-fix.log`；未提交生成任务或验证外部 ComfyUI 服务。本次不新增运行逻辑或后台资源开销。工作流空草稿默认显示 JSON 失效警告，退出前应核实名称与 JSON 的实际内容，不能仅据警告推定存在未保存数据。

- 2026-09-07：按用户要求重启并置前包含运维文件页 SFTP 生命周期修复的主分支开发客户端，继续使用 `PROMA_DEV_INSTANCE=main` 与 Renderer 5177 映射；本次启动日志为 `/private/tmp/proma-main-electron-files-fix.log`。新进程存活且 IPC 初始化完成，启动时仍记录历史 Canvas 候选任务恢复告警及 LAN Bridge 29889 端口占用；客户端启动成功不等于真实服务器文件页已复测。

- 2026-09-07：ComfyUI 合并与主线首图默认选择兼容时，后台对账不得把首次自动选中视为明确验收；用户继续只接纳该次继续之前已等待的图片，新生成的后继仍保留评审边界。为什么这样处理：默认展示和继续生产的授权不同，重启与定时恢复不能扩大后续生成范围；对用户的影响是首次图片可立即显示，但流程不会因此自行继续计费。继续范围按本次有限任务集合判断，不新增后台扫描。

- 2026-09-07：用户要求将 ComfyUI 媒体分支合入本地 main；使用独立集成 worktree 处理与主线画布、运维模块的冲突，保留 owner 绑定、精确恢复凭证、分页与布局性能约束，再接入媒体快照和进度。主工作区已有修改单独保存并在合并后恢复，不混入媒体提交；备份引用为 `refs/codex/backups/main-before-comfyui-20260907`。为什么这样处理：旧功能分支不能覆盖主线已发布修复或并行开发成果；对用户的影响是主线可同时使用媒体和原有能力，当前开发 worktree 保留，本次不推送、不发布、不执行远端生成。
- 2026-09-07：多个开发 worktree 并行时，固定 Renderer 端口 5174 可能属于旧分支；启动最新 main 可给 Vite 独立端口 5177，并仅对新 Electron 进程传入 `--host-rules=MAP 127.0.0.1:5174 127.0.0.1:5177` 和 `PROMA_DEV_INSTANCE=main`。本次已用进程 cwd、Renderer app-path 与 NetworkService 到 5177 的实际 TCP 连接确认加载 main，不能只看窗口中的 5174 URL。日志位于 `/private/tmp/proma-main-vite.log` 与 `/private/tmp/proma-main-electron.log`。为什么这样处理：保留旧实例会话并避免主进程和界面混用分支；不改共享配置和生产源码，新增资源开销仅来自独立开发实例。多实例共享业务配置时，LAN Bridge 仍可能因正式版占用端口而启动失败。

- 2026-09-07：将 `codex/server-ops-completion` 的两个提交合入本地 `main`，包含信任恢复、文件/SFTP 传输、Docker 与异步退出清理；保留主分支 Canvas 修复、权限等待生命周期、`bone.10` 版本和 Windows Sharp 打包规则。数据库能力仍未接入，源开发 worktree 继续保留供当前实例使用。为什么这样处理：用保留双方历史的合并维持功能边界，避免用旧分支整文件覆盖已发布修复；对用户的影响是主分支同时拥有画布修复和运维已实现能力。1183 项合并回归、全仓类型检查、macOS arm64 Electron 完整构建及配置锁资源检查通过；LAN 兼容检查两项与 Design 数据根路径检查失败已在合并前 `9c18e40b` 快照复现，属于既有基线问题。本次不推送或发布，不代表 Windows 安装包、真实服务器或数据库验收；不额外增加客户端后台任务。

- 2026-09-07：Canvas 缩放逐帧数据应由当前工作台独立订阅，Graph 根不能同时订阅工作台几何并更新受控 viewport，否则会重复刷新整图；旧开发 worktree 还需核对是否包含节点复用、稳定回调和边缓存。已将缺失性能修复精确回补到 `server-ops-completion`，并在 main 与该 worktree 隔离工作台订阅。为什么这样处理：消除每帧连线分配和重复 React 更新，同时保留手势结束提交、可见区裁剪与恢复优先级。对用户的影响是当前开发实例热更新后缩放开销降低，节点内容和付费任务不变；没有新增后台进程或轮询。固定 120 帧的 26 节点/38 边隔离测试中，Graph render 502→238、边替换 502→1、P95 216ms→9.1ms；这些数据不代表用户全应用负载下的帧率保证。完整证据、测试范围与复现夹具见 `.omx/reports/2026-09-07-canvas-zoom-performance.md`；正式安装包尚未发布。

- 2026-09-07：排查 `Main runtime request timed out: agent.capability.canUseTool` 时确认用户运行的是 `.worktrees/server-ops-completion` 的旧开发实例，主分支已有权限等待修复并不代表其它 worktree 或其 `dist` 已更新；本次仅回补已有请求生命周期修复并重建该目录的 Agent runtime。为什么这样处理：审批可能合法超过两分钟，必须由用户响应、运行取消或退出收口，普通 RPC 仍保留故障时限；诊断时同时核对进程 cwd、源码、实际 bundle 和运行进程生命周期。对用户的影响是下一轮 Agent 会启动新 utility 并加载修复，无需重启整个客户端，失败的付费图片任务不自动重放；既有运行仍使用启动时代码。66 项相关测试、全仓类型检查及 runtime 构建通过，未执行真实付费生图；修复不新增轮询或后台进程。

- 2026-09-07：`bone.10` 的 Canvas 图片采用会因新增 `upstreamChange` 的对象字段顺序与 Store parser 不同，发生图已提交但 raw JSON 哈希不匹配；此后 revision 前进使残留 intent 持续阻断 LOAD。图提交证明必须沿真实 Store parser 规范化，测试必须跨过实际规范化边界，不能只克隆内存对象；旧布局仅在完整图哈希和精确 revision 可证明时兼容。当前受影响画布通过连续 30 条历史 batch 和紧邻后续提交的哈希复演证明后，仅补齐两份采用终态，保留 26 节点、38 条边与图片；原始备份和完整证据见 `.omx/reports/2026-09-07-canvas-adoption-recovery.md`。为什么这样处理：恢复不能用“当前素材相同”替代完整提交证据，也不能删除残留事务掩盖问题；对用户的影响是当前采用恢复阻塞解除，后续新采用不再因字段顺序留下同类事务。新增开销仅为采用/恢复时线性规范化，不增加渲染或后台轮询；160 项定向测试、全仓类型检查和独立复核通过，GUI 加载截图未验证，预防性源码修复尚未打包发布。

- 2026-09-07：`v0.19.31-bone.10` 已通过正式 Release 工作流 `34076461380` 发布，标签固定在 `ad19ee6c5740c415ab2c35d0e4a449a82132cb61`；版本校验、macOS arm64/x64、Windows x64、Linux x64 与 Release 汇总全部成功。GitHub 已将其标为最新正式版本，15 个资产均上传完成；下载的三个更新 YAML 已核对版本、文件存在性、大小、SHA-512 格式及 GitHub 提供的元数据 SHA-256，Release 正文与仓库说明一致。macOS 仍为未签名发行，生产完整业务闭环与真实渠道验证缺口保留在版本说明中；未安装或覆盖本机应用。为什么这样处理：发布成功必须同时有跨平台产物、更新索引与公开 Release 的实际证据，不能以推送标签代替完成；对用户的影响是 GitHub 下载和应用内更新统一获得包含本轮 Canvas、Windows 启动与原生构建修复的 `.10`。
- 2026-09-07：`v0.19.31-bone.9` 的业务源码在独立 Windows 工作流 `34075117447` 已完成 53 pass、14 skip、0 fail，并生成安装包；正式 Release `34075860809` 的 Windows runner 因负载较高，MSVC 仍编译时被 beforeAll 的 30 秒上限终止（日志 30026.82ms、hook timed out），不是同一编译错误复发。编译子进程改为 120 秒独立期限，hook 150 秒仅用于清理余量，行为测试超时保持不变；修复发布版本为 `.10`，保留 `.8`、`.9` 失败标签。这样让构建准备的资源波动与业务超时合同各自有界，不增加客户端运行时开销。
- 2026-09-07：`v0.19.31-bone.8` 在 Windows Release job `101597765171` 的原生编译阶段失败：Canvas intent 精确读取调用了未定义的 `WindowsVolumeId`，应直接读取既有 `BY_HANDLE_FILE_INFORMATION.dwVolumeSerialNumber`。MSVC 的 stdout 被测试 `execFileSync` 的 pipe 截留，测试改为继承输出以保留真实诊断。为什么这样处理：跨平台条件编译分支必须在对应 runner 上验证，macOS 通过不能证明 Windows 编译成功；保留 `.8` 失败标签，修复版使用 `.9`，先用独立 Windows 构建验证再发布。对用户的影响是安装包可包含完整 Canvas 修复而不改写旧版本，运行时没有新增开销。
- 2026-09-07：用户授权 GitHub 版本发布，本次发布边界设为 `0.19.31-bone.8`，包含已提交的 Canvas 操作补齐与性能修复，以及 Windows 数据根启动门和 Sharp 目标平台依赖修复。此前 `.6`、`.7` 已用于本地修复包，使用新构建号避免同版本不同内容；正式说明统一使用 `release-notes/bone/v0.19.31-bone.8.md`，保留生产完整闭环与真实渠道验证缺口，ComfyUI/音视频规划不纳入本次发布。版本、标签、安装包与更新元数据必须一致，跨平台构建全部成功后才由现有 Release job 汇总发布，不增加客户端运行时开销。
- 2026-09-06：用户要求为 Canvas Agent 剩余缺口列出补齐计划，方案保存在 `docs/superpowers/plans/2026-09-06-canvas-agent-completion.md`。规划先统一入口与容量治理，再补任务控制、版本采用、原工作流续跑、导出恢复及完整 Electron 验收；本轮仅规划。调研发现 `.worktrees/comfyui-media` 已有持久 WorkflowRun 合同、Store 和 resume/cancel 服务，后续应整合并验收，不能另建第二套；该分支运行历史列表的 512 条上限与外层共享事务文件的 512 项上限须分别处理。为什么这样处理：避免重复维护运行状态，并防止新增操作放大长期容量与重复计费问题。对用户的影响是按业务闭环逐阶段交付，清楚区分源码完成、真实渠道验收和安装包生效；性能上坚持有界查询、终态分片归档与事件驱动，不增加全量历史轮询。
- 2026-09-06：用户补充采用恢复报错与多图/多节点卡顿。图片采用恢复必须接入 Canvas LOAD 的既有串行写边界，通过显式 locked 接口避免自锁；锁外发布已提交事实，后续事务失败也不能吞掉恢复事件。性能上，任务事件采用有界合批和单在途补读，已有图片工作台在后台刷新失败时保留内容；Graph 用稳定回调及三方字段合并保留未变节点和局部拖动事实，边缓存以数组身份而非 revision 失效。为什么这样处理：重开画布必须真正恢复未完成采用，重复事件也不应扩大为重复扫盘与整图重渲染。对用户的影响是采用恢复可继续、批量生成期间交互更稳定；100 条突发事件从 101 次列表读取降到 2 次，1000 节点单状态变化仅替换 1 个节点对象。完整证据与共享事务目录 512 项上限的剩余归档风险见 `.omx/reports/2026-09-06-canvas-recovery-performance.md`；未修改用户业务事务或运行付费生成。
- 2026-09-06：用户要求补齐截图中的候选图片验收与批量生图权限超时，并审查 Canvas Agent 操作缺口。本轮将 canUseTool 的用户等待与普通 RPC 故障时限分离，等待由响应、运行取消和 utility 退出收口，迟到取消绑定原请求代次；批量图片启动使用十五分钟窗口并传递取消信号。候选图片通过同节点成功任务与素材来源校验后只读预览，正式采用仍独立；卡片区分未生成与已生成未采用，并在候选删除后刷新，能力声明按真实工具清单和 plan 上限过滤。配置读取补齐职责、Skills、模型选择和配置版本，让普通 Agent 可按真实版本局部更新；配置与正式输出共享有界响应预算。为什么这样处理：审批等待不应自行取消，视觉验收不能依赖先采用，Agent 也不能被告知不存在的操作能力或被要求猜测版本号。对用户的影响是可在原对话看候选、完成批量启动审批并更新专业 Agent 配置，无需手动搬运截图；性能上仅按需读取有界预览与配置，复用任务和素材事件，不增加后台轮询或重复生图。其余任务控制、采用、续跑、导出与恢复缺口见 `.omx/reports/2026-09-06-canvas-agent-capability-audit.md`，未执行真实付费任务。
- 2026-09-06：用户提供的 S1-B 截图暴露了图片验收流程缺口：对话报告生成成功、候选未采用，但 Agent 无法读取候选画面，折叠卡片还显示“尚未生成图片”。本轮仅查看与代码核对；建议让普通 Agent 和 Canvas Agent 共用按明确版本读取候选图片的只读能力，把视觉验收与正式采用分离，并区分“未生成”和“已生成未采用”的展示，尚未实施。为什么这样处理：先采用才能看图会倒置验收顺序，生成成功也不能证明视觉合格；对用户的影响是后续应能在原对话检查候选，无需手工截图搬运。性能约束是按需、有界读取预览，不自动采用、不触发下游或重新生成。

- 2026-09-07：macOS 直接运行 Electron 开发实例时，系统自动化仍可能以 `Electron` / `com.github.Electron` 识别它，主窗口标题也可能继续显示 `Proma`；验收版本必须同时核对进程 cwd、`PROMA_DEV_INSTANCE` 和 Renderer 端口所属目录，不能只凭 Dock 名称。为什么这样处理：共享业务数据会恢复同一会话外观，容易把旧实例或正式版误认为当前分支；对用户的影响是能准确确认新增运维模块来自待验收代码，不增加应用运行时开销。

- 2026-09-07：Server Ops 退出与取消必须等待真实异步清理完成；从 owner/lease 索引摘除仅表示禁止再使用，不代表 fd、临时文件或 SSH channel 已释放。重复取消和重复退出复用清理 Promise，utility 的 SFTP owner 关闭需要明确确认；无法确认远端清理时保留待检查事实，不自动重放。为什么这样处理：避免第二次取消误删队列末项，或进程提前退出留下临时文件；对用户的影响是取消与退出结果可信。等待只发生在相关操作结束时，不增加后台轮询。

- 2026-09-07：运维补全分支已从规划进入开发，信任恢复、文件与 Docker 共用同一主进程授权/审计和独立 SSH runtime；文件选择只签发窗口绑定的 fd lease，切主机或关闭面板先确认并等待活动传输收口，写入结果未知不重放。数据库驱动尚待批准，数据服务页面必须明确标为未接入。为什么这样处理：用户验收要能区分已实现工作流与占位，并避免旧窗口请求继续操作新目标；性能沿用按需读取、事件进度、有界分块与并发。真实验证脚本位于 `apps/electron/scripts/server-ops-*-smoke.ts`，只使用临时数据根及 localhost fixture，不能视作用户服务器或跨平台安装包验收。

- 2026-09-07：干净运维 worktree 手动启动时，须在 bundle 生成 `dist` 后同步资源并确认 `dist/resources` 内的原生 helper 存在；本次资源先于 `dist` 复制导致路径缺失，重新同步并重启开发实例后恢复。验收实例使用 `PROMA_DEV_INSTANCE=server-ops-completion`，开发服务通过 detached 子进程持续运行，日志位于 `/private/tmp/proma-server-ops-vite.log` 与 `/private/tmp/proma-server-ops-electron-ready.log`。为什么这样处理：首次启动不能只以构建退出码判断资源就绪；对用户的影响是主窗口和运维面板可正常打开，正式版继续运行。两实例共享业务配置时，正式版已占用的 LAN Bridge 端口会让开发版 Bridge 绑定失败，不应为运维验收停掉正式版或修改共享端口。

- 2026-09-07：用户要求服务器运维后续工作在分支继续并先规划；已从 `feed23c4` 建立 `codex/server-ops-completion` 与 `.worktrees/server-ops-completion`。设计与实施计划位于 `docs/superpowers/specs/2026-09-07-server-ops-completion-design.md`、`docs/superpowers/plans/2026-09-07-server-ops-completion.md`，当前是待实施草案，顺序为指纹恢复、文件管理、Docker、数据服务。为什么这样处理：先恢复可信连接并逐领域完成审批、审计、取消与验收，避免新页签停在占位或把数据库权限混入通用 Shell；对用户的影响是每阶段可单独验收，不受主工作区并发 Canvas/打包改动干扰。性能约束是复用 SSH 运行时、按页签工作、传输和查询有界，不增加全主机后台采集；本轮没有业务运行时改动，也未访问真实服务器。
- 2026-09-07：媒体本地回流的验收需要分开记录真实二进制服务测试与客户端模型编排测试。新增 ffmpeg 合成音视频经生产 ffprobe、资产导入、文件回流和 Canvas 候选采用的组合验证；同长度篡改须阻断采用，恢复原文件可继续，截断容器不得登记。为什么这样处理：模拟文件头与模拟探测只能证明接口衔接，不能证明真实产物可读；对用户的影响是后期处理产物进入画布前有实际文件证据，同时不会把本地合成测试误报为 ComfyUI 或完整业务验收。测试按工具可用性运行，不新增应用依赖或运行时开销。

- 2026-09-06：用户提出将画布的图片、视频、音频与音乐纳入统一生成管理，接入方式必须覆盖 ComfyUI 与 CLI（当前以即梦为例），音频选型需比较 MiniMax、ComfyUI 与具体 TTS 方案。本轮为规划讨论，具体供应商优先级和数据模型尚未确认为实施决策；后续应从统一接入、任务及资产版本管理出发评估方案，并保留现有图片工作流的兼容性。
- 2026-09-06：用户将媒体生成首阶段明确收敛为 ComfyUI 接入，要求支持远程服务、预留仙宫云等第三方云算力，并把现有生图配置纳入统一媒体配置模块；工作流采用 Proma 自动构建还是复用 ComfyUI 已保存工作流仍在讨论。为什么这样处理：优先围绕一个可执行多类媒体的后端建立接入与管理闭环；对用户的影响是图片、视频和音频可共用配置入口，后续云算力与 CLI 接入继续扩展同一体系。
- 2026-09-06：用户要求同步规划面向 Agent 的 ComfyUI 自动化能力，覆盖工作流发现与配置、生成执行、任务跟踪和结果使用，具体工具合同与授权范围仍待方案确认；后续媒体接入需同时考虑画布手动操作和 Agent 调用，避免只交付设置界面而缺少自动化闭环。
- 2026-09-06：用户进一步要求 Agent 自动化必须覆盖工作流节点读取、素材匹配、上传、取得真实远端素材标识、填充节点和生成进度查询，避免步骤缺失导致任务无法继续；专项规划草案保存在 `.omx/plans/2026-09-06-comfyui-agent-execution-contract.md`，含阶段输入输出和待实施验收矩阵。外部接口核实：ComfyUI 上传回执 `name/subfolder/type` 必须按具体 Loader 编码，音视频自定义节点需要单独验证输入输出合同；客户端 `prompt_id` 不构成服务端幂等保证，提交响应丢失只能先对账，不能直接重投。官方源码版本及来源链接见草案，当前尚未实施或验收真实远端生成。
- 2026-09-06：用户明确要求 ComfyUI 等可报告进度的执行器将实时进度展示在画布节点，纳入媒体接入首版范围。专项草案已补折叠卡片、展开详情与 Agent 共用任务进度事实、节点内进度和全任务完成的区分、断线/重载恢复、旧运行事件隔离及节流验收；进度只作为运行态投影，不写入 Canvas 图 revision，避免高频进度造成自动保存和画布布局开销。
- 2026-09-06：用户要求再次审查媒体扩展对 Canvas 生态的实际增益，并确保普通 Agent 与 Canvas Agent 使用统一规则。专项草案补充的建议为：共用媒体服务和工具策略，普通交互 Agent 与手动 Canvas Agent 在各自授权范围内可完成生成，父编排子 Agent 准备后由父调度器执行；保留 image 并新增 video/audio 节点，产物按角色和版本参与引用、采用与依赖；持久化 workflow run，采用后只续跑未完成后继，防止重复生成和计费。配置所有权、独立项目媒体任务挂画布、媒体生命周期与相应验收同步纳入规划；这些是待实施的方案建议，不代表现有代码已支持。
- 2026-09-06：用户明确要求查看已连接 ComfyUI 的模型与节点，并让 Agent 利用已有资源自动编写工作流。专项草案补入按实例隔离的资源目录、API 图草稿新建/改图、静态校验、沿统一任务链试运行及项目模板保存。协议边界：object_info/模型列表/Loader 枚举只能分别证明节点注册、文件可见和字段可选，不能证明模型配套、已加载或运行成功；标准 /prompt 校验通过会入队，不能当无执行 dry-run。相关官方源码锚点见草案；此阶段仍为规划，实际目标实例和自动编写运行尚未验收。
- 2026-09-06：用户要求再次镜像评估整体画布业务。本轮按用户生产流程和现有模块双向复核，规划补充单节点/批量与 DAG 运行的明确语义、媒体/文档起点、动态后继范围，以及运行期间改配置后旧候选的来源与采用规则。现有图片批次采用事务和依赖传播应作为复用基础；统一能力描述与权限策略各司其职。实施顺序调整为尽早验证“最小公共合同 + 单个真实 ComfyUI 图片工作流 + 两类 Agent”，再扩展完整续跑、资源编写和音视频；仍是待实施建议，避免仅凭技术生成成功声称最终交付完成。
- 2026-08-18：首次建立项目记忆文件；创建时仓库 `main` 指向 `d1a131c7`，配置 `origin` 与 `upstream` 两个远端。
- 2026-08-18：官方 `upstream/main` 停留在旧版本，发布进度应以远端 `v*` 标签为准；本次确认最新官方标签为 `v0.17.42`。
- 2026-08-18：将 fork 从 `v0.12.23` 升级到官方 `v0.17.42`，保留 LAN Bridge、移动端、开发环境隔离与独立发布流程；桌面版本与官方保持为 `0.17.42`。
- 2026-08-18：补齐 Pi `sdk_delta` 到 LAN Bridge 的正文、思考与工具事件映射；修复端口热重启丢失 Agent EventBus 订阅的问题。
- 2026-08-18：独立发布流程集中合并 macOS 双架构更新清单，并上传 `latest*.yml` 与 blockmap；自动更新源指向 fork 的 `kuangtao22/Proma`。
- 2026-08-18：将 fork 的 Electron `appId` 改为 `com.bone.proma.app`，避免与官方安装包使用同一系统应用标识。
- 2026-08-18：明确版本同步规则：桌面版本始终与已合入的最新官方标签一致，当前恢复为 `0.17.42`，方便后续按官方版本更新。
- 2026-08-18：用户确认无需隔离开发版业务配置，统一使用 `~/.proma`；不迁移或复制旧 `~/.proma-dev` 数据。
- 2026-08-18：评估 `dsh-pocket` 后确定远程方案方向：可借鉴二维码发现与可选公网隧道，但不采用无应用层鉴权、改写 Host/Origin 绕过上游安全边界的全量反向代理；公网传输仍须保留 Proma 配对认证、设备撤销和限速。
- 2026-08-18：发现 LAN Bridge 心跳检查间隔为 30 秒、无活动超时为 20 秒且先检查后发心跳，空闲客户端可能约 30 秒被断开；后续应配套回归测试修复，并补齐二维码发现、工具审批、附件传输和断线事件重放。
- 2026-08-18：fork 自动更新源已接入 `kuangtao22/Proma`；更新器按 `latest*.yml` 内的应用版本比较，同一官方版本下仅递增 `bone.*` Release 标签不会触发客户端更新，需等待应用版本提升或另行设计独立的 fork 构建版本。
- 2026-08-19：完成 LAN/mobile 上游兼容优化，实现协议与 Adapter 隔离、设备配对和撤销、连接自愈与移动端恢复，并加入每周只读上游兼容验证。
- 2026-08-19：完成 LAN Bridge 安全收口：修复 Token 刷新延期、恢复失效、并发启动泄漏、协议绕过、未认证资源占用和会话路径穿越，并补齐上游前向兼容配对。
- 2026-08-19：将扫码与 PIN 配对升级为可信设备白名单：稳定设备 ID 和长期凭证支持永久自动续签直至桌面撤销；新增 `trusted-device-credentials` 协议能力及上游兼容检查，IP 仅用于最近访问审计。
- 2026-08-19：补齐 GitHub Release 的 Linux x64 构建与发布合同，全平台发包统一由 `v*` 标签触发。
- 2026-08-19：首次 Linux deb 云端构建暴露缺少项目主页元数据，已补充 fork 主页并加入回归测试。
- 2026-08-19：Linux FPM 会把 `@proma/electron` 默认产物名解释为目录路径，已固定为 `Proma-${version}-${arch}.${ext}` 并加入回归测试。
- 2026-08-19：确认整理发布版本与开发/正式身份；下一版本采用 `0.17.42-bone.5`，正式版保持 `Proma`，开发版固定显示为 `Proma Dev`。
- 2026-08-19：完成 `0.17.42-bone.5` 版本体系：应用版本、标签、Release 标题、关于页、更新频道和三平台产物命名统一；GitHub Actions 在四平台构建前校验标签合同，并只在全部构建成功后发布。
- 2026-08-19：Bun 1.3.14 的 `bun install --lockfile-only` 不会自动刷新 workspace 自身的 `version` 字段；修改应用版本后必须显式检查 `bun.lock` 中 `apps/electron.version` 是否同步。
- 2026-08-20：确认文件路径管理设计：全局数据根与单项目路径作为两个独立功能；采用复制、SHA-256 校验、原子切换和保留旧目录策略，移动盘/NAS 离线时禁止静默回退。
- 2026-08-20：完成路径管理的两阶段实施计划；先交付数据根定位、迁移与离线恢复，再复用校验组件交付项目路径迁移、工作区锁和中断恢复。
- 2026-08-22：补齐已发布 Bone 版本和 `0.17.55-bone.1` 的中文更新说明；Release 工作流改为读取仓库内说明文件，并在任务重跑时同步更新已有 Release 正文。
- 2026-08-22：完成项目文件路径管理与迁移：支持外部/托管/离线项目、复制校验、崩溃恢复、取消/继续/放弃、watcher 切换和桌面设置页；发布版本递增为 `0.17.42-bone.6`。
- 2026-08-22：将直接启动的开发客户端名称从 `Proma Dev` 统一为 `PromaDev`；正式客户端仍为 `Proma`，开发/正式 App ID 与 Electron 内部数据隔离规则保持不变。
- 2026-08-22：确认历史共享凭据由 `@proma/electron Safe Storage` 加密，`PromaDev` 与 `Proma Dev` 钥匙串均无法解密；开发客户端现于 `ready` 前使用历史加密身份，之后恢复显示名 `Proma Dev`。
- 2026-08-22：Windows 用户目录可能在定位文件已原子提交后，对目录 `open` 返回 `EACCES`，或对目录 `fsync` 返回 `EPERM/EACCES`；`safe-file` 仅将这些目录能力错误降级为 `file-only` durability，`EIO` 等真实 I/O 故障继续向上传播。
- 2026-08-22：合并官方最新正式版 `v0.17.55`，应用版本重置为 `0.17.55-bone.1`；保留 LAN、路径迁移守卫、Agent generation 隔离与历史 Safe Storage 身份，并将 Pi utility 的 abort/force-close/finally 统一到单一 runtime 关闭 Promise。
- 2026-08-22：关于/更新页将版本历史拆为 `Proma 修改` 与 `官方版本` 两个懒加载标签；历史查询固定映射 Bone/官方仓库并按来源隔离缓存，Electron Updater、最新版本和按标签查询仍只使用 `kuangtao22/Proma`。
- 2026-08-22：`v0.17.55-bone.1` GitHub Actions 全平台发布成功，并回填 `bone.1`、`bone.4`、`bone.5` 中文 Release 正文；若根目录类型检查异常解析到 `apps/electron/node_modules` 的旧 `@types/node`，说明是历史嵌套依赖遮蔽，`bun install --frozen-lockfile` 不会自动清理该目录。
- 2026-08-22：整理 fork GitHub Releases，仅保留当前正式版 `v0.17.55-bone.1` 与上一稳定版 `v0.17.42-bone.5`；删除旧 `0.9.x`、重复草稿及已淘汰 Bone Release，但保留 Git 标签用于历史代码定位。
- 2026-08-22：本机 `gh auth status` 中账号令牌失效时，Git HTTPS 仍可通过系统 Git 凭据直接推送 `kuangtao22/Proma`；排查推送权限时应分别验证 `gh` 与 Git 凭据，不把前者状态当作后者结论。
- 2026-08-22：用户确认 macOS 缺少签名证书时仍需发布，并允许通过 `xattr` 命令解除隔离安装；发布说明必须明确未签名风险，工作流有证书时签名、无证书时显式生成未签名包。
- 2026-08-22：修复 Windows 数据根迁移在计划创建后被退出流程更新 `.proma` 时无法继续的问题；零进度计划在隔离进程中安全刷新容量基线，旧版本已记录为 failed 的同类计划也可直接恢复。
- 2026-08-22：发布 `v0.17.55-bone.4`，macOS arm64/x64、Windows x64、Linux AppImage/deb 与自动更新元数据共 15 个资产均由 GitHub Actions 成功生成；macOS 云端未配置签名证书，Release 正文已提供 `xattr` 解除隔离命令。
- 2026-08-22：评估 `Xiangyu-CAS/codex-canvas` 后确认可将无限画布、JSON 状态模型和图片处理能力移植到 Proma；正式集成必须改用 React/Jotai、Proma 四层 IPC、`safe-file` 与 Pi Agent runtime，禁止原样引入其 Codex CLI/app-server、独立 MCP/HTTP 服务及 `~/.codex/generated_images` 扫描链路。
- 2026-08-22：用户确认 Proma「设计」采用方案 A：每个项目一个原生画布，通过顶部 `设计 · 项目名` 标签在会话与画布间切换，左侧继续只承载项目/会话；正式素材随项目保存到 `.proma/design/`，缓存放 `~/.proma/design-cache/`，首版聚焦画布、批注、Pi 图片任务、版本关系和会话传递。
- 2026-08-22：项目级设计工作区实施计划确定使用 `@xyflow/react@12.11.3`、revision mutation、目录级 `proma-file` 媒体授权和可见 Pi Agent Design Job；Design Job 仅允许 Nano Banana 图片工具，实施按 12 个 TDD 任务推进。
- 2026-08-23：完成手机端 Agent 会话四态、星标与相对时间展示；抽屉和顶部下拉统一为“色块、标题、星标、时间”四列，Automation、Bridge 与协作入口启动的会话均通过主进程事件实时同步，重连后从权威快照恢复。
- 2026-08-23：设计工作区的撤销/重做历史按权威文档基线失效：普通 remount 加载相同 revision 与相同 document 时保留；revision 或内容变化、恢复快照及冲突 rebase 时清空。
- 2026-08-23：设计保存冲突仅允许整批 `set-viewport`/`move-nodes` 自动 rebase；批次含任一结构 mutation 时采用远端基线、保留 pending 与失败阻断态，禁止自动或重试覆盖。job 节点首版只允许选择和移动，复制、删除、分组、取消分组及不安全历史在 reducer、Jotai action、键盘和工具栏统一拒绝；`executeDesignEditAtom` 在应用 reducer 结果前按当前文档复核完整 mutation，是 Renderer 防止间接改写 job 分组的最终防线。主进程 `SAVE_MUTATIONS` 在 workspace write guard 内基于权威文档保护 job 所有权并保留 recovery-required 语义；Renderer 所有权策略由 store 在单次权威加载后、apply/write 前执行，IPC 禁止额外预读。Renderer 保存遇到 recovery-required 时必须恢复 mutation 队列并先加载权威快照，位置 mutation 可在新基线上安全 rebase，结构 mutation 进入远端阻断；两者均不得直接重试旧 revision。结构冲突可通过“采用远端版本”清理本地冲突队列，后续编辑按远端 revision 保存。
- 2026-08-23：设计右栏完成素材、AI 编辑和版本三标签；导入素材由主进程先提交 `upsert-assets` 并返回新 revision，Renderer 同批乐观展示素材与节点但只将 `upsert-nodes` 放入保存队列，避免素材重复提交。Task 9 仅通过必填回调验证生成/编辑任务输入，connected 右栏在 Task 10 注入真实 handler 前保持 AI 提交禁用。
- 2026-08-23：设计素材导入遇到 `DESIGN_RECOVERY_REQUIRED` 时，Inspector 仅通过项目级一次性 Jotai 请求通知画布，唯一工作区 controller 在发起强制加载前同步把旧快照设为只读，成功后清空历史、选区、待保存队列与冲突状态并接管新媒体授权 URL；失败保持阻断并提供恢复重试。画布 mutation updater 必须基于最新 Jotai 状态二次校验，不能只信任 React 闭包中的 writable。
- 2026-08-23：Design Store 通过 `requireStableAuthoritativeDocument` 统一约束恢复边界；import/delete/relink/export 在选择器、staging、文件写删等业务副作用前遇到 tmp/backup 恢复必须抛 `DESIGN_RECOVERY_REQUIRED`。Renderer 四种素材 action 共用同一恢复错误分流，并交给唯一工作区 controller 重载和重置本地状态。
- 2026-08-23：Design Job 以项目缓存 journal 作为任务状态事实、Design Store 作为画布事实，并复用可见 Pi Agent 会话执行；单次运行级工具白名单只允许 Nano Banana，任务会话携带项目/job 追踪元数据并自动受既有 generation-owned 项目迁移守卫保护。任务事件携带 Store 权威 revision：同 revision 只刷新 jobs，revision 前进时同步加载创建/重试/成功产生的节点结构，并在权威文档上重放 Renderer 本地 pending。queued journal 与占位节点采用 `pending -> ready` 两阶段恢复：无占位的 pending 原子删除，有占位的 pending 补 ready，重启遗留 queued/running 统一转 interrupted。
- 2026-08-23：Design Job output 在完整 workspace write lease 内采用 terminal pending journal 与 Store 素材/节点双事实对账，durability 不确定时不得提前 commit、rollback 或标 failed；retry 以 Store 当前 job 节点绑定为所有权事实，并通过旧 journal 的唯一 replacement ID 保证重复请求幂等。Renderer 将任务 journal/结构同步错误与画布保存错误分离，普通失败保留快照和 pending 并提供精确 revision 重试，`DESIGN_RECOVERY_REQUIRED` 统一转入权威恢复。
- 2026-08-23：Design IPC 的权威 `LOAD` 完成后必须在同进程触发 terminal pending 二次对账，不能只依赖重启恢复。retry 在创建 replacement 前先持久化包含预分配 ID 的 pending intent；重复 retry 或启动恢复必须同时核对旧 journal、replacement journal 与 Store 节点绑定，续建同一 replacement 或拒绝第三方接管。Renderer 任务结构快照 revision 低于任务事件 revision 时保持同步失败并按原目标 revision 重试，禁止把陈旧快照当作 idle 成功。
- 2026-08-23：重启恢复在补完 pending retry intent 并新建 replacement 后，必须在同一次恢复中把新任务立即收敛为 interrupted；恢复只重建可重试状态，不自动调用付费 Agent run，后续运行必须由用户显式 retry。
- 2026-08-23：完成项目设计素材与 Agent 会话双向传递：Inspector 仅列同项目未归档会话并优先当前会话，素材引用只进入输入框；Nano Banana 图片结果可原地加入所属项目设计，无项目会话禁用入口，成功不切页。
- 2026-08-23：Agent 图片附件提升必须由同批、顺序 SDK 消息中的 Nano Banana `tool_use_id` 精确关联证明，禁止非 Nano 工具或独立结果字段伪造授权；设计导入根仅限该 session 附件目录与其实际 Agent cwd 下的 `generated-images`，并在读取前执行大小边界检查。
- 2026-08-23：Renderer 的 Agent 图片“加入设计”操作必须显式使用消息所属 `sessionId` 贯穿渲染链，禁止以当前激活标签身份代替消息身份；无项目会话的禁用按钮由可聚焦包装元素承载提示。
- 2026-08-23：Nano Banana 新图片附件只允许由本地主进程 `saveAttachment` 结果经 Pi `tool_use_result` 结构化详情传递，并以来源标识和 `toolUseId` 双重匹配提升；Gemini/工具普通文本中的附件标记永不构成新授权事实，旧 JSONL 已有的结构化 `imageAttachments` 与 legacy 事件继续只读兼容。
- 2026-08-23：完成设计工作区恢复、staging 清理、viewport 保存压缩、窄窗口与无障碍收口，并通过 Design/关联回归、全仓类型检查、Electron 完整构建及开发启动冒烟。
- 2026-08-23：修复 Renderer 忽略低 revision backup 恢复的问题；recovery 事件与 recoveredFrom LOAD 统一失效旧缓存基线，并按位置/结构 mutation 安全边界处理未保存编辑。
- 2026-08-23：质量复核确认 recovery 不再被 job 结构刷新取消；真实 Chrome 下 1000 节点首帧仅挂载 16 个可见节点，并完成宽窄窗口交互、无障碍与明暗主题验证。
- 2026-08-23：用户确认 Design Job 采用可信双模型链路：设计页按项目选择系统生图模型，现有 Agent LLM 先理解任务，再由主进程运行级上下文强制图片工具使用任务固化的模型；模型不能仅靠提示词传递，重试保留原模型，普通 Chat/Agent 不受项目选择影响。
- 2026-08-23：完成 Design 项目级生图模型选择与可信双阶段调用：设置页支持多 profile，Inspector 展示项目选择，任务节点展示固化的名称与真实模型 ID；两批共 261 个相关回归测试、全仓类型检查和 Electron 完整构建通过。真实 Electron 窗口完成宽窄布局、明暗主题、禁用/错误状态、设置定位、长名称与模型 ID、焦点可见性验收；本机 Nano Banana API Key 未配置，因此未执行会产生外部计费的真实出图链路，自动化测试已覆盖 Agent LLM 后的可信模型路由、取消、重试与单次图片调用上限。
- 2026-08-24：用户确认 GPT Image 2 采用低冲突统一模型配置方案：渠道继续管理连接、加密凭据和模型列表，生图目录只声明 `openai-images` 路由；设置入口从 Chat 工具迁到模型配置，Design 仍按项目固化可信快照并保持 Agent LLM -> 生图执行器的两阶段调用。
- 2026-08-24：完成 GPT Image 2 统一模型配置接入：模型配置页在渠道区块后管理生图 profile，Design 的配置入口跳转并聚焦该区块，Chat 工具页仅保留 Nano Banana 连接凭据；GPT Image 2 generations/edits、参考图边界、URL 安全下载、渠道刷新、可信快照与 Nano Banana 向后兼容均有 BDD 回归覆盖。
- 2026-08-24：修复 Design GPT Image 2 已生成图片却误报失败的问题，并为失败、已取消和已中断任务增加可恢复删除、垃圾桶入口及 Delete/Backspace 快捷键；现有成功图片无需重新付费生成。
- 2026-08-24：用户确认 Agent 与 Design 应共享同一个创作任务：Agent 对话负责语义理解、澄清与结构化转交，Design 负责视觉执行和版本迭代；两边共享原始要求、附件、上下文和状态，底层 Design 执行会话不进入普通会话列表，转交前不启动付费生图。
- 2026-08-24：Design 上下文从代码项目扩展为按任务匹配的创作上下文库，不固定项目类型；可按需使用品牌、产品、代码、角色、故事、场景和连续性资料，单次结果只有经用户确认采用为视觉标准后才能沉淀。完整 AI 漫剧分镜与批次生产另立规格。
- 2026-08-24：合并最新正式上游 `v0.17.59` 并将应用版本重置为 `0.17.59-bone.1`；上游“在文件改动面板显示记忆更新”只增加 `WorkspaceMemoryChangeDock` 展示入口，不修改 `MEMORY.md` 格式、存储路径或维护规则，与现有项目记忆和 Design 记录无结构冲突。
- 2026-08-24：完成 Agent 与 Design 统一创作任务的设计确认：结构化语义转交只预填不生图，所有 Design 提交走内部 Agent，上下文库支持品牌、产品、代码、角色、故事、场景、连续性和参考素材，任务详情展示摘要、精确提示词与可用 Thinking，内部会话转存 trace 后回收；完整 AI 漫剧生产另立规格。
- 2026-08-24：完成 Design 任务透明度第一阶段：真实失败任务可查看原始要求、精确提示词、模型、尝试历史、Thinking 和执行日志，任务节点与快捷键共用不可恢复删除确认；修复旧项目 trace 目录缺失导致转存失败的问题，并通过阶段回归、类型检查、Electron 构建及真实窗口验收。
- 2026-08-24：完成 Design 项目创作上下文第二阶段安全验收：视觉标准和子版本引用在删除事务前阻断；损坏 Catalog 不影响普通画布加载，`project` 模式在创建内部会话前失败，旧 Job 恢复为 `contextMode: none` 且不自动生图；231 个定向测试、全仓类型检查和 Electron 完整构建通过。
- 2026-08-24：真实启动发现 Design 上下文工具顶层导入 ESM-only Pi SDK 会使 Electron CJS 主进程报 `ERR_PACKAGE_PATH_NOT_EXPORTED`；已改为本地纯类型推断 helper，并增加按主进程构建条件检查同步 `require()` 的回归测试，开发窗口启动、IPC 注册和 Agent runtime 初始化通过。
- 2026-08-25：真实 GPT Image 2 验证发现图片已生成但 Design 误报失败，根因是完成回调仍使用旧 `AgentMessage.events`，而 Pi 把结构化附件写入持久化 SDK `user/tool_result.imageAttachments`；现改为在真实图片工具调用关联、会话归属、媒体类型和文件校验边界内兼容该权威消息，并在 Renderer 增量 upsert 与全量合并入口统一排除 Design 内部会话。
- 2026-08-25：Agent 项目会话中的高置信度视觉请求必须在发送前询问“打开设计面板/继续 Agent 实现”；Design 交接先切到会话所属项目，只预填原始要求且不自动生图。明确代码实现和普通分析不拦截，未命中本地判断的歧义表达由 Agent 系统提示词继续澄清，禁止把“设计”默认解释为修改代码。
- 2026-08-25：用户确认以项目下 `Canvas` 会话类型替代“一项目一张全局 Design 画布”：每个 Canvas 可包含多个独立 Pi Agent、生图、视觉文档和底层 `webview`/界面“原型”模块；连线是显式数据与任务合同，无连线完全独立。Agent 按语义自动建模块和连线，讨论只建待运行结构，明确执行才调用付费能力，上游变化仅标记下游待更新；旧 Design 迁移为默认 Canvas 并保留素材、任务、版本和布局。
- 2026-08-25：Canvas 顶层会话基础使用项目内独立原子索引，不写入 `AgentSessionMeta` 或 Agent 会话索引；旧 `.proma/design/canvas.json` 仅在列表时以固定 `legacy-design` ID 幂等投影，现有 Design Store、生图、素材、任务和 trace 继续使用旧路径，待后续迁移阶段显式切换。
- 2026-08-25：Renderer Canvas registry 独立于 Agent atoms；`legacy-design` 是唯一允许加载旧 Design 工作区的兼容入口，原生 Canvas 缺少独立文档时显示空画布，禁止别名复用旧画布。Agent 发起 Design 转交时使用确定性的 legacy Canvas 选择；所有普通会话统一导航入口在守卫通过后必须清理 Canvas 选择，避免顶部身份与主视图不一致。
- 2026-08-25：归档 Canvas 分组固定排在 Agent 归档历史之前，避免 Agent 归档异步加载或超长历史列表把 Canvas 恢复入口挤到列表末尾；归档状态仍只由项目 Canvas 原子索引维护。
- 2026-08-26：稳定目录 Windows 安全合同固定为：授权后若目录被 junction 替换且无法相对根 HANDLE 重开子项，整项 fail closed 为空并绝不读取 replacement；独立 Windows 构建与正式发布必须在打包前运行 helper/host 定向测试，UNC 管理共享不可用时只能显式 skip。
- 2026-08-26：Canvas Agent 节点创建使用目标 Canvas 内的可恢复 intent 事务，在同一 workspace write lease 内按 `prepared -> session-created -> committed` 推进；LOAD、SAVE 和 CREATE 都先对账，只有 committed 后才能广播或返回。用户后来删除节点时 intent 转为 `detached`，不重建引用；预分配会话 ID 仅允许完整 Canvas 归属的内部会话幂等复用。
- 2026-08-26：Canvas 创建事务对账产生的 graph revision 是独立发布事实，SAVE/CREATE 后续失败也必须在 workspace lease 释放后广播并原样抛错；Canvas Agent 会话必须独占 Canvas 三字段归属，禁止混入 Design、Automation 或 Delegation/父子协作来源。Store 同次安全 LOAD 只提供固定 `canvasRoot` 的 canonical/dev/ino capability；`transactions` 创建、intent 扫描和原子写全部由 stable-directory helper 在授权后的同一 root fd/HANDLE 下相对执行，扫描正文直接走内存协议且不向 Node 返回可重开绝对路径，写入正文只在授权后经 stdin 传递。
- 2026-08-26：Canvas Agent CREATE 必须通过单次 `createReconciled` 同时返回历史对账事实与当前操作结果；IPC 不得预先重复对账，必须在 workspace lease 释放后先广播历史 revision，再返回结果或原样抛出后续错误。单次 CREATE 只允许扫描一次 transactions 目录。
- 2026-08-26：workspace write lease 只用于迁移准入和引用计数，不提供写串行；Canvas LOAD/SAVE/CREATE 异步化后必须在 IPC 边界按 `projectId + canvasId` 键控串行完整事务，不同 Canvas 保持并行。兼容 Node intent 读取必须把初始路径 `lstat`、打开 fd 和最终 fd/path 的 `dev/ino/size/mtimeNs/ctimeNs` 全部绑定，拒绝同名 inode 替换与同 inode 等长改写。
- 2026-08-26：Canvas helper 的 `OPENED` 授权必须在回调瞬间重新读取 native session registry，并重新解析当前项目/Canvas 路径；只有当前 canvasRoot、首次 LOAD capability 与 helper OPENED 的 path/canonical/dev/ino 三方完全一致才允许 scan/write，撤权、迁移或重链一律 fail closed。`session-created -> committed` 成功补写即构成必须发布的图事实，即使节点 revision 已在上次失败前落盘；同 Canvas 继续串行，不同 Canvas 保持并行。
- 2026-08-26：Canvas reconciliation 发布统一以 `recoveredFrom` 优先于普通 graph，SAVE/CREATE 后续成功或失败均不能吞掉恢复事实，且所有广播必须在 workspace lease 释放后执行。Canvas intent helper 在同一 transactions fd/HANDLE 下只统计合法 UUID 普通文件，512 个时允许覆盖并拒绝新增；写协议区分 rename 前失败、提交成功和 rename 后耐久性未确认，后者由服务重扫精确 intent，可见后先发布再传播明确错误，不可见则 fail closed。目录 `dev/ino` 全程使用 bigint 与 helper 十进制字符串精确比较；Renderer 切换 Canvas 时以命令代次隔离旧创建请求的迟到结果。
- 2026-08-26：Canvas intent 容量检查、目标覆盖、相对 rename 与目录 flush 必须由固定内部普通锁文件提供跨进程原子性，不能只依赖单进程或单 IPC 串行；POSIX 锁文件使用 `O_NOFOLLOW`/`O_CLOEXEC` 与 `flock`，Windows 必须在同一 transactions HANDLE 下相对打开、拒绝 reparse 并使用 `LockFileEx`。锁文件不计入 intent 容量，helper 被 host 超时终止后由系统释放锁。
- 2026-08-26：Canvas intent 的所有状态迁移（包括 `detached`）都必须消费 helper 的结构化写确认；detached rename 已可见但目录持久性未确认时原样传播明确错误，同时保持 `documentChanged=false` 且不发布 graph revision。intent scan 容量只统计 no-follow 验证后的合法普通文件，目录、symlink 与 Windows reparse point 均忽略且不占 512 项预算。
- 2026-08-26：修复 Canvas Agent renderer reload 归属恢复、损坏 completion 旧缓存 fallback、完成通知未切 Design 视图、切节点 SEND 迟到回调污染和 owner/messages 无界生命周期；active-run bootstrap、事件 gate 与 Jotai lifecycle 均有 BDD 回归覆盖。
- 2026-08-26：Canvas Agent completion 的正常与早期异常路径统一携带主进程权威轻量 metadata，所有 completion 进入 bootstrap gate；SEND 准入拒绝的全局 lifecycle 收口不再依赖已卸载 UI，open/running 消息完整保真，active invalid 与有界 terminal tombstone 分层管理。
- 2026-08-26：补齐 headless completion 异常路径的权威 metadata，统一四条 completion producer；soft completion 不再提前结束合法或损坏 Canvas 运行，bootstrap gate 改为 O(1) 环形流缓冲与独立有界终态缓冲，token 洪峰不再淘汰 completion。
- 2026-08-26：Agent completion/error 跨 IPC 只允许显式公开 metadata 白名单，缺 metadata 不再使用 Renderer owner fallback；Canvas error 与 completion 共用 bootstrap gate，面板 busy 同时服从 active-run 快照和 stream state，hard completion 先由权威 JSONL 接管最终消息，再在 closed/settled 边界回收 live/error/stream 状态。
- 2026-08-26：Canvas Agent SEND 的 Renderer 乐观运行与 bootstrap/流事件权威运行必须分层；`userMessageUuid` 作为乐观 token，`startedAt` 作为权威运行 generation，迟到 reject 只能收口匹配 token。主进程同会话 busy 通过稳定内部码映射为公开 `SESSION_BUSY`，保留真实 running/owner 以维持 STOP 可用和 SEND 禁用；hard invalid 必须立即回收 live/error/stream 共享状态，soft invalid 继续保留 active fail-closed runtime。
- 2026-08-26：Canvas Agent completion 必须在主进程启动预留、active-run bootstrap、Renderer lifecycle 和 JSONL GET 交接全链路携带并复核同一 `startedAt`；缺 generation 或旧 generation 完全无终态副作用，GET 晚回需二次校验，closed/settled 后释放 generation。异常 completion toast 只属于普通 Agent，合法 Canvas 仅更新面板错误，损坏内部会话不进入 toast、通知或普通会话 upsert。
- 2026-08-26：Canvas Agent 权威 `startedAt` 与 Renderer 乐观 generation 必须分表保存，权威代次存在时禁止回退乐观值；active-invalid bootstrap 仅公开并恢复 `sessionId + startedAt + valid:false`，snapshot replace 同步回收无当前运行和无 optimistic token 保护的 stale generation。
- 2026-08-26：完成 Canvas Agent 节点创建落点与对话关闭交互收口：新节点按可视中心确定性避让，关闭对话后迟到选区事件不再重开；真实 Electron 验证新增节点、Canvas 切换、重载恢复和深浅主题，既有修复前重叠节点保持原位置不做隐式迁移。
- 2026-08-26：用户确认 Canvas 节点操作与局部故障恢复设计：恢复顶部悬浮工具栏，区分独立添加和节点扩展连线，删除保留对话；单个坏会话不再阻断整张画布，提供显式重建或删除并使用公开中文错误状态。
- 2026-08-26：原生 Canvas 节点命令统一由顶部工具栏与节点侧扩展入口触发；扩展节点和边共享事务 revision，删除保留底层会话并受主进程 active-run 守卫。committed 坏会话只派生内存 `nodeIssues`，重建通过独立可恢复 intent 换绑空白 session；Preload/Adapter 只允许公开错误信封进入 Renderer。
- 2026-08-27：`stable-directory-native-host` 的真实 helper 合同测试必须在文件级只编译一次原生产物，禁止每个用例重复冷编译挤占 Bun 默认 5 秒卡死保护；当时组级构建钩子使用 30 秒上限，此预算已被 2026-09-07 的 Windows CI 证据推翻，现按编译子进程 120 秒、hook 150 秒执行，单用例继续保留原有超时。
- 2026-08-27：`stable-directory-native-host` 必须等 ChildProcess `close` 后再判断协议是否提前结束；`exit` 可能早于 stdout 管道排空，不能据此拒绝已经正常产出的尾部 `write-result`/`done`。
- 2026-08-27：完成原生 Canvas 客户端手测，覆盖创建与重命名、独立/扩展 Agent、自动连线、对话、删除确认与快捷键、缩放平移拖动持久化、深浅主题、归档恢复和窄窗口布局；修复深色缩放控件不可见、对话面板遮挡工具栏及移动节点被 LOAD 对账误判归属损坏，83 项定向测试、全仓类型检查和 Electron 完整构建通过。
- 2026-08-27：修复新 Canvas 顶部添加入口看似无响应，以及 Agent 面板打开后节点被裁在边界造成选中框闪烁的问题；单类型时直接创建，新增落点与一次性可见区校正统一基于真实 Canvas surface，89 项定向测试、全仓类型检查和 Electron 完整构建通过。
- 2026-08-27：用户复测确认边框仍闪后，通过真实客户端像素与 Electron DOM 连续采样定位第二根因：两个节点的 `selectedNodeId` 被 React Flow 派生 selection 回调循环互换；改为由 `onNodesChange` 单向同步用户选择并去重，连续 5 秒节点 ID 与 16 帧边框像素稳定，90 项定向测试、全仓类型检查和 Electron 完整构建通过。
- 2026-08-27：Canvas 非 Agent 内容以 `meta.json` 最后提交作为创建完成标记；`trashId` 只表示一次回收身份，始终与稳定 `contentId` 分离。删除、恢复与重放必须保留 helper 的结构化提交证据，提交已可见但耐久性未确认时优先返回可对账错误，授权或后置校验失败不得覆盖该事实。
- 2026-08-27：Canvas `content-node` intent 永不承载 Agent 节点；Agent 与内容 intent 共享 `transactions` 的 512 项总容量。内容生命周期的 committed tombstone 负责 operation 幂等，图 publication 与本轮是否再次 mutation 分离：重启仅补 tombstone 时仍发布既有 revision，后续 committed 对账不重复发布。
- 2026-08-27：Canvas 多类型节点四层接口只使用主进程唯一的内容 Store/Lifecycle；LOAD 在同一 workspace lease 内先完成内容迁移与对账，再执行 Agent 对账。历史与当前 publication 必须在 lease 释放后按顺序发布，并按 `cause + revision` 去重；通用 SAVE 禁止绕过生命周期直接删除非 Agent 节点。Renderer 创建成功只接管权威文档和新节点选区，不得清理请求期间形成的 `expandedNodeId`、待切换目标或 dirty 草稿。
- 2026-08-28：Canvas 节点工作台只由 Renderer `expandedNodeId` 驱动，同一时刻最多挂载一个节点锚定覆盖层；dirty 跨节点切换必须走保存、放弃、取消三分支，通知导航也必须复用同一 dirty-aware 状态转换。切换 Canvas、recovery、删除和真实卸载清理临时态，StrictMode 同 key 演练重挂不得误清。
- 2026-08-28：Canvas 工作台草稿保存 Promise 必须同时复核 workspace key、源节点、目标节点和单调 operation generation；业务身份 stale 时只收口当前 generation 的 saving，generation 或 workspace stale 时完全无副作用，避免迟到结果关闭新工作台或永久锁住切换对话框。
- 2026-08-28：Canvas 运行中 Agent 的“停止并删除”必须 authoritative-first 绑定明确 `startedAt`，每次 STOP attempt 另有独立单调请求代次；resolve/reject 在任何 UI 副作用前同时复核请求 token、Canvas、节点、session、`startedAt` 和当前 pending 身份，新运行接管、代次未知、Canvas 切换或卸载时先失效旧 token 并 fail closed。回收区每次列表读取使用独立单调请求代次，恢复成功、关闭和销毁都会失效旧请求，禁止迟到结果复活旧条目或覆盖新状态；restore 成功失效在途 list 时必须同步收口 `loading:false`。
- 2026-08-28：Canvas 顶部独立新增节点采用“可视区优先追加”：全局右侧追加位置完整可见时保持原有横向排列，超出可视区后在当前视口按从左到右、从上到下寻找空位；既有节点与 viewport 始终不移动，可视区无空位时才回退到确定性全局追加。
- 2026-08-28：Canvas 顶部添加入口使用锚定 `+` 按钮下方的 Radix `Popover` 单列菜单；选择 Agent/生图/文档/原型后立即关闭并沿用工作区可视区追加逻辑，视频保留“即将支持”禁用占位。该实现不再使用曾出现视觉显示不稳定的 `DropdownMenu`，也不使用遮挡画布的居中 `Dialog`。
- 2026-08-28：用户确认 Canvas 生图节点采用现有 Pi Agent/Design Job/Design Asset 的单引擎双目标适配；配置、任务和版本按 `projectId + canvasId + nodeId + imageModuleId` 隔离，成功自动采用、重试固化旧快照，工作台展示最终提示词、上下文和可用 trace。节点单击只选中，双击或放大按钮打开工作台；节点侧 `+` 打开 Agent/生图/文档/原型悬浮菜单并创建自动连线的下游节点。
- 2026-08-28：Design 图片工具使用独立 10 分钟跨进程超时，普通工具继续保持 120 秒；单次图片工具调用后终止本轮以避免 Agent 自动二次付费调用，无图片输出时保留清洗后的真实工具错误。Canvas 图片采用提交后必须广播准确 revision 的 `graph` 变化事件，广播失败不得回滚已提交素材与节点事实。
- 2026-08-28：Canvas Agent 工作台与普通 Agent 共享 `AgentMessages`、`RichTextInput` 和 `AgentComposerFrame`；Thinking、工具执行卡片、输入框表面及发送/停止控件保持同源样式，Canvas 仅保留节点工作台外壳和必要的轻量交互。
- 2026-08-28：原生 Canvas 会话删除必须经过独立不可恢复确认，并打通 shared、main、preload、renderer 四层合同；删除前阻断运行中的 Canvas Agent/生图任务，成功后清理会话索引、Canvas 正式目录、可重建缓存和按完整 Canvas 归属隔离的内部 Agent 会话，同时关闭当前 Canvas 入口。`legacy-design` 仍引用项目级旧存储，只允许归档，不提供删除。
- 2026-08-28：Canvas Agent 扩展连线只属于创建事务提交前的恢复合同；committed 后连线回归可编辑图状态，删除连线或上游节点时下游 Agent 合法转为独立节点。历史创建 intent 只继续约束节点与会话归属，禁止因已合法消失的初始连线阻断整张 Canvas 的加载或删除。
- 2026-08-28：原生 Canvas 选择模型分为主节点 `selectedNodeId` 与 Renderer 完整多选集合：主节点继续服务详情和单节点工作台，完整集合负责框选、批量拖动与删除，禁止把 XYFlow 多选压缩成首个节点。批量删除复用单节点主进程生命周期并按权威 revision 串行提交，部分失败保留剩余选区和稳定 operation；多选含运行中 Agent 时整批阻断。
- 2026-08-29：Canvas 节点工作台必须以 XYFlow 的 `nodrag nopan nowheel` 类隔离节点拖动、画布平移和滚轮手势，并恢复普通指针；生图工作台的配置内容随详情滚动，生成、取消和重试主操作固定在底部，避免长内容遮挡操作入口。
- 2026-08-29：Canvas 生图媒体授权根已直接指向 `assets`/`thumbnails` 目录，Renderer 只能拼接编码后的文件名，不能再次拼持久化相对路径中的目录段；否则会形成 `assets/assets` 或 `thumbnails/thumbnails` 并让已成功素材显示为破图。
- 2026-08-29：Canvas 折叠生图卡片通过工作区 LOAD 一次性接收已采用素材的安全 `imagePreviews`，同窗口同项目共享单个缩略图媒体授权，禁止逐节点加载图片模块；授权、素材或图片加载失败时回退文字卡片，旧生命周期快照不得清空已有预览。
- 2026-08-29：Canvas Agent 重建事务固定使用 `agent-node-rebuild-<UUID>.json`，文件名合同必须由 TypeScript Host 与 C++ stable-directory helper 同步识别；开发客户端实际执行 `dist/resources` 副本，修改原生 helper 后必须执行资源同步并重启，否则会出现重建当下成功、重载后因旧 helper 漏扫重建事务而整张 Canvas 加载失败。
- 2026-08-29：修复 Canvas Agent 详情消息撑高后裁掉输入框的问题，并为四类节点工作台增加右下角双向缩放；详情首次打开会按当前画布剩余空间收敛，真实 Electron 验证生图主操作和缩放手柄在底边可达，拖拽宽高同时变化且不移动节点。
- 2026-08-29：合并官方最新正式版 `v0.19.1` 并将应用版本重置为 `0.19.1-bone.1`；接入 Vault、Terminal、OfficeCLI、双 Pane 与 MCP 安全更新，保留 Canvas、DataRoot、独立规划窗口和 Todo Agent 跨窗口接管，并补齐普通 Renderer 对内部会话的隔离。
- 2026-08-29：Canvas 图片详情的每次 LOAD 必须签发不可复用 `mediaLeaseId`，RELEASE 只精确释放对应授权；旧详情的迟到释放不得取消新 LOAD，失效 controller 收到迟到成功快照时主动回收该授权。
- 2026-08-29：Canvas 折叠生图卡片复用工作区 LOAD 已验证的素材宽高计算受限比例，固定 288px 宽、预览高度限制 96–320px，图片使用完整展示且 XYFlow 节点与连线 Handle 共用同一高度事实；Canvas Agent 身份通过可信单次系统上下文追加，不改写用户消息或 JSONL，并明确禁止要求用户另建或切换 Design/Canvas。
- 2026-08-29：用户确认将 Canvas 从左侧独立会话和独立主视图迁入普通 Agent 的右侧工作区，界面统一使用中文“画布”；画布仍归项目所有，每个 Agent 默认绑定一张并可关联多张，多个 Agent 可共享同一画布但视口、选区和面板状态按 `sessionId + canvasId` 隔离。节点通过结构化引用进入对话，Agent 使用受控画布工具按需读取、批量创建或修改节点并建立连线，明确执行才调用付费能力；长期上游接缝收敛到右侧工作区适配与 Pi 工具提供器。
- 2026-08-29：Agent-Canvas 共享合同固定以 `AgentCanvasBinding` 维护项目内会话关联，重复画布按首现顺序去重且默认/最近画布必须已关联；发送与队列使用公开 `canvasNodeReferences`，SDK JSONL 持久化使用私有 `_canvasNodeReferences`，两者不复用附件或 mention。共享层以 `CanvasBatchOperationEnvelope` 严格解析并深度隔离 plain JSON 外壳，不声称 mutation 已验证；真实 Agent 发送边界由 Agent service 接入引用解析，批量 mutation 的完整 schema、重复 ID 和图引用由事务执行前的 CanvasDocumentStore 权威验证并构造 `CanvasBatchOperationInput`，禁止在共享类型重复维护图语义。
- 2026-08-29：普通 Agent 与项目画布的多对多关联独立保存在业务配置根 `agent-canvas-bindings.json`（schema v1），不写入 `AgentSessionMeta` 或画布文档；索引严格拒绝未知字段、错误版本和重复项目+会话身份，损坏读取只降级为空并保留原文件，直到下一次有效业务变更才通过 safe-file 原子重建，no-op 不写盘。每次 mutation 必须 fresh-read 并绑定完整文件状态执行 secure CAS，合法旧值作为 prior backup；冲突或提交可见性不确定时失效缓存并从磁盘权威重载，禁止多 Store 覆盖已提交事实。
- 2026-08-29：Agent-Canvas LIST 的 legacy 投影与失效关联修复属于项目 mutation，必须先检查只读原因并进入 workspace write guard；画布枚举保持纯读。项目级失效会话与画布对账由 Store 单次 fresh-read、单次 secure CAS 提交，只有提交成功后统一广播；CAS 冲突轮零事件并由下一次 LIST 基于磁盘权威重试。CAS 成功后不得再次读取易变会话事实来阻断已提交 changes 的广播，返回与事件都使用同轮对账快照。Link、unlink、default 与删除清理同样必须由 Store 在单个 fresh snapshot 内原子返回 before/after/changes，IPC 禁止用长期缓存自行推导事件。
- 2026-08-29：原生 Canvas Renderer 状态拆为共享 graph 与 Agent session view：`NativeCanvasState` 只缓存 snapshot、mutation 保存/恢复状态，视口、工具、完整选区、展开节点、工作台尺寸和草稿按 `JSON.stringify([sessionId, projectId, canvasId])` 隔离；平移缩放只更新 view atom，不生成共享 mutation 或 revision。通知导航早于 LOAD 时只暂存节点意图，首个权威 snapshot 仍负责首次 viewport 初始化；StrictMode 安全的真实最后卸载删除当前 session view，并保留共享 graph 与其它 session view。
- 2026-08-29：同一原生 Canvas graph 的 Renderer controller 按 `projectId + canvasId` 单例引用计数，共享 LOAD、事件订阅和 SAVE scheduler，最后一个 graph 用户真实卸载才 dispose。创建、删除、恢复、重建以共享结构 token 串行化，token 期间所有 session view 禁止追加 graph mutation 或启动 SAVE，dirty graph 不允许开始结构操作；工作台 resize 高频 move 只更新 Overlay 局部预览，pointer 结束时一次提交 session view 尺寸。
- 2026-08-29：Canvas 结构操作取得共享 token 后，所有 adapter 调用必须位于 async `try/finally` 边界内；同步抛错与异步拒绝均须收口 loading/error 并释放 token，禁止因 Promise 链尚未建立而永久阻塞后续结构操作和 SAVE。
- 2026-08-29：普通 Agent 右侧画布标签由 `AgentCanvasBinding` 的关联顺序与项目 Canvas 元数据共同派生，后台 Canvas 事件只推进活动状态，不切换焦点；窄宽仅降级为单 Pane 呈现并保留 split 身份，展开态继续按 Agent 会话、项目与画布隔离。
- 2026-08-29：普通 Agent 右侧的 `legacy-design` 必须直接挂载旧 `DesignWorkspaceView`，禁止构造原生 Canvas controller 或触发 native LOAD；Canvas 菜单 Promise 由 TabBar 的 pending/finally 与 SidePanel 固定中文错误边界双层收口，删除被运行任务阻断时保持确认框以便停止后重试，内部异常正文只进入工程日志。
- 2026-08-30：Agent 右侧画布 Registry 的 LIST、关联动作和事件订阅必须绑定 `projectId + sessionId + generation`，旧宿主迟到成功、失败与 finally 均不得回写或提示；删除确认额外绑定 Canvas 与独立 operation generation。画布活动使用完整 view key 的 `activityRevision/seenActivityRevision` 轻量状态，不能为未 LOAD 的后台画布伪造 viewport；同一 Renderer 的 Canvas 变化消费者共享一个 Preload listener，并在最后消费者释放时解绑。legacy 与 native view 共用按完整 key 引用计数的 StrictMode 延迟清理协调器：同 key 即时重挂取消候选，切换或真实最后卸载只删除 session view 与待导航，不清共享 graph。
- 2026-08-30：普通 Agent composer 的 Canvas 节点引用只保存在按 `sessionId` 隔离的 Renderer Jotai 内存状态，Canvas 动作每次从当前权威 snapshot 过滤有效选区并生成完整公开快照，不写共享 graph/view atom。立即发送和 after-current 队列只在主进程正式接管后精确清除本次未变化引用；失败合并恢复最新正文、附件与引用，历史消息只展示发送时 `_canvasNodeReferences` 的标题和 revision，不回查当前节点替换历史。
- 2026-08-30：Agent 消息的结构化 Canvas 引用本身属于可发送内容，不能用空正文判定整条消息为空；Renderer 发送 helper 必须返回明确的 `submitted/skipped` 结果，只有主进程真实接管成功后才能清除引用或移除队列投影，历史消息即使正文为空也必须渲染引用 chip。
- 2026-08-30：Canvas 引用合并必须区分来源：用户从权威 Canvas 再次引用同一节点时原位替换为最新快照，失败恢复或撤回只补缺失 key、禁止旧 revision 覆盖当前引用。after-current 的 started 状态不携带引用，Renderer 必须在删除匹配队列项前捕获其 `canvasNodeReferences`，并写入 optimistic 用户消息的 `_canvasNodeReferences`。
- 2026-08-30：Agent Canvas 批量提交由主进程唯一服务与 LOAD/SAVE/所有可能写图的单节点及 Agent 对账操作共享同一按 Canvas serializer 和 workspace write lease；任何新批次或其它图变更都必须先收敛该 Canvas 的全部 pending batch intent，恢复失败时禁止产生第二份 intent 或当前操作副作用。batch `execute` 在锁内成功/失败 envelope 中保留全部恢复 publication 与可选当前 publication，唯一外层等 serializer 和 lease 完全释放后按旧到新顺序 best-effort 广播，再返回或重抛原始错误；单条发送失败不回滚图事实，也不阻断后续 revision。`canvas-batch-<UUID>.json` 对每项 Agent session、内容目录及内容 trash 移动先持久化 `pending/preparing/ready/cleanup-pending/cleaned` ownership，再执行副作用；Store 从权威基线按 mutation 顺序规划一次最终文档，intent 固化不含 revision/时间的最终图 SHA-256，不确定提交只接受 `baseRevision + 1` 与精确哈希同时匹配。图片、文档、原型内容资源按 `节点类别 + resourceId` 的基线/最终引用集合规划，合法同类型 A→B 转移或删除部分共享引用均保留活动目录，只有最后引用删除才单次移入 trash；Agent session 则按 `agentSessionId -> nodeId` 保持单图唯一且跨基线/终态不可转移，同图共享或 A→B 复用必须在 intent、inspect 和资源副作用前拒绝。跨类型复用同一物理 contentId、remove→recreate 同节点 ID 同样在 intent 前拒绝，create→remove 不创建目录、trash 或 session。图片、文档、原型删除继续复用 `CanvasTrashEntry` move/restore，Agent 删除保留旧 session；清理失败或目录耐久不确定时保留恢复证据并要求 LOAD 对账。Store mutation 校验以节点/边 Map、Set 和邻接索引保持 O(n)，`sourceToolCallId` 作为跨重试幂等键。
- 2026-08-30：普通 Agent 的画布产物联动由 Agent 语义和 `AskUserQuestion` 决策，Renderer 不做关键词路由；模糊视觉需求固定提供 WebView、图片设计稿和继续普通 Agent 三种选择，明确需求不重复询问。`canvas_create_artifact` 先准备完整 HTML 或图片提示词，再通过批处理原子提交图事实，成功后只打开产出会话自己的右侧画布并定位节点；WebView 创建后即可直接预览，禁止再调用 `canvas_run_nodes` 触发无效审批，图片默认只预填提示词且不自动付费生图。
- 2026-08-31：Canvas WebView 详情只在展开时按完整五元身份读取受管 `index.html`，快照 revision 不匹配时不得继续执行旧 iframe；WebView `meta.json` 必须通过统一严格入口解析并保留必需的 `legacySourceUrl`。Agent HTML 预览采用前置 CSP、无同源 sandbox 与 Electron `will-frame-navigate` 三层离线隔离，任何 srcdoc 子 frame 的脚本、刷新或链接导航都不得离开 `about:srcdoc`。
- 2026-08-31：Canvas WebView 节点只维护一份响应式 HTML，并以持久化 `desktop/mobile` 设备预设控制卡片外形、离屏静态缩略图和详情 viewport；卡片不运行 iframe，预览按完整节点身份、内容 revision 与设备预设缓存，切换设备不调用 Agent、不复制内容、不自动重排其它节点。
- 2026-08-31：Canvas WebView 设备切换后的静态预览必须等待对应 `set-webview-device-preset` mutation 完成权威保存后再请求；主进程继续严格校验完整目标身份，普通移动、缩放和其它节点保存不得阻断 WebView 预览。
- 2026-08-31：用户确认 Agent 画布生产系统采用“通用产物图 + Agent 编排循环”：一个普通 Agent 对话默认维护一张画布，聊天只保留持续更新的任务卡和关键决策，画布只保存长期 Agent、产物及 `association/reference/depends-on/derives` 关系，执行过程独立保存为 Plan/Run/Artifact Revision。普通 Agent 是唯一 Orchestrator，低风险结构和草稿操作自动执行，付费、覆盖、删除、批量运行和外部导出统一审批；实施固定按通用产物生命周期、单 Agent 编排、局部迭代与交付、受控多 Agent、新媒体执行器分阶段推进，首轮不开放第三方插件或任意连线自动触发。
- 2026-09-01：Canvas 图片版本是主进程从成功 Job 与现存 Asset 严格派生的瞬时公开事实；`LOAD_IMAGE_MODULE` 通过图片 Artifact Registry 返回有限、去重、稳定排序的 `imageVersions`，Renderer 只消费该字段并禁止自行从 `jobs/assets` 重建版本历史。完整图片模块快照必须在 Preload 和 Renderer Adapter 通过 shared exact-key parser 深重建，严格限制 100 个版本并复核任务、素材、来源与排序关系；公开素材集合必须精确闭合为目标 Job 输出与 adopted 祖先链，拒绝孤儿、断裂、循环及 generate/edit 父链不一致，非法成功信封统一降级为公开加载失败。
- 2026-09-01：Canvas 原生 helper 在一次提交需要刷新多个目录时必须分别执行全部 `fsync`/`FlushFileBuffers` 后再聚合耐久结果，禁止短路跳过后续目录；Canvas 关系的 `relation` 判别字段必须在 shared、IPC exact-key 解析与主进程重建全链路保留。
- 2026-09-01：Agent 创建 Canvas 文档产物时，revision 0 的 `content.md` 必须保存真实初始 Markdown；相同身份重放需逐字比较正文并幂等成功，不同正文必须按 `CANVAS_CONTENT_IDENTITY_CONFLICT` 拒绝，空白节点与 legacy 文档仍使用空正文默认值。
- 2026-09-01：Canvas 文档与 WebView 正文版本使用 `revisions/<sha256(contentId, revision)>` 不可变存储，图节点只保存当前采用的 `contentRevision`，所有读取、更新、采用和导出先在主进程对账，再经同一 Canvas serializer 与 workspace write lease 提交；WebView 详情、静态缩略图与新版文档/原型生图输入必须按图节点当前 revision 读取，v1+ 只读不可变版本库，只有 v0 才回退 `nodes/` 初始化基线。新边语义固定为 `association/reference/depends-on/derives`，旧图片直接入边迁移为 `reference`，图片版本继续从 Design Job/Asset 派生。Trash v2 必须完整保存三类内容节点的采用状态；共享物理内容批删采用“一次资源移动 + 每节点独立快照”，未恢复状态只留在 `trash` 侧 canonical tombstone，全部恢复后清理 tombstone，nodes 侧幂等历史按 nodeId 去重并保持有界。
- 2026-09-01：项目行右侧 `+` 恢复 Proma 官方一键创建 Agent 会话行为：按钮直接调用 `onNewSession(workspaceId)`，保留项目归属、事件冒泡隔离和快捷键 Tooltip，禁止再次套入只有“新建 Agent 会话”一项的 Dropdown。为什么这样处理：单项中间菜单不提供选择价值，且偏离官方交互；对用户的影响是新建项目 Agent 从两次点击恢复为一次点击，旁边的项目管理菜单、会话创建链路和错误处理不变。性能上减少一次 Radix Dropdown 挂载，没有新增状态或 I/O。
- 2026-09-01：Canvas 开放能力新增 Proma 内置 `canvas-production` Skill：Skill 只负责用户意图触发、产物路由和九个 Canvas 工具的操作编排；项目授权、Schema、revision、事务、路径、安全边界、删除/覆盖/付费审批继续由原生 Host 负责。Skill 的启停不得成为权限依据，Canvas 工具仍原生注入；长流程说明从 `systemPromptAppend` 迁入 Skill，原生提示仅保留最小安全合同。为什么这样处理：工作流知识需要独立迭代和按需加载，但可编辑、可停用的 Skill 不能承担可信执行。对用户的影响是 Canvas 请求编排更一致，普通代码任务减少无关提示和误路由，停用 Skill 不会削弱安全边界或删除既有产物。
- 2026-09-01：Agent 权限、问答和计划审批在主进程已因超时或中止删除后，Renderer 的迟到响应必须按无副作用幂等成功处理，不能再把空 owner 作为非法会话抛错；真实 owner 仍先经过普通会话可见性校验。工具提供给 Agent 的路由约束与用户审批说明必须分离，`canvas_run_nodes` 的审批描述只呈现生图行为和潜在模型费用，WebView 禁止运行等编排规则保留在系统提示与 `canvas-production` Skill。为什么这样处理：内部约束不等于用户当前操作说明；对用户的影响是过期卡片可正常收起，生图审批不再显示无关 WebView 文案。性能上只增加常数级空值判断，没有额外 I/O。
- 2026-09-01：Canvas 手型模式只改变画布平移与结构编辑能力，不关闭节点单击选中和双击打开详情；节点拖动、连线和框选仍由选择模式独占。为什么这样处理：查看节点内容与改变图结构是两类能力，手型模式不应阻断查看；对用户的影响是平移画布时仍可直接查看任意节点，同时不会误拖节点或创建连线。性能上只调整 XYFlow 事件开关，没有新增状态或 I/O。
- 2026-09-01：Canvas 图片修改采用“稳定节点 + 不可变 Asset + 候选版本 + 批次验收”：生成成功只登记候选，不立即覆盖 `adoptedAssetId`；批量任务默认全部完成后整体采用，部分采用必须明确提示混合版本。旧版、候选和历史 Run 引用均保留，正式采用后只标记数据关系下游待更新，不自动触发付费任务。为什么这样处理：逐节点自动采用会在部分成功时制造无提示的新旧混合正式状态；对用户的影响是可以先对比、补齐和整批验收，并随时回退。性能上不复制图片文件，只新增有界批次元数据和 O(n) 采用事务。
- 2026-09-01：Canvas 四类节点统一使用结构化 `activityState` 表达活动状态：`queued` 使用慢速流动虚线，在用户点击生成后立即反馈已提交；`running` 使用更快的流动虚线；`waiting-approval` 保持静态强调轮廓；候选待验收使用“有新版本”标记。卡片不得从中文状态文案反推活动态；动画层不改变几何、不接管指针，并遵循减少动态效果。为什么这样处理：排队虽未实际消耗生成资源，但也是用户已触发且正在推进的状态，完全静止会被理解为点击无效；不同速度和状态文字仍能区分排队与真实运行。对用户的影响是点击生成后立即看到反馈，缩放画布后也能判断进度阶段。性能上只为活动节点挂载 CSS 驱动的 SVG 轮廓，不增加定时器、轮询或 I/O。
- 2026-09-01：Canvas 节点详情改为 surface 上层的屏幕空间浮窗，按 Agent/生图/文档/桌面 WebView/手机 WebView 提供固定首次尺寸，不随 zoom 变小；用户尺寸按节点保存在会话 view。新节点布局由 Host 按可信锚点、真实尺寸和关系计算紧凑槽位，Agent 不负责普通绝对坐标；旧节点不自动移动，只通过显式“整理选中/可见/整图”操作重排，运行或待审批节点默认跳过。为什么这样处理：节点内覆盖层会被缩放和边界压小，最右追加会制造超长单行；对用户的影响是详情首次可读，10～20 个产物无需极端缩放即可查看。性能上 view 状态不写图，批量布局使用有界空间索引并原子提交位置。
- 2026-09-01：Canvas 图片候选整批采用使用 `image-candidate-adoption-<operationId>.json` intent，在同一 Canvas serializer 与 workspace write lease 内按 `prepared -> modules-committing -> graph-committed -> batch-committed` 幂等向前恢复；模块提交以旧/新 Asset 和 config revision 精确证明，图提交以 `baseRevision + 1` 与稳定图 SHA-256 证明，耐久性无法证明统一阻断为 `CANVAS_IMAGE_BATCH_RECOVERY_REQUIRED`。Job 固化 `candidateBatchId` 定位批次；正式采用只给 `reference/depends-on/derives` 直接下游写结构化 `upstreamChange`，排除 `association` 且不自动运行任务。为什么这样处理：逐文件写无法提供物理原子事务，持久化意图与可证明事实才能避免崩溃后的半批状态；对用户的影响是整批采用可在重启后继续完成且不会重复推进版本，下游只提示待更新。性能上采用过程为 O(n) 校验和有界小 JSON 写入，不复制图片、不新增付费调用。
- 2026-09-01：Canvas Workspace 必须先把 Agent、图片 Job 与审批事实按 `nodeId` 聚合为四态活动映射，卡片只做 O(1) 消费；`canvas_run_nodes` 的候选批次摘要只返回有界 `entries[nodeId,status]` 和计数，不暴露 Asset ID。Host 新建节点使用真实矩形做紧凑避让，整理布局只提交一次原子位置批次。只有固化 `candidateBatchId` 的新图片 Job 才进入候选终态登记和恢复；旧 Job 无该字段时静默沿用原采用链路，避免启动时反复报告不存在的 Canvas 或批次。为什么这样处理：运行事实、布局事实和候选事实应各自有单一可信边界；对用户的影响是状态反馈准确、批量产物更集中、升级旧数据不产生误报。性能上不增加轮询或原图读取，聚合查询与卡片读取保持常数级，布局和原子保存只处理目标范围。
- 2026-09-01：Canvas 视口手势显式采用 XYFlow 原生合同：两指滑动或鼠标滚轮平移，触摸板捏合及 `Command/Ctrl + 滚轮` 缩放；节点工作台继续用 `nowheel/nopan` 隔离内部滚动。为什么这样处理：浏览器无法稳定区分普通滚轮与触摸板两指事件，启发式判断会造成跨平台抖动或反向缩放；对用户的影响是画布操作与 Figma/Miro 类工具一致，详情内滚动不带动画布。性能上只切换既有事件配置，不增加监听器、定时器、状态或 I/O。
- 2026-09-01：Agent 右侧原生 Canvas 必须独立订阅当前项目的 Design `cause: job` 事件，并只在事件到达时刷新轻量任务列表；卡片活动态不能依赖旧 `DesignWorkspaceView` 是否挂载。为什么这样处理：直接在 Canvas 生图详情点击生成后，详情关闭会卸载局部模块，旧工作区未挂载时项目任务事实无人同步，导致折叠卡片看不到排队/运行轮廓。对用户的影响是点击生成后卡片持续显示慢速排队虚线，任务运行后切换为快速虚线；性能上无轮询、不读原图或任务详情，仅增加项目级事件订阅和任务变更时的一次轻量列表读取。
- 2026-09-01：Canvas 全量图片风格核对由普通 Agent 的 `canvas_list_nodes` 与 `canvas_inspect_images` 两个只读工具完成：节点按绑定项目、画布、revision 和类型分页，图片只读取节点与模块一致指向的当前正式采用缩略图，每批最多四张、单张 512 KiB、总计 2 MiB；工具结果不暴露路径、URL 或 Asset ID。为什么这样处理：提示词和当前视口截图都不能证明 Agent 看过全部正式图片；对用户的影响是“核对所有图片”可以得到真实视觉报告，但不会修改提示词、生图或覆盖资产，画布变化时会要求重新枚举。性能上只在用户发起核对时读取当前页模块和受控缩略图，超限压缩仅在内存中执行，无后台扫描、临时文件或额外生成费用。
- 2026-09-01：Canvas 图片详情的单任务“重新生成”必须通过候选批次服务定向重试：先持久化 replacement Job ID，再启动任务，禁止直接调用 Job Manager 绕过批次。历史单节点 UUID 批次文件缺失时，只能依据 Job 固化的节点、模块、初始 adopted Asset 与配置 revision 重建候选；Agent 多节点批次缺失继续 fail closed。候选批次与采用事务文件名必须同时加入 TypeScript Host 和 C++ 原生 helper 的精确白名单，覆盖 UUID 单节点批次、UUID 采用事务与 `agent-canvas-<sha256>` 多节点批次，禁止只放行上层合同。为什么这样处理：旧路径会让新图片已落盘但终态无法按 Job ID 登记，而原生白名单遗漏会让候选批次始终无法持久化，最终卡在 `terminalState: pending` 并导致详情加载失败；对用户的影响是重试结果可正常进入候选验收，已有悬空单节点输出在重启恢复时直接收口且不会再次付费生成。性能上只在重试或异常恢复时增加一次有界 JSON 读写，正常加载、Agent 对话和 WebView 不增加扫描。
- 2026-09-02：Design Job 的磁盘 journal 可以保留 `maskAnnotationId`、`placementState`、`terminalState`、`replacedByJobId`、`retryState`、`deletionState` 等主进程恢复字段，但任何返回 Renderer 的公开任务副本必须在 `clonePublicDesignJob()` 统一剥离这些字段，再进入 Preload 与 Renderer exact-key 解析。为什么这样处理：重试会给旧任务写入 `replacedByJobId`，若原样跨 IPC 会让整份图片模块快照被严格解析拒绝；对用户的影响是重新生成后的节点详情仍可展示图片、提示词和版本历史，同时崩溃恢复证据不丢失。性能上只增加常数级字段删除，不新增 I/O、扫描或图片读取。
- 2026-09-02：官方 `upstream` 已拉取到 `v0.19.16`（`9415c521`）；相对共同祖先 `v0.19.1`，Bone 分支有 490 个独有提交，官方新增 38 个提交。三方合并实际产生 23 个内容冲突，集中在 Agent 编排/队列代际、IPC、Renderer 入口、Electron 版本/打包与 `bun.lock`；必须保留 Bone 的 Canvas、LAN、工作区安全边界并逐项移植官方 Pi 0.84.4、异步 I/O、Vault/Markdown 和终端修复，禁止用单侧策略自动覆盖。为什么这样处理：官方仓库不包含 Bone 扩展，直接覆盖会删除约 168 个 Canvas/LAN/DataRoot 相关文件；对用户的影响是本次只完成拉取和冲突分析，未污染当前 26 个未提交文件，后续合并需在提交或暂存本地改动后进行。性能上分析阶段无业务运行时影响。
- 2026-09-02：macOS 原生 Agent Island 展开态采用单层视觉结构：外壳只绘制一道静态轮廓，计划区仅负责内容排版，不再嵌套圆角卡片；NSPanel 只有从隐藏变为可见时才调用 `orderFrontRegardless()`，持续 Agent 快照只更新内容。为什么这样处理：双描边与内外圆角会让顶部状态看成两个重叠面板，运行流的周期快照重复置顶还会触发透明窗口重合成；对用户的影响是执行状态层级更清楚且不再周期性闪边，Agent、待办和 IPC 语义不变。性能上减少半透明合成与无效 WindowServer 置顶操作，不新增状态、定时器或 I/O。
- 2026-09-02：原生 Agent Island 仅保留收起/展开这一层过渡，移除 live/recent/planning 内容树的插入移除动画，并要求 NSPanel 只有高度实际变化时才启动尺寸动画。为什么这样处理：流式快照会持续重建内容，双重 transition 或重复 frame 动画会叠出旧层并产生闪烁；对用户的影响是顶部“正在执行”始终为单层稳定面板，展开交互仍保留。性能上避免重复 SwiftUI/AppKit 合成和动画重启，不增加轮询或 I/O。
- 2026-09-02：Canvas WebView 详情的预览、HTML 与版本面板均为绝对定位内容层，不能用原生 `hidden` 属性与 Tailwind `flex`/`block` utility 同时控制显隐；必须按活动模式显式切换 `flex|block` 与 `hidden`，并保留 `aria-hidden` 只承担可访问性语义。为什么这样处理：utility 的 `display` 声明可能覆盖浏览器 `[hidden] { display: none; }`，使非活动 Tab 继续在同一位置绘制；对用户的影响是三个 Tab 内容严格互斥，不再出现预览与 HTML/版本叠加，保存、版本和 iframe 沙箱语义不变。性能上非活动层不参与布局和绘制，不新增状态、I/O 或 iframe 重载。
- 2026-09-02：Windows 数据根启动探测与恢复目录校验必须统一使用 `lstatSync` 的 no-follow 语义；此前启动探测使用 `statSync` 跟随 junction/symlink，把链接误判为可用目录，随后安全原子写拒绝链接父路径而迟到崩溃。现在链接数据根在写入前判为 unavailable 并进入恢复窗口，恢复选择也只接受真实目录。为什么这样处理：不能为兼容 junction 放宽安全原子写边界；对用户的影响是应用不再因该场景直接打不开，也不会自动跟随链接或改写链接后的数据，用户需在恢复页选择链接实际指向的真实目录。性能上只替换一次同步文件元数据检查，不增加目录扫描、轮询或额外 I/O。
- 2026-09-02：Canvas Agent 节点运行时可使用严格固定到自身 `projectId + canvasId + nodeId` 的受控 `canvas_*` 工具，SEND 对账快照中的直接入边节点作为本轮权威输入引用；Canvas Agent 不暴露 `canvas_manage`，不能创建、关联、切换或访问其它画布，普通 Agent 的画布关联与工具路径保持不变。为什么这样处理：`canvas-production` Skill 只能编排已有能力，不能突破 Host 工具白名单；原只读白名单使 Agent 节点无法合法创建用户要求的脚本、图片配置、文档或 WebView 下游产物。对用户的影响是可在 Agent 节点内直接读取上游内容并生成当前画布下游节点，不再需要回主会话复制任务，同时仍保持跨画布和会话隔离。性能上复用 SEND 已有对账文档并做一次直接入边过滤，不新增轮询、全量磁盘扫描或额外 I/O。
- 2026-09-03：原生 Canvas 的 Fit View 显式使用 `includeHiddenNodes: true`，结合节点投影已有的稳定宽高，把尚未测量的离屏新节点纳入视口边界，同时保留 `onlyRenderVisibleElements`。为什么这样处理：XYFlow 默认只适配已测量节点，离屏裁剪会让新节点永远无法进入首次 Fit View；对用户的影响是一键适配可以可靠显示 Agent 新建的全部下游产物。性能上继续只挂载可见节点，不增加全量 DOM、轮询或 I/O。
- 2026-09-03：Canvas 后续演进采用“合同驱动的人机共创”：普通 Agent 依据用户目标与领域 Skill 自主生成 `CreativeTaskContract`，Canvas Agent 只用于需要长期状态、独立上下文或反复迭代的分支，单次媒体生成继续使用隐藏执行 Agent；节点业务关系与类型化输入绑定分离，卡片和下游始终读取正式采用版本，候选只显示“有新版本”，采用后才传播待更新。网上下载的普通专业 Skill 由无工具、无副作用的 Skill-to-Canvas Adapter 按内容 hash 生成能力画像，与内置 `canvas-production` 组合，不要求第三方作者适配 Proma，也不能扩大权限、注册任意执行代码或绕过审批。为什么这样处理：仅靠 Skill 文案无法证明参考媒体、版本和执行条件真实成立，而把场景流程写死又限制生态；对用户的影响是可用任意专业 Skill 动态搭建可靠画布，并在缺少视频等执行器时得到明确降级而非伪完成。性能上 Skill 仅按需分析并缓存，图校验保持 O(nodes + edges)，候选不触发下游传播。
- 2026-09-03：Canvas 执行绑定复用 `CanvasEdge.sourcePort/targetPort` 表达 Artifact 输出能力与目标输入槽；旧通用端口只保留为待确认关系，不进入图片执行或采用传播。图片任务只消费正式采用素材，Job journal 保存 Asset ID 与绑定端口，真实路径仅在运行时经 Asset Service 解析；候选不传播，候选或历史版本正式采用后才沿已绑定边标记下游。为什么这样处理：视觉关系、真实媒体和正式版本必须由同一 Host 合同证明；对用户的影响是 WebView 不再冒充参考图，旧画布可继续打开并逐边确认，卡片展示与下游实际消费保持一致。性能上绑定判定保持 O(nodes + edges)，单次任务最多解析 4 张真实媒体，不新增轮询或数据库。
- 2026-09-03：原生 Canvas 的待确认关系菜单必须绑定打开时的画布身份；文档切换后立即丢弃旧菜单，菜单渲染边界还要复核源、目标端点仍存在，Host/模型层继续保留严格端点校验。为什么这样处理：Renderer 局部状态可能跨画布复用，陈旧边若进入新文档的关系推导会抛 `CANVAS_EDGE_ENDPOINT_MISSING` 并卸载 React 根树；对用户的影响是打开关系菜单后切换画布或会话不再整窗黑屏，切回原画布也不会复活旧菜单。性能上只在菜单可见时对内存节点做两次存在性检查，不新增 I/O、保存或任务执行。
- 2026-09-03：普通 Agent 的原生 Canvas 能力新增 `canvas_import_image` 与 `canvas_create_agent`：前者只在用户显式要求时读取当前会话已授权目录中的单张图片，经稳定文件身份校验和素材事务导入为 revision 0 的正式采用图片节点；后者复用 Canvas 批处理事务创建具有独立内部会话的 Agent 节点，并可同时建立类型化来源关系。流程重建必须先创建并验证替代节点与关系，再显式删除旧节点。为什么这样处理：Skill 只能编排 Host 已提供的能力，原工具缺口会迫使 Agent 用 WebView 冒充参考图或把建节点工作推给用户；对用户的影响是可直接在对话中把真实参考图导入画布并分派专业 Agent，下游消费与卡片展示指向同一正式版本。性能上不做后台目录扫描，只在显式导入时读取、校验和解码目标图片；Agent 节点继续使用既有有界事务与 O(n) 图校验。
- 2026-09-03：项目画布管理入口归属每个 Canvas Pane 内默认收起的覆盖式抽屉；顶部加号菜单只在最后保留“打开画布”入口，智能选择最近、默认、首个关联画布或稳定 launcher，不再维护画布列表、重命名、归档或删除。`SidePanel` 继续作为 registry、固定错误提示、归档回退和删除确认的唯一所有者，标题原地编辑、删除当前画布与展开操作由 `CanvasWorkspaceAdapter` 组合。零可见关联使用稳定 launcher，但必须等待当前 `projectId + sessionId` 的 binding 成功就绪后才能展示、清洗 split 或发起依赖默认状态的创建/关联操作；所有异步导航显式携带发起 Pane，归档返回后还要复核该 Pane 仍显示原画布。为什么这样处理：用户需要一个不依赖现有 Canvas 标签的稳定入口，但画布管理必须跟随当前 Pane 和当前画布上下文；把管理能力放在全局顶部菜单会造成入口重复和分屏作用域混乱，未就绪状态或迟到结果则会误删恢复标签、错误设置默认画布或覆盖用户刚完成的切换。对用户的影响是可从顶部随时进入画布，再在画布内直接切换、新建、归档、恢复和删除；加载过程不会闪现错误入口，窄 Pane 抽屉最多占 80% 且不会遮挡另一 Pane，legacy Design 仍禁止永久删除。性能上入口选择与状态复核只读取内存，抽屉关闭时不挂载，不新增 IPC、持久化字段、轮询或后台 I/O。
- 2026-09-04：普通 Agent 的 Canvas binding 只表示权限与上下文关联，不再直接生成右侧可见标签；Canvas 打开标签由 Renderer 运行期内存状态独立管理，默认和重启后均隐藏，只有“添加 -> 打开画布”、画布内切换/新建或通知点击等明确用户导航才登记并打开。后台 Agent 创建 Canvas 产物只记录未读与待定位节点，不自动展开右栏或抢焦点；归档、删除、解除关联与会话清理同步移除失效打开项。为什么这样处理：关联事实可能长期存在，而 UI 标签是用户当前工作意图，两者耦合会让旧画布在每次进入会话时自动出现。对用户的影响是普通对话界面保持简洁，需要画布时仍可从添加菜单一次进入，Agent 后台生产不会打断当前操作。性能上只增加每会话一个小型内存标签列表和常数级过滤，不新增持久化、IPC 或 I/O。
- 2026-09-04：合并官方最新正式版 `v0.19.26`，Electron 版本更新为 `0.19.26-bone.1`，共享包跟随官方更新为 `0.1.69`；接入官方单一 delegation 观察 Tab、未查看完成态、Vault、Browser、MCP 与文件面板改进，同时保留 Bone Canvas、LAN、数据根和内部会话隔离。为什么这样处理：fork 的版本基线必须与已合入的官方标签一致，冲突解决以官方新能力与 Bone 安全边界并存为目标；对用户的影响是获得官方新功能和修复，既有画布与移动端工作流不回退。性能上未增加轮询或后台扫描，新增状态仍沿用事件驱动。
- 2026-09-04：delegation 与 Canvas 必须严格互斥：Canvas 内部会话、Canvas 引用发送以及畸形完成事件不得进入协作委派或思考强度修改路径；`set_delegation_thinking_level` 与其它 delegation 工具都要复用同一可见父会话和 owner 边界。为什么这样处理：协作子会话和 Canvas 内部执行是两套不同生命周期，混用会泄漏内部会话或产生错误完成态；对用户的影响是画布任务不会被误显示为协作 Agent，协作状态也不会污染画布执行。性能上仅增加常数级身份校验。
- 2026-09-04：文件路径展示只通过轻量 canonical-path / batch-exists IPC 获取规范路径与存在性，不读取文件内容或创建媒体 token；managed session 必须按 owner 校验，批量检查每 16 项让出事件循环。Renderer reload 恢复 deferred 消息时必须保留正文、附件、Canvas 引用、引用选区和文件引用块，`GET_QUEUED_MESSAGES` 先执行可见会话校验。为什么这样处理：Tooltip 不应为显示路径读取最高 100 MiB 文件，重载也不能丢失待发送上下文；对用户的影响是大文件悬停更快、更安全，刷新后排队消息保持完整。性能上路径检查为轻量元数据 I/O，并通过分片避免阻塞主进程事件循环。
- 2026-09-04：PowerShell 工具审批返回后必须重新校验当前运行代次与权限模式，禁止复用审批前快照继续执行；重复权限分支保持删除。为什么这样处理：审批等待期间会话可能停止、重启或切换权限，旧快照不再代表当前授权；对用户的影响是 Windows 命令不会在过期授权上下文中误执行。性能上每次审批后仅增加一次内存态复核。
- 2026-09-04：右侧 Canvas 全屏不能只提高 `fixed` 画布子节点的 `z-index`，还必须同步提升 `SidePanel` 与 AppShell 右栏宿主的祖先层叠上下文；展开状态由画布根节点的稳定 data 属性向上声明，宿主使用 `:has()` 只在展开期间升层。为什么这样处理：子节点无法越过祖先 stacking context，应用栏位边界线和拖拽手柄会穿透全屏画布；对用户的影响是展开后只显示画布内容，还原后普通侧栏边界与交互保持不变。性能上仅增加 CSS 关系匹配，不引入 React 状态同步、重挂载、轮询或 I/O。
- 2026-09-04：Canvas 展开态整条导航栏统一位于系统标题栏安全区之后：macOS 预留 `40px` 并保留原生 traffic lights，Windows 复用 `32px` 自绘标题栏高度，展开宿主层级低于 `WindowControls`，Linux 保持系统标题栏布局；普通右侧 Pane 不变。为什么这样处理：标题、管理入口和画布动作属于同一导航层，拆分左右位置会破坏层级，而统一安全区可同时保留窗口拖拽与系统按钮可用性。对用户的影响是 macOS 实机展开时导航不再遮挡窗口按钮或出现穿透线，还原后无额外顶部空白；Windows 由同一平台合同保证控制按钮可见。性能上只有常数级平台判断和一个仅在展开态存在的拖拽区 DOM，不新增状态、监听器或 I/O。
- 2026-09-04：Canvas 整理布局必须复用 Renderer 卡片投影的当前尺寸索引，图片按已采用素材比例计算动态高度，WebView 按当前设备预设计算宽高；可移动节点、范围外固定障碍和分层列宽必须读取同一几何事实，缺失时才回退类型默认尺寸，视觉尺寸不写入共享 Canvas 文档。为什么这样处理：Canvas 文档只负责业务身份与位置，动态预览尺寸属于 Renderer 视觉事实；对用户的影响是重新排版会根据当前卡片真实大小保持 24px 净间距，不再出现竖图、横图或 WebView 卡片相互叠加。性能上只在用户触发整理时基于最新快照建立一次 O(n) 内存 Map，整理仍为一次有界内存计算和一次原子 `move-nodes` 保存，不新增 IPC、磁盘字段、DOM 测量或监听器。
- 2026-09-04：Proma 后续服务器运维能力采用独立运维模块，不作为 Canvas 节点或 Agent 会话字段的附属能力；服务器资产、连接、权限和审计由 Bone 自有边界维护，只通过薄接入点进入应用，并以降低后续官方版本合并成本为首要架构约束。为什么这样处理：运维连接是跨项目、长生命周期且高权限的基础设施能力，绑定 Canvas 或官方高频变化的数据结构会扩大冲突面并混淆权限归属；对用户的影响是官方升级时运维数据与连接能力保持稳定，后续仍可按需提供项目过滤视图或 Canvas 只读引用。性能上核心模块按需加载，连接与日志流应隔离运行且采用事件驱动，不增加常驻轮询。
- 2026-09-04：独立运维模块首版只面向 Linux + SSH，不在同一阶段加入 Windows WinRM/PowerShell 远程协议或 Kubernetes API。为什么这样处理：先用单一成熟连接协议建立资产、认证、终端和权限闭环，避免多套连接语义同时扩大安全与测试面；对用户的影响是首版覆盖常见云服务器、Docker 主机和 Web 服务，Windows 与 Kubernetes 管理留作后续独立适配。性能上无需常驻多种协议运行时，连接资源更容易按会话精确释放。
- 2026-09-04：Linux 运维模块首版采用“SSH 工作台 + Agent 辅助”的人工主导模式，并提供覆盖单机故障处理闭环的综合控制台；Agent 负责分析与生成操作建议，高风险命令必须经过明确审批，不把首版扩张为长期监控平台或自动部署系统。为什么这样处理：连接、凭据、权限、终端和审计是后续监控与部署的共同基础，而先建立人工可观察、可接管的闭环能控制远程执行风险；对用户的影响是可在一个工作台完成主机查看、问题分析和受控处理，同时不会因后台自动操作失去控制。性能上状态按需刷新、日志按流读取、连接按主机复用并在空闲后释放，避免全量常驻采集。
- 2026-09-04：开发版与正式版共享 `~/.proma/agent-canvas-bindings.json` 时，Canvas binding 的公开读取必须每次采用磁盘最新权威快照，不能把首次读取结果作为进程生命周期缓存；关联摘要应从项目 Canvas registry 读取公开元数据，允许 `legacy-design` 提供标题，但被引用节点的文档加载仍必须通过原生 Canvas Store。普通 Agent 发送开始时应立即从 composer 清除本次精确 Canvas 引用，不能等待覆盖整轮执行生命周期的发送 Promise 完成；主进程拒绝时再幂等补回引用，并按消息对象身份撤销本次乐观消息、保留正文/附件且显示可见错误。为什么这样处理：多个客户端可在同一业务配置上更新关联，长期读缓存会把合法节点误判为“引用已失效”；混合关联若对 legacy 错用 `requireNative()`，也会连带阻断合法原生节点；发送 Promise 等待整轮完成会让运行期间的下一条消息重复携带旧引用，而静默失败与未回滚会让用户认为发送按钮无响应并产生重复假消息。对用户的影响是跨客户端新增或混合关联后可正常发送，引用在发送接管后立即离开输入区，失败时原因可见且输入不丢失；性能上每次 binding 公开读增加一次小型 JSON 快照读取，composer 状态只增加一次有界内存数组过滤，摘要沿用项目 registry 的有界元数据读取，不扫描目录、不读取素材。
- 2026-09-04：Canvas 节点详情工作台按节点保存屏幕像素级相对偏移，位置由节点实时屏幕投影派生，平移、缩放和拖动节点时跟随节点移动但自身尺寸不随 zoom 缩放；图片候选批次只保留为主进程内部一致性与崩溃恢复事务，Canvas `LOAD`、节点卡片和详情 UI 不再读取或展示候选摘要，用户统一在历史版本中显式“设为默认”，成功后才更新卡片正式版本并传播下游待更新。为什么这样处理：详情位置应属于节点关系而不是视口绝对坐标，内部事务也不应暴露为第二套版本操作模型。对用户的影响是移动画布时详情不会悬停在原地，图片预览、卡片展示和下游实际采用版本始终一致，历史版本可直接预览后设为默认。性能上每个会话仅保留最多 64 个节点偏移，位置换算为常数级内存计算，并移除 Canvas `LOAD` 的候选摘要磁盘扫描及卡片候选 Map 构建，不新增轮询、媒体读取或持久化字段。
- 2026-09-04：用户将独立运维模块界面确定为 Proma 现有 Agent 工作流的最右侧工作区：左侧继续使用现有 Agent 会话导航，中间继续使用现有 Agent 对话与输入区，右侧可展开运维工作台；不增加第三种“运维模式”，也不另造运维 Agent 助手。服务器切换必须复用 Canvas Pane 的导航方式：标题栏左侧图标打开当前 Pane 内的全高左侧抽屉，当前项高亮，行尾收纳管理动作，选择后自动关闭，遮罩或 `Escape` 可退出。高保真 HTML 原型位于 `/Users/xutaoyu/.codex/visualizations/2026/09/04/01a06b48-7a40-7470-b627-652273badfb6/server-ops-console/index.html`。为什么这样处理：用户的核心工作流仍是与 Agent 协作，服务器状态和 SSH 控制属于当前会话可引用的工具上下文；对用户的影响是无需在 Agent 与运维助手之间切换，并能用与画布一致的肌肉记忆管理服务器，当前 Agent 可直接结合右栏主机、终端、服务、日志、文件、Docker 与审计继续工作。性能上右栏应按需挂载、按页签订阅数据、对指标节流并对日志流设背压，关闭右栏后释放连接视图和非必要渲染。
- 2026-09-04：运维工作台需要包含独立“数据服务”页，首批界面覆盖 PostgreSQL、MySQL 与 Redis；数据访问通过当前服务器的 SSH 隧道建立，控制台默认只读，数据库 DDL/DML、终止连接、复制拓扑变更以及 Redis `FLUSHALL`、`FLUSHDB`、`DEBUG` 和大范围键删除必须审批且禁止自动重放。为什么这样处理：数据库与 Redis 的连接、指标和危险命令语义不同于 systemd/Docker，不能复用无边界的通用 Shell 权限；对用户的影响是可在同一故障排查流程查看连接、容量、慢查询、复制、Keyspace 和 Slowlog，同时避免误操作直接破坏业务数据。性能上各数据服务适配器按需连接、按页签采样，关闭页签或右栏后释放隧道和指标订阅。
- 2026-09-04：Canvas 节点详情的逐帧跟随由 `NativeCanvasGraph` 内的瞬时几何 Store 驱动，只发布当前 viewport 与发生变化的节点位置；Workspace、Jotai 和 Canvas 文档不接收每帧写入。viewport reducer 必须额外保留最近一次权威文档 viewport，相同 viewport 的普通 graph revision 只同步节点几何，不能误记为手势中的远端视口更新。历史图片设为默认的事务完成后，主进程必须在同一写 lease 内读取权威 Canvas revision，并在释放 lease 后依次广播 graph 与 image-module 变化。为什么这样处理：工作台必须与 XYFlow 的即时画面共享同一坐标事实，采用图片也同时改变节点和下游状态；若把普通 graph 更新误当作 viewport 更新，采用图片时并发平移会在手势结束后回弹。对用户的影响是平移、缩放、拖动节点时详情连续跟随且尺寸不变，采用历史版本后卡片、详情和下游待更新状态同步刷新，同时不会覆盖用户正在进行的视口手势。性能上每帧只复制一个小型 viewport 对象或当前变化节点的坐标，权威基线仅增加常数级数值比较，不触发全 Workspace 渲染、磁盘写入或 IPC。
- 2026-09-04：Agent 发起的 Canvas 多节点候选批次 ID 必须直接生成为 `agent-canvas-<sha256>`，与 TypeScript Host 和 C++ stable-directory helper 的原生写入白名单保持一致；哈希输入仍使用 `candidate-batch` 域分隔区分单个图片 Job。为什么这样处理：候选 Store 会再添加 `image-candidate-batch-` 文件前缀，生成端额外加入 `batch` 会使合法任务在付费执行前被原生合同确定性拒绝。对用户的影响是普通 Agent 批量生图可正常建立候选批次，不再显示“Canvas intent 原生写入合同无效”；性能上哈希计算、原子写入和恢复成本不变，只消除失败回滚。
- 2026-09-04：服务器运维首个正式纵切已注册为 Agent 右侧可关闭的 `server-ops` 工作区组件，主机资产通过独立四层 IPC 保存到 `~/.proma/server-ops/hosts.json`，Store 使用 `safe-file` 原子写和 schema 恢复，Renderer 只持有 Jotai 投影与最后选择的主机 ID。主机合同只允许 Linux SSH 地址、端口、用户名、认证方式和标签，拒绝密码、私钥路径与未知字段；当前版本提供真实主机 CRUD、Canvas 风格服务器抽屉、真实 SSH 终端以及概览/服务/日志/文件/Docker/数据服务/审计入口，尚未接入在线指标和非终端远程数据。为什么这样处理：服务器稳定身份、持久化和主窗口授权是 SSH、审计与数据服务的共同安全前提，先完成这一闭环可避免凭据和连接生命周期反向污染 UI 与会话模型。对用户的影响是现在可在任意 Agent 右栏维护并切换同一份服务器清单并登录终端，PostgreSQL、MySQL、Redis 已有固定入口但会明确等待后续 SSH 隧道；性能上只在运维工作区挂载时读取一次小型 JSON，连接进程按需启动，不新增轮询或数据库。
- 2026-09-04：普通 Agent 的 Canvas 工作区状态按会话持久化已打开的具体画布标签与最后正在查看的画布，客户端重启后仍恢复；用户切到其他右侧标签或关闭右栏时不得被 Canvas 抢焦点，恢复前必须以实时 metadata 与 binding 过滤已删除、归档、解绑或跨项目失效的画布。launcher、分屏、详情框和缩放不进入该持久化状态，会话终态同步清理，历史状态最多保留最近 50 个普通 Agent 会话。为什么这样处理：Canvas 权限关联与用户当前界面意图是两类事实，只保存 binding 会让旧画布自动出现，只保存内存标签又会在重启后丢失工作上下文。对用户的影响是再次进入对话时回到上次实际查看的画布，同时手动切换到文件、Skills 等标签后不会被迟到恢复覆盖。性能上仅在打开、关闭和切换时读写少量本地字符串并做有界内存过滤，不新增 IPC、轮询、目录扫描或媒体读取。
- 2026-09-04：独立运维模块的 SSH 连接、Host Key 校验与远程 PTY 固定运行在独立 Electron utility process；首次未知指纹必须在认证前阻断并由用户确认后重新连接，指纹变化直接阻断。密码默认只保留在主进程内存，用户选择记住时才使用 Electron `safeStorage`；Linux `basic_text` backend 必须 fail closed。主机公开配置只保存非敏感 `credentialRef`，私钥路径和 passphrase 不进入 `hosts.json`，旧 `keyPath` 数据需连同备份原子脱敏迁移。`ssh2` 打包固定排除 `cpu-features`、`nan` 与包内可选 `.node`，统一使用跨平台 JavaScript fallback。为什么这样处理：远程运维是高权限边界，必须在认证前建立服务器身份、在 Renderer 外隔离秘密与协议运行时，并避免可选原生模块造成跨平台打包漂移；对用户的影响是真实支持密码、私钥和 SSH Agent 登录，首次连接可核对指纹，服务器身份变化时不会静默发送凭据。性能上 utility process 按首次连接启动，终端输出以 16ms 合批、1 MiB 上限和 ACK 背压控制内存，空闲时不轮询服务器。
- 2026-09-04：`v0.19.26-bone.1` 是合入官方 `v0.19.26` 后的首个 Bone 发布边界，发布说明统一记录本轮画布按需打开与恢复、节点详情跟随、历史版本采用、真实尺寸排版、跨平台标题栏修复，以及首版 Linux SSH 运维工作台。为什么这样处理：正式标签必须对应一个可复核的功能闭集，避免把画布修复和新运维运行时以无说明的混合快照交付；对用户的影响是安装包版本、下载页说明和应用内 Bone 更新历史指向同一份变化事实。性能上发布元数据不增加运行时开销。
- 2026-09-05：服务器密码、私钥路径和私钥口令统一在“添加/编辑服务器”弹窗中配置并默认安全保存；公开主机资产仍只保存 `credentialRef`，秘密进入 `~/.proma/server-ops/credentials.json` 的 Electron `safeStorage` 密文。已有凭据不解密回填，用户可显式替换或清除；正常连接直接使用已保存凭据，只有凭据缺失或认证失效时才打开补录弹窗。Linux 检测到 `basic_text` 等不安全后端时继续 fail closed，拒绝保存秘密。为什么这样处理：服务器身份与登录方式是同一配置流程，而运行时反复询问会割裂连接体验；同时必须保持公开资产可审计且不含秘密。对用户的影响是服务器保存后可直接登录，凭据异常时仍有明确恢复入口，不会在配置文件中暴露明文。性能上仅在保存或连接时读取一条有界密文记录，不增加轮询、常驻进程或远程请求。
- 2026-09-05：Server Ops Agent 的 connect、exec、disconnect 使用唯一 `~/.proma/server-ops/audit.json` schema v1 Store 写 start/result 审计，固定保留最近 5000 条；命令在完整字符串上经过一次携带 source span、quote removal 值和可靠性标记的 shell WORD 词法扫描，再过度脱敏秘密键/flag、Bearer、`-H`/`--header` 的完整参数与 `Authorization:` token、URI userinfo、普通环境变量值、不透明 quoted/JSON payload、复杂 heredoc、重定向和私钥路径。同一命令行的多个 heredoc 按声明顺序逐段脱敏，无法可靠识别 WORD 或任一 heredoc 边界时保守隐藏整词或剩余正文，最后才截断 512 字符并由 Store 计算不可伪造的 `commandTruncated`。结果语义固定拆分为 `outcome`、`exitCode`、`signal` 与稳定 `errorCode`，错误码只允许明确维护的内部 allowlist，未知 `error.code` 和全部 `Error.message` 统一降级；非零退出码和 signal 都记录为失败，连接/断开成功不伪装成错误码。公开记录禁止凭据、连接标识、Host Key candidate、stdout/stderr，并在 main IPC 与 preload 两个生产边界严格解析返回 DTO。审计页按 owner、请求代次和 `hostFilter + operationFilter + selectedHostId` 完整身份接纳结果，刷新推进代次，当前主机缺失时同步进入 ready 空态。主审计文件损坏或版本未知时必须保留现场并阻断 Agent 远程动作，即使备份有效也不自动覆盖。为什么这样处理：审计是高权限远程操作的前置安全条件，原始文本正则、Authorization header、供应商错误码和迟到异步结果都可能泄漏秘密或误导用户，自动恢复、秘密落盘或审计失败后继续执行也会让实际操作与证据链分离。对用户的影响是 Agent 操作都有准确且不会被旧请求覆盖的开始和结果记录，可分别看到成功/失败、退出码、signal、稳定错误码和截断提示；没有当前服务器时立即显示空态，结果审计写入失败仍保留真实远程结果并返回明确 warning。性能上每次 Agent 远程动作增加两次有界 JSON 原子提交，脱敏保持单条命令 O(n) 扫描，请求门禁仅增加常数级内存比较，审计页仍只在进入、筛选或手动刷新时读取，不新增轮询。
- 2026-09-05：Canvas 生图工作台的任务创建、素材采用与配置保存共用 `state.error`，主操作区必须独立于 `saveState` 始终展示该错误；旧格式图片连线继续保留为“衍生 · 待确认”，不得自动升级为图片参考。为什么这样处理：任务前置校验失败时配置仍可能处于 `saved`，若只在保存失败时渲染错误，用户会误以为“生成图片”无法点击；自动确认旧连线则会未经用户确认把上游图片作为付费生成输入。对用户的影响是点击生成被校验拒绝后会原位看到“请先确认连线用途”的明确提示，确认或断开连线后再生成；性能上仅增加一次常数级状态派生与条件渲染，不新增 IPC、磁盘访问或模型调用。
- 2026-09-05：Server Ops Agent 授权与 SSH 连接生命周期使用精确 `sessionId + hostId` 和 connection generation 管理 pending/active 状态；常驻 AppShell 在会话切换时撤销旧授权，utility 双向消息经 exact-key parser 校验，非预期 SSH 断线或 runtime 退出也在共享 IPC 服务边界撤权。命令在 Facade 与 Connection Service 两层拒绝 NUL；审计脱敏覆盖 `curl -u`、`sshpass -p/-f`、`redis-cli -a` 等粘连短选项，远程失败与结果审计失败同时发生时保留原始稳定错误及 cause，并追加公开 warning。为什么这样处理：Agent 远程能力必须绑定当前用户意图与当前真实连接，不能因迟到连接、会话切换、异常断线或审计故障遗留可复用授权，也不能让 CLI 紧凑写法绕过凭据脱敏。对用户的影响是 Agent 可调用登录、状态、命令和断开等常用运维方法，但授权会随上下文和连接失效自动回收，审计异常也会明确可见且不掩盖真实远程结果。性能上只增加常数级内存代次比较、严格消息解析和异常路径撤权，不新增轮询、常驻连接或磁盘扫描。
- 2026-09-05：Server Ops 按“真实概览 -> systemd 服务 -> 实时日志”形成纵向排障闭环，三个领域继续复用唯一 SSH connection generation。概览执行无用户插值的版本化固定脚本，以 250ms 窗口采样 CPU/网络，只在页签可见时立即读取并每 10 秒刷新；systemd 只接受严格 `.service` unit 与五种固定动作，每次动作先确认、按连接和 unit 单飞，并写入由 v1 原子迁移的审计 v2（区分 `agent/user`）；日志使用独立 exec stream、单 owner 单流、ACK 背压和 2 MiB/5000 行双上限，远程筛选或切换主机必须确认旧流停止后才启动新流，切页、断线或关闭右栏立即失效并释放；Renderer 在 render 阶段按 `hostId` fail closed，主机切换首帧不得展示上一主机正文或筛选。Renderer 只消费结构化 IPC，页面日志不自动进入模型上下文；布局使用右侧 Pane 自身的原生 CSS 容器查询，在窄 Pane 降密度并堆叠服务详情。本阶段不扩展文件、Docker 或数据服务。为什么这样处理：SSH 已连接后的核心需求是可验证的观测、受控处置与有界日志，先把结构化数据、审计和流生命周期做成稳定底座，后续 Docker、数据库与 Redis 才能安全复用；运维展示还必须与当前服务器身份同步，不能把短暂旧投影误当成当前事实。对用户的影响是可从异常指标进入服务和日志完成真实排障，停止失败时不会悄悄产生第二个远端日志进程，切换服务器时也不会闪现上一台的日志，窄宽面板保持可读。性能上只有当前可见页面按需工作，概览同主机请求单飞，日志按 ACK 限制在途数据，展示门禁只增加一次常量级身份比较，不新增跨主机轮询、历史数据库或后台常驻采集。
- 2026-09-05：Windows 原位升级提示通过 electron-builder `nsis.include` 的 `customPageAfterChangeDir` 实现，复用其已按当前用户/全体用户安装模式设置的 `SHELL_CONTEXT` 读取 `InstallLocation` 与 `DisplayVersion`；无旧路径时跳过摘要页，缺版本时显示“未知版本”。旧版卸载、新版安装、`--updated` 数据保留和快捷方式保留继续由 electron-builder 负责，不允许 `/NCRC` 绕过完整性校验。为什么这样处理：提示界面必须和实际升级使用同一注册表作用域，同时避免维护完整自定义 NSIS 主脚本。对用户的影响是安装前能看到将升级的旧版本、旧位置和本次目标位置，首次安装流程不增加步骤，`~/.proma` 数据不变。性能上只在安装向导中增加两次注册表读取和一个静态页面，不增加应用运行时开销、后台进程或网络请求。
- 2026-09-05：Windows stable-directory helper 打开 Canvas 根目录时按内容模式拆分最小权限：读取和列表只申请只读目录权限，写入、移动与删除标记仅追加根目录 flush 所需的 `FILE_ADD_FILE`，不再提前申请未使用的 `FILE_ADD_SUBDIRECTORY` 与 `FILE_DELETE_CHILD`；下层 `nodes/trash/revisions/entry` 仍通过 NT 相对句柄申请并校验真实操作权限。为什么这样处理：原实现把所有内容请求都放大为根级创建和删除权限，受限 ACL、企业安全策略或同步目录会在授权前直接以 `cannot open root` 失败，而这些权限并不是相对操作的前置条件。对用户的影响是 Windows 项目中的现有 Canvas 与新 Canvas 可以在更常见的目录权限配置下正常读取和写入，同时 reparse 防护、原子提交和恢复合同不变。性能上不增加 I/O、进程或扫描，只减少不必要的 ACL 权限检查。
- 2026-09-05：普通 Agent 的全局系统提示不再把视觉任务分流到独立 “Design 面板”；用户明确要求画布，或任务明显需要多个可关联、可持续迭代的产物时，当前 Agent 必须加载 `canvas-production` 并直接使用 `canvas_*` 工具协同右侧画布，只有画布产物与代码实现的差异会实质影响结果且意图不足时才澄清。为什么这样处理：独立 Design 面板已经被普通 Agent 右侧画布取代，旧提示会与当前 Skill 冲突并让 Agent 拒绝执行。对用户的影响是无需切换不存在的面板或复制任务，当前对话可以直接创建、编排和按需运行画布节点；性能上只改变静态提示文本，不增加 IPC、磁盘访问、模型调用或常驻状态。
- 2026-09-05：普通 Agent 与 Canvas Agent 统一通过专用 `canvas_update_image_config` 局部修改生图节点的提示词、模型、画幅、尺寸和上下文；Host 从权威画布解析 `imageModuleId`，同时校验 graph revision 与 config revision，未指定字段保留当前值，配置保存与 `canvas_run_nodes` 付费运行继续分离。为什么这样处理：通用 `canvas_update_artifact` 只暴露正文，原实现会原样回写旧图片配置，导致 Agent 能看到节点却无法完成 `1:1 -> 3:4` 等明确操作；扩大 `canvas_apply_changes` 会混淆图结构和图片配置的所有权边界。对用户的影响是普通 Agent 可以直接调整图片节点完整生成配置，不再要求用户手动打开设置面板，并且并发编辑不会被静默覆盖；性能上每次配置修改只读取一次小型模块配置，不新增媒体授权、轮询或生成调用。
- 2026-09-05：Canvas 工作流执行固定由普通 Agent 作为唯一 Orchestrator，通过 `canvas_run_agent` 启动单个专业 Agent，或通过 `canvas_run_workflow` 运行指定起点的可达下游；工作流只沿共享解析器确认的 `bound` 可执行边扩展，`association`、未解析或不兼容边不触发执行。普通 Agent 可操作全部受支持节点的类型化业务参数，但不能修改 session、素材路径、版本指针、任务 ID 或权限字段。Canvas Agent 的长期职责、Skills 和模型保存在独立 `agent-configs/<nodeId>/config.json`，正式输出只以内部消息 UUID、SHA-256 与完成时间指针进入画布；headless 子 Agent 禁止递归工作流和生图，手动工作台保留既有能力。为什么这样处理：现有能力能搭图和单独生图，却不能从父对话可靠启动 Agent、传递真实正式输出或执行有界 DAG；独立配置根避免与内容资源目录碰撞，运行模式隔离避免递归扩权和嵌套费用。对用户的影响是明确执行后可自动推进策划、文案、分镜、文档、WebView 和待验收图片链路，图片仍需在历史版本中设为默认后才继续下游；展示性、待确认或不兼容连线不会触发 Agent 或付费图片任务。性能上图预检为 `O(nodes + edges)`，并限制 32 节点、8 个 Agent、深度 8、Agent 并发 2、图片 16 张和总时限 15 分钟，不增加连线监听、轮询或后台自动运行。
- 2026-09-06：Canvas 原工作流续跑统一使用单一 `CanvasWorkflowRun` JSON journal，固定 owner、operationId、节点/边与正式输入输出指纹、子 operation/batch/task 及次数和时长预算；单文件 revision CAS 负责状态提交，长执行使用可崩溃回收的跨进程租约。resume 只由用户明确触发，先对账原 Agent 或图片任务，再推进正式输入仍有效的未完成节点；布局与 viewport 变化不失效。cancel 保留已生成批次和完成产物，迟到采用或执行回调不能复活运行。历史由 Store 游标分页，终态 archive 由外层独立治理，不再用 512 条硬上限阻断执行。为什么这样处理：进程内状态无法证明付费任务是否已提交，自动 adoption 续跑也会越过用户决策。对用户的影响是应用重启后可安全继续原 DAG，重复 resume 或多进程竞争只会有一个推进者，且不会因历史增长或纯布局编辑丢失运行；分页扫描只在用户查询历史时发生，节点推进仍为单 run 原子写。
- 2026-09-06：Agent Island 的远期计划提醒必须把等待时间限制在 Node `setTimeout` 的 `2_147_483_647ms` 单次上限，并在每段等待结束后重新计算下一阈值；禁止把超长延迟直接交给 Node。为什么这样处理：Node 会把溢出的延迟降为约 1ms，远期待办会形成高频调度循环并拖慢 Electron。对用户的影响是数十天后的待办仍会在正确时间进入提醒窗口，客户端不会因远期事项持续占用 CPU；性能上始终只保留一个定时器，每约 24.8 天最多进行一次常数级重算，不增加轮询、磁盘访问或后台进程。
- 2026-09-06：Canvas 图片工作流把“任务已进入 running”和“候选批次达到终态”拆成两阶段合同：启动确认后立即交出有界批次与任务所有权，终态通过 Job 与候选批次事件等待，并按节点返回脱敏结果；父运行取消或到期时只取消该批次仍活跃的 owned 任务，候选保存与日志故障不得反转已提交事实。为什么这样处理：等待完整生图后才返回任务身份会留下无法精确取消的付费窗口，聚合批次状态也会误判混合成功与失败分支。对用户的影响是长时间生图可被工作流准确停止，成功候选仍停在等待采用，失败只阻断对应下游；性能上不新增轮询或第二套任务管理器，只增加有界事件监听、单次期限定时器和最多 32 条逐节点摘要。
- 2026-09-06：Design/Canvas 生图继续使用双模型职责：Chat 模型只负责理解需求和整理精确提示词，图片执行器与模型始终来自任务创建时固化的节点 `imageModelSnapshot`；`mcp__nano_banana__generate_image` 只是兼容工具名，任务详情必须展示真实图片模型。`auto` 上下文模式已有主进程固化的 Canvas 直接输入时可通过图片调用准入，不再强制重复读取项目文本，显式 `project` 模式保持原规则。为什么这样处理：参考图已经在节点创建阶段经过权威解析，再要求受限 Design Agent 使用 Read 会在图片 API 前误阻断，并被旧工具名误导为模型串线。对用户的影响是带正式参考图的 GPT Image 2 节点可以进入真实图片执行，日志也能准确区分提示词模型与生图模型；性能上仅增加常数级布尔准入和展示映射，不新增文件读取、模型调用或媒体复制。
- 2026-09-06：`v0.19.26-bone.2` 作为官方 `v0.19.26` 基线上的第二个 Bone 发布边界，集中交付 Server Ops 真实观测与受控操作、Canvas Agent 有界工作流、图片批次取消与候选验收、Windows 原位升级提示和跨平台稳定目录修复。为什么这样处理：这些能力已经形成可独立验证的用户工作流，需要用单一版本、标签和发布说明锁定一致交付边界；对用户的影响是安装包可明确升级到包含本轮运维与画布自动编排能力的版本。性能上发布元数据本身无运行时开销，各功能继续沿用可见时刷新、事件驱动、ACK 背压和有界并发。
- 2026-09-06：Windows 自定义升级摘要页的变量声明和页面函数都必须受 `!ifndef BUILD_UNINSTALLER` 保护；electron-builder 会先编译不注册该页面的卸载器，未保护的函数触发 NSIS warning 6010，进一步只保护函数仍会让顶层变量触发 warning 6001，两者都会终止发布。`v0.19.26-bone.2` 与 `v0.19.26-bone.3` 保留为无 Release 的失败标签，完整修复随 `v0.19.26-bone.4` 发布。为什么这样处理：公开标签不应强制改写，安装页全部符号的所有权必须与实际引用阶段一致；对用户的影响是 Windows 安装包可正常生成，首次安装和升级摘要内容不变。性能上只影响构建期预处理，不增加应用运行时开销。
- 2026-09-06：Design 内部生图运行必须显式禁用 Workspace Skills，`replace` 工具白名单既传入 Pi 原生 `tools` 以收窄模型可见 schema，也继续在 Proma `canUseTool` 执行边界拒绝越权；普通 Agent 与 `extend` 模式保持完整工具集合。Chat LLM 只负责理解需求和形成精确提示词，实际图片执行模型始终取自任务固化的 `imageModelSnapshot`，历史兼容工具名不得被当作模型来源。为什么这样处理：只在执行阶段拒绝会让模型先尝试不可用 Skill、Read 或其它工具，产生误导性失败并浪费上下文；对用户的影响是新 Design 任务只会选择当前允许的上下文工具和真实生图执行器，任务详情与执行日志继续显示实际图片模型，普通 Agent 自有 Skills 与工具能力不受影响。性能上减少单次 Design 请求的工具 schema 与无效调用，不新增 I/O、轮询、媒体复制或模型调用。
- 2026-09-06：合并官方最新正式版 `v0.19.31`，Electron 版本更新为 `0.19.31-bone.1`，Pi SDK 升级到 `0.85.0`；接入官方 Slack Bridge、GPT-6 Astra、Gemini 思考深度、折叠侧栏、文件浏览与运行时打包修复，同时保留 Bone Canvas、Server Ops、LAN Bridge、共享业务数据根和内部会话隔离。MCP 删除必须先通过 workspace slug 写守卫，配置使用原子写；desktop/headless 的 `run_completed` 复用 `run_started` 的权威 `startedAt/runGeneration`。为什么这样处理：官方能力与 Bone 高权限边界必须取并集，避免升级后功能回退或让删除、迟到运行事件绕过当前工作区和运行代次。对用户的影响是获得官方新功能和修复，既有画布、远程运维与移动端流程保持可用，MCP 删除和运行完成状态更可靠。性能上新增 Slack/Pi 依赖会扩大安装包和按需加载成本；MCP 写入每次增加一次小型索引查询及原子 rename/fsync，不增加常驻轮询、后台扫描或数据库。
- 2026-09-06：`v0.19.31-bone.1` 是官方 `v0.19.31` 基线上的首个 Bone 正式发布边界，发布说明统一覆盖 Slack Bridge、GPT-6 Astra、Gemini 思考深度、折叠侧栏、文件与浏览器改进，以及 Bone 的 Design 工具隔离、MCP 写守卫、运行代次一致性和 EventKit 干净 workspace 构建修复。为什么这样处理：标签、应用版本、安装包和应用内更新历史必须引用同一份可审计说明，避免用户看到版本升级却无法确认官方能力与 Bone 扩展是否同时保留。对用户的影响是可从 GitHub Release 和应用更新入口获得同一套跨平台安装包与变更说明。性能上发布元数据本身不增加运行时开销。
- 2026-09-06：`v0.19.31-bone.1` 发布流水线在四个平台统一失败，原因是 `sync:runtime-deps` 清理了 Bun configVersion 1 安装在 Electron workspace 内的 `electron-rebuild` 可执行链接；旧锁文件将工具提升到根目录，曾偶然掩盖这一顺序错误。保留 `.1` 失败标签，不移动公开 Git 引用；`0.19.31-bone.2` 改为先重建 `node-pty`、再同步已重建的运行时依赖，并由发布合同和 fork 兼容测试锁定顺序。为什么这样处理：native rebuild 的输入是完整开发依赖，runtime 同步的输出才是精简打包目录，顺序必须符合数据流而不能依赖 hoist 布局。对用户的影响是 `.2` 可恢复 Windows、macOS 与 Linux 安装包发布，应用功能不变。性能上步骤数量不变，仅调整构建期顺序，不增加运行时资源开销。
- 2026-09-06：`v0.19.31-bone.2` 在资源准备成功后仍因裸 `npx`/`bunx electron-builder` 于 workspace devDependencies 被清理后下载 `26.15.3` 而失败，新版 schema 拒绝现有 25.x 配置；保留 `.2` 失败标签，`0.19.31-bone.3` 将依赖和所有本地、可视化及 CI 打包入口统一固定到 `electron-builder@25.1.8`。为什么这样处理：打包器属于可复现发布的构建输入，不能依赖 hoist 状态或注册表最新版本。对用户的影响是恢复 macOS、Windows 和 Linux 安装包发布，不改变客户端功能。性能上只可能增加 CI 首次下载固定工具的构建时间，不增加应用运行时资源开销。
- 2026-09-06：`v0.19.31-bone.3` 已验证 Linux 与 macOS 的固定 Electron Builder 打包成功，但 Windows 在 `package:prepare` 清理 workspace 开发依赖后才运行 stable-directory 测试，因被测模块无法解析 `electron` 而失败；保留 `.3` 失败标签，`0.19.31-bone.4` 将正式发布和手动 Windows 工作流的原生回归前移到资源准备之前。为什么这样处理：测试需要完整开发依赖，而最终 helper 仍由随后相同的构建脚本重新生成，测试不应依赖精简运行时目录。对用户的影响是 Windows 回归可以真实执行并继续生成安装包，客户端行为不变。性能上只是调整 CI 步骤顺序，不增加构建步骤或运行时开销。
- 2026-09-06：`v0.19.31-bone.4` 在 Linux 使用 Bun `1.4.2` 安装 `node-pty` 时，因缺少受管 `node-gyp` 而临时执行 `/tmp/bunx-node-gyp@latest`，其 `undici` 缓存不完整导致安装失败；保留 `.4` 失败标签，`0.19.31-bone.5` 在根包固定 Bun `1.3.14` 与 `node-gyp@12.4.0`，正式 Release 和手动 Windows 工作流同步使用该 Bun 版本。为什么这样处理：原生模块工具链也是发布输入，不能依赖包管理器最新版和临时 bunx 缓存。对用户的影响是跨平台依赖安装更可复现，不改变应用功能。性能上只增加一个已在锁文件中的构建期工具直连，不进入安装包运行时依赖。
- 2026-09-06：`v0.19.31-bone.5` 已通过 Release 工作流 `34026863285` 完成正式发布，Linux、Windows、macOS arm64/x64 四个构建任务和 Release 汇总任务全部成功，共上传 15 个安装、增量更新与更新元数据资产；macOS 因仓库未配置证书继续发布带明确解除隔离说明的未签名包。为什么这样处理：只有跨平台产物、更新元数据和 Release 正文全部由同一标签成功汇总，版本才形成可供客户端发现和用户下载的完整发布事实。对用户的影响是应用内更新和 GitHub 下载页现在可获得 `0.19.31-bone.5`，并修复前四个失败标签暴露的构建工具链问题。性能上没有新增客户端运行时开销，代价仅为 CI 固定工具版本和构建期依赖安装。
- 2026-09-06：Windows `v0.19.31-bone.5` 启动时若自定义数据根不可用，`ipc.ts` 顶层构造 Agent-Canvas 关联 Store 会在 bootstrap 的 recovery gate 前调用 `getConfigDir()`，导致主进程直接报“数据根不可用”且恢复窗口无法出现；关联 Store 改为在 `registerIpcHandlers()` 入口懒创建并保持进程级唯一，启动隔离测试锁定模块导入不得构造业务 Store。为什么这样处理：所有依赖活动数据根的业务对象必须晚于 normal gate 初始化，不能让静态模块求值绕过恢复流程；对用户的影响是磁盘离线或路径失效时可进入原有数据根恢复界面，正常画布关联行为不变。性能上只延后一次 Store 构造，不新增 I/O、常驻对象或后台任务。
- 2026-09-06：`0.19.31-bone.6` Windows x64 安装包从 `feed23c4` 隔离 worktree 仅叠加数据根启动修复和版本发布文件后生成，避免主工作区并发中的 Canvas/Agent Runtime 未提交改动混入；macOS 跨平台打包首次下载 Wine 超时时，可用独立 `ELECTRON_BUILDER_CACHE` 与已验证的 Electron Builder binaries 镜像完成工具获取。为什么这样处理：打包输入必须在整个构建期间稳定且可审计，网络缓存故障不能通过混用半下载文件恢复；对用户的影响是本次 Windows 包只包含明确修复，安装器版本和更新元数据一致。性能代价仅是隔离工作区首次安装依赖和重新构建，不增加客户端运行时开销。
- 2026-09-06：用户第二次反馈 Windows “数据根不可用”截图后，生成代码行号完成旧包指纹确认：`bone.5` 的 `main.cjs:716993` 是顶层 `new AgentCanvasBindingStore()`，与截图完全一致；解包本地 `bone.6` 安装器后，同一行已是无关类定义，唯一 Store 构造位于懒 getter，内置 package 版本为 `0.19.31-bone.6`。为什么这样处理：相同错误文本不能证明新修复失败，必须核对实际安装的 `app.asar` 而非源码或安装器文件名；对用户的影响是后续先用安装器 SHA-256 和安装后 `app.asar` SHA-256 排除旧安装目录、旧快捷方式或传输错包，再决定是否继续改代码。性能上仅增加发布验收的离线哈希检查，不影响客户端运行。
- 2026-09-06：macOS 跨平台构建 Windows 安装包时，Bun 默认只安装宿主平台的 Sharp optional dependencies；Windows 打包必须在宿主构建和 `node-pty` 重建完成后执行 `bun install --frozen-lockfile --os=win32 --cpu=x64`，再由 `sync-runtime-deps.ts` 以 `win32-x64` 合同强制确认 `@img/sharp-win32-x64` 已复制，最终还要在 `app.asar`/`app.asar.unpacked` 验收 `.node` 与 libvips DLL。为什么这样处理：optional dependency 缺失不会让 Electron Builder 失败，却会生成安装后主进程启动即崩溃的假成功产物；对用户的影响是 `0.19.31-bone.7` Windows 包可正常加载 Sharp，今后同类漏包会在构建期直接失败。性能上只增加 Windows 打包期一次目标依赖解析与常数级合同检查，不增加客户端运行时开销。
- 2026-09-06：Canvas 任务详情、取消和重试统一经过纯主进程 `CanvasTaskOperationService`，外层必须先完成窗口授权、权威节点解析并持有 Canvas 串行锁；服务再用 `projectId + canvasId + nodeId + imageModuleId + jobId` 和目标索引复核归属。详情尝试与 trace 都按游标最多返回 50 条，trace 以固定小块读取且完整 JSON 响应不超过 64 KiB；取消区分本地 stop 请求与未确认的供应商取消。候选重试复用批次 locked 路线和 Job 原 journal 的 replacement 事实，无批次旧任务复用 Manager retry/run；同旧 attempt 重放只返回或补完原 replacement，再次重试必须引用新的 attempt。为什么这样处理：任务操作必须共享现有付费、快照和候选事务边界，不能另建易漂移的任务管理器或仅靠进程内 operation map 去重。对用户的影响是 Agent 与 UI 可安全查看失败原因、停止任务并按原模型和引用版本重试，响应丢失不会重复创建付费任务。性能上首次目标查询后为索引读取，日志内存受页和字节预算限制，不新增全项目扫描、数据库或轮询。
- 2026-09-06：Canvas Agent 创建、重建、内容生命周期、批操作、图片候选批次与采用事务的无未决副作用终态会从 active `transactions` 幂等迁移到 `transaction-archive/<文件名 SHA-256 前两位>/<原文件名>`；每条先原子写归档并精确读回正文，再由 stable-directory helper 对已打开 active 文件复核正文与稳定身份后删除，任一步失败都保留 active 供重试。恢复按 operation/session 派生的精确别名读取，正文必须匹配项目、Canvas 与事务身份；单次最多整理 64 条、4 MiB，旧 active 扫描上界提高到 4096，但新增事务容量仍保持 512。为什么这样处理：恢复区只应保存可能继续推进的事务，已完成历史不能长期占满写容量，同时归档不能通过全目录扫描、路径重开或读后替换窗口误删新事务。对用户的影响是长期使用 Canvas 不会因累计终态事务达到 512 条而阻断新操作，崩溃后仍能精确重放原结果；性能上整理随 LOAD 按固定预算增量执行，分片精确读取为常数次 I/O，不新增后台扫描、数据库或常驻任务。
- 2026-09-06：Canvas 产物导出固定绑定显式图片 `jobId` 或文档/WebView `revision`，从权威 Job、Asset、节点和不可变正文版本重建身份，导出历史版本不切换当前采用状态；项目目标仅接受当前授权 Agent cwd/active worktree 内的相对路径，保存窗口仅允许本轮交互式 Renderer `WebContents` 打开，headless、Automation、Delegation 和排队恢复不得借用焦点窗口。外部文件写入统一经过 stable-directory helper：授权前打开目标父目录稳定句柄，文本使用同目录临时文件与原子 rename，图片按 64 KiB 分块复制并核对打开源文件的大小、身份和 SHA-256，默认 no-replace，显式 `overwrite: true` 才允许覆盖。为什么这样处理：普通路径复验后再写仍存在目录替换窗口，最近 100 条版本列表也不能证明老图片版本；对用户的影响是可以导出精确历史产物且不会误采用、误覆盖或让后台任务弹出保存窗口，多窗口保存窗口归属当前发起页。性能上文本上限 256 KiB、图片上限 64 MiB，复制保持 O(1) 内存且不新增数据库、全量版本扫描或后台轮询。
- 2026-09-06：Canvas 图片活动首屏复用 Design Job 的项目、Canvas 与完整图片目标索引，只返回每节点最新任务、全部 queued/running 任务和最近成功任务的去重窄摘要；首次查询自动扫盘，窗口重获焦点使用 `resync: true`，项目变化或注册器释放通过 `invalidateCanvasImageActivity(projectId?)` 使缓存失效，正常任务状态更新只增量维护内存索引。为什么这样处理：每次任务进度事件都扫描数千条历史 journal 会让长生命周期项目持续卡顿，而只保留 latest 又会在新任务失败后隐藏旧成功候选。对用户的影响是大项目中的图片节点状态可即时更新，失败后仍能检查最近成功结果，跨进程新增、更新和删除会在聚焦重同步后准确反映。性能测试锁定 5000 条历史与 12 条活动任务只在首次读取 5012 个 journal，后续进程内取消与查询不增加目录扫描或历史文件读取。
- 2026-09-06：Canvas 终态事务归档属于图提交后的整理步骤；批操作、Agent 创建/重建和内容生命周期在归档失败时必须保留权威文档及已形成的 publication，由 IPC 在 workspace lease 释放后先广播已提交 revision，再向调用方返回整理错误。为什么这样处理：归档失败不能反转已经提交的图事实，也不能让其它窗口因异常路径漏刷新；对用户的影响是恢复过程即使遇到磁盘归档故障，已恢复节点和内容仍会立即同步，后续操作继续明确失败并可重试。性能上只复用已有异常信封和返回字段，不增加扫描、写入或常驻任务。
- 2026-09-07：`canvas_read.capabilities` 必须由当前实际装配的工具名与 `permissionCeiling` 共同派生，能力仅用于发现，Host 继续逐调用执法；普通 Agent 可发现已装配的 15 个任务、版本、导出、回收、重建与工作流操作，手动 Canvas Agent 只保留固定画布内的任务控制、版本、导出和回收操作，父编排 Canvas Agent 仅保留任务状态、版本列表与历史正文读取。异步 operation handler 返回模型前必须再次校验取消信号、项目授权和 Canvas 绑定，默认 `canvas-production` Skill 同步升级到 `1.0.10`。为什么这样处理：Agent 不应被告知不存在、当前模式不可用或已撤权的操作；对用户的影响是原对话能准确发现任务、版本、导出、恢复和续跑能力，父子 Agent 不会越过编排边界。性能上每节点仅对小型 `Set` 做常数级匹配，响应前复核复用现有内存与授权检查，不新增 I/O。
- 2026-09-07：Canvas Workflow 历史分页保持无索引 JSON journal 合同，查询仍按需完整扫描目录并解析匹配记录，但必须在 owner 与 cursor 过滤后只保留 `limit + 1` 个最新完整 run。为什么这样处理：不改变可移植存储格式和 cursor 语义的前提下，避免历史增长使单次分页常驻全部 run；对用户的影响是分页结果、顺序和续页位置不变，长历史查询的内存上界降为 O(limit)。性能上磁盘读取和解析仍为 O(目录条目数)，没有实现物理终态分片归档或索引加速。
- 2026-09-07：Canvas 产物导出以 `operationId` 对应的同一 transactions receipt 作为跨实例幂等边界，首次执行必须通过 stable-directory helper 锁内 `createOnly` 原子 claim；批量最多 16 项、一次选择目录并按请求顺序返回 `files`。receipt 必须严格校验状态、精确版本、索引、绝对规范路径、SHA-256 和结果结构，prepared/completed 重放只在单次 no-follow fd 上流式核验目标大小与哈希，无法证明提交时返回 `CANVAS_ARTIFACT_EXPORT_COMMIT_UNCERTAIN`，不得再次弹窗或猜测重写；取消与 `CANVAS_ACCESS_DENIED` 必须终止整批。为什么这样处理：进程内 singleflight 无法阻止多实例同时产生保存窗口和文件写入，浅解析与路径重开也会让损坏 receipt 或目录替换污染恢复结果。对用户的影响是重复工具调用只产生一次外部副作用，批量导出可看到每项成功、失败或取消，崩溃和撤权场景安全失败。性能上 active receipt 从最多 4096 项全扫改为 O(1) 精确读取，单次导出仅增加小型 receipt I/O，图片和恢复核验保持 64 KiB 流式内存，且无后台扫描或轮询。
- 2026-09-07：Canvas 图片活动查询必须通过文档 Store 的无副作用 `readSnapshot` 读取当前稳定主文档；合法主文件直接返回，只有 tmp/backup 可用时保留候选并返回 `CANVAS_RECOVERY_REQUIRED`，Canvas 根缺失时只构造内存空文档。为什么这样处理：普通 `load` 会提升恢复候选并消费 tmp，纯读活动查询既不持写锁也不广播 recovery，调用它会在只读项目写盘并让其他窗口漏掉恢复事实。对用户的影响是只读项目仍可查看活动，待恢复画布会保留恢复数据并通过既有错误态提示重新加载。性能上不增加写锁、对账或扫描，仍只读取固定的主/tmp/backup 候选链。
- 2026-09-07：Server Ops 文件能力统一通过单连接 SFTP runtime 提供有界分页、1 MiB 预览与 64 KiB 分块 I/O；Renderer、Agent 和传输分别使用独立 owner，owner 关闭或候选取消会以本地代次拒绝全部迟到结果。写操作只复用 Main 签发的五分钟候选，提交前后复核连接、源文件、审计可写状态与 Agent 授权；保存要求服务端真实声明 `fsync@openssh.com` 和 `posix-rename@openssh.com`，另存与上传 no-clobber 不跟随符号链接，临时文件只允许创建它的 owner 发布。为什么这样处理：SFTP v3 缺少通用路径 CAS 与 `O_NOFOLLOW`，必须通过打开前后 `lstat`、句柄 `fstat`、完整内容 hash 和 owner 生命周期降低路径替换与迟到写入风险；对用户的影响是可浏览、预览、编辑、另存、新建、重命名和删除远程文件，取消、切换或关页后不会继续执行旧操作，能力不足时明确拒绝安全替换。性能上目录最多每页 200 项、每 owner 总计 2000 项，预览仅增加一次路径复核，不做全目录扫描或后台轮询；真实 Electron smoke 只使用临时数据根和 localhost 内存 SSH fixture。
- 2026-09-07：Server Ops 文件 owner 关闭复用现有 SFTP RPC 等待 utility 精确 ACK；utility 先等待资源打开落定、关闭远端 handle/cursor，再以创建或关闭时的 `fstat` 与删除前 `lstat` 复核临时文件身份。路径被替换、关闭失败或连接失联时保留文件并返回清理失败，传输任务记录待检查恢复意图；不把 SFTP 描述为支持 CAS。为什么这样处理：本地失效只阻止继续使用，不能证明远端资源已释放；对用户的影响是取消、关页和退出结果可信，不会误删外部替换的同名文件。性能上只在 owner 收口时执行每个临时文件一次身份复核和删除，无轮询；localhost Electron smoke 使用临时数据根与内存 SSH fixture，不接触真实服务器或用户数据。
- 2026-09-06：普通 Agent 和 Canvas Agent 可复用的入站媒体授权事实统一为 `AgentSendInput.mediaAttachments -> SDKUserMessage.mediaAttachments`；Host 在普通发送、queue-now 和 deferred 入队前核验当前 session/project、权威授权根、普通文件稳定句柄、`image/*`/`audio/*`/`video/*` 类型、100 MiB 单文件与 32 项数量上限，并按内容哈希固化到会话私有 `attachments` 目录。历史重试复用同一持久字段，正文、`@file:` 与 `queuePresentation.attachments` 都不得推导或扩大媒体授权。为什么这样处理：展示快照和 prompt 文本没有可靠身份与所有权，无法支撑 Agent 自动导入图片、音视频或 ComfyUI 工作流持续运行；对用户的影响是图片、音频和视频在发送、排队、重载和重试后仍可被媒体来源工具稳定发现，并且伪造路径、跨项目路径和符号链接会在运行前拒绝。性能上只对显式媒体附件执行单次有界稳定读取与会话内复制，内容哈希路径让二次准备和重试复用已有文件，不增加轮询或数据库。
- 2026-09-06：Canvas 持久工作流的 15 分钟预算必须保存 `remainingDurationMs` 与 `activeStartedAt`，只累计真实执行和外部任务运行时间，等待候选采用不计时且恢复不得刷新预算；已取得 `mediaRunId` 的音视频节点只能 reconcile 原任务，已提交 Agent 输出只能按原 operation、消息 UUID、开始时间和目标节点精确恢复，已有 execution 的幂等重放不再次消耗媒体次数。外部任务短返回时由进程级 coordinator 按 workflow run 去重保留一个 deadline 唤醒，远端终态可提前触发同一恢复。为什么这样处理：仅靠内存计时或重新提交会让重启刷新期限、重复付费，静默远端还可能永久占用 running；对用户的影响是 ComfyUI 长任务可在重启后继续收集，等待验收不会吃掉运行时间，到期后父流程停止推进；远端不支持精确取消时明确保留未确认状态，继续收集原任务，不承诺 GPU 已停止。性能上只增加有界 journal 字段、事件唤醒和每个运行最多一个定时器，不引入工作流轮询。
- 2026-09-07：统一媒体模块通过同一远端任务事实服务普通 Agent、手动画布 Agent 与父工作流；ComfyUI API 图、素材版本和上传回执在提交前固化，未知提交只对账，历史产物作为候选按输出角色显式采用。为什么这样处理：外部生成成功、画布采用和父流程推进有不同所有者，混为一步会造成重复付费或自动覆盖正式版本。对用户的影响是图片、视频和音频能复用素材与生成进度，视频封面可作为旧图片节点的显式输入，普通 Agent 的独立结果可幂等挂接画布。性能上使用共享连接、节流进度和有界收集，不新增数据库；音视频技术探测依赖本机 `ffprobe`，缺失时在生成前阻断，真实 ComfyUI 端点仍须单独联调。
- 2026-09-07：Canvas 取消请求先持久化意图，启动筛选和期限调度都必须恢复尚未收敛的取消。按最新全局媒体合同，连接改名和启停不改变实例代次；地址、认证或远端用户改变时产生新代次，同时保留历史身份供旧任务恢复。为什么这样处理：崩溃窗口不能遗失停止意图，当前配置也不能替换已准备任务的执行身份。对用户的影响是启停只影响新任务，旧任务继续精确对账；性能上增加有界配置历史和常数级身份比较，不增加网络轮询。
- 2026-09-07：父编排中的动态后继只能由 Host 创建事务回执逐节点登记，原工作流 journal 通过只追加节点的 CAS 保存；恢复不得扫描最新可达图扩大范围，子 Agent 登记后父驱动必须重新读取 journal 再合并本次完成事实。登记失败仍须保存确切节点阻断事实并将父流程原子标为 partial，超过节点上限时持久化当前专业分支失败。为什么这样处理：节点创建、计划授权和执行有不同提交时刻，旧内存快照会丢失动态计划或误纳入其它分支，单独返回工具错误也不能阻止父流程误报完成。对用户的影响是专业 Agent 可在原预算内创建后续媒体产物，登记受阻时保留已经创建的节点身份及可恢复的失败事实，不会重复创作或静默跳过后继。性能上校验继续限制在最多 32 个计划节点及有界图遍历，不引入后台全图扫描。
- 2026-09-07：ComfyUI 接受任务与全部输出齐全必须分开记录：HTTP 200 带 promptId 和部分 node_errors 仍保留已提交身份，可信成功 history 缺少声明产物则持久化 collection-failed，已有输出描述符继续严格验证来源和路径；窗口撤权或项目删除后释放对应媒体进度订阅。为什么这样处理：执行接收事实不能被局部校验错误抹去，否则重试可能重复付费，失效订阅也不应在项目恢复后自动重获权限。对用户的影响是重启后继续对账和补收原任务，缺产物保持明确失败，删除项目后不再遗留进度监听。性能上沿用同一任务 journal 与有界监督器，只增加必要的常数级清理和描述符校验。
- 2026-09-07：媒体 worktree 客户端联调时，5174 已由 server-ops-completion 占用；使用独立 Vite 5175、PROMA_DEV_INSTANCE=comfyui-media 和仅本次 Electron 进程的 Chromium host-resolver-rules 将 127.0.0.1:5174 映射到 127.0.0.1:5175，保留既有开发来源校验和其它客户端。真实客户端已验证“设置 -> 媒体生成”及旧生图配置读取，本机 ffprobe 8.1 可用。为什么这样处理：另一个 worktree 的页面不能与本次 preload/main 混用，直接运行常规 dev 脚本还会停止原测试服务。对用户的影响是可并行打开本次测试界面；业务配置仍共享，Bridge 与调度器也不因此隔离，当前 LAN Bridge 端口冲突不作为媒体功能失败。性能代价是增加一个测试客户端和本地 Vite 服务，测试结束后可停止这两个独立进程。
- 2026-09-07：用户最终确认媒体配置使用“媒体模型 / 服务连接 / 公共工作流”三个全局列表，采用独立添加、编辑、保存返回交互，取消逐项目配置授权及全部默认生成来源；只维护启用状态。画布顶部多选 API 模型范围，未配置时跟随全部可执行模型，显式空集合保持空，固定集合不自动加入新模型。公共工作流由任务按真实输入组合与输出需要选择，普通 Agent 和 Canvas Agent 共用合同；素材、会话和任务仍保持项目边界。为什么这样处理：多图、图加音频等组合无法由单个媒体类型默认值表达。对用户的影响是配置一次跨项目复用，并能控制当前画布候选而不干扰旧任务；性能上仅增加有界目录过滤，无全实例轮询。此记录取代本会话早期“默认生成配置”的方案。
- 2026-09-07：ComfyUI 模型、工作节点、用户工作流和 assets 资源按连接代次、远端用户与真实来源独立按需缓存；工作流正文和资产内容只能通过固定 `connectionId + instanceGeneration + remoteUser + 服务端路径/ID` 的描述符有界读取，显示名不参与寻址，UI/API 工作流格式识别也不授予自动执行权限。为什么这样处理：四种接口的可用性、认证和启用状态彼此独立，跨来源共用快照会让单点失败拖垮全部页签，弱文件名身份则会在多用户或实例重建后误读资源。对用户的影响是设置页和 Agent 可共用准确目录，单个页签失败不影响其它来源，旧描述符失效时明确拒绝而不会读到同名文件。性能上缓存最多 16 个来源快照、TTL 60 秒、分页最多 100 条，资源库不触发 `object_info`，正文和媒体只在预览或导入时读取且默认限制 64 MiB。
- 2026-09-07：媒体设置采用“媒体模型 / 服务连接 / 公共工作流”三个平级页签；三类配置均以已保存列表和独立草稿完成添加、编辑、保存、取消、复制与归档，连接不再要求项目授权，公共工作流固定 `projectId: null` 且不自动创建 profile。工作流编辑器只从 API JSON 的真实节点输入生成字段，连线和复杂值只读，媒体控件仅允许官方 Loader 精确输入；JSON 变化会使旧绑定失效并阻止保存，输出必须显式选择节点、key、媒体类型和索引。为什么这样处理：服务与模板是全局复用配置，项目范围属于任务和素材，不应污染连接；表单必须绑定真实 prompt 字面量，避免制造默认值或把私有文件名带入公共模板。对用户的影响是跨项目只配置一次，取消编辑不会污染已保存条目，地址或认证变化后旧资源立即隐藏，旧项目工作流可清洗媒体路径后复制为公共版本。性能上四类资源仅在当前页签按需分页，迟到响应通过请求代次丢弃，不增加后台轮询或全量扫描。
- 2026-09-07：Canvas 图片公共 ComfyUI 工作流使用独立 snapshot 固化工作流版本、连接实例代次和完整媒体输入，旧 profile 路径继续兼容；Renderer、Agent 批量运行与 preload 复用同一严格配置合同，多输出工作流在创建图片 Job 前拒绝。图片表单的未完成多字段输入只保留为本地草稿，外部配置的业务基线真实变化后才重建，完整合法时才提升并保存。为什么这样处理：把不完整输入上推会触发父状态回灌并清空用户刚填写的字段，Agent 只透传 profile 又会让同一节点在手动与自动运行时行为分叉。对用户的影响是填写多个动态字段时内容不再消失，手动生成、普通 Agent 和父工作流都能运行同一公共工作流并保留素材身份；性能上仅对最多 128 个输入执行有界校验和稳定基线序列化，不新增 IPC、网络请求或常驻任务。
- 2026-09-07：用户确认 ComfyUI 节点、模型和工作流目录采用“首次拉取并持久保存，平时读取本地快照，显式同步更新”，取代这三类目录原先 60 秒内存 TTL 的方案；普通 Agent、Canvas Agent 和设置页共用快照。模型首次保存全部目录名称，工作流正文首次查看时保存，每次成功同步目录后按需重读；失败保留旧内容与成功时间，首次失败不缓存为空目录。快照继续按连接实例代次和远端用户隔离，存于 `~/.proma/media/resource-snapshots/`；连接测试只探测轻量状态，真实执行校验和生成进度保持实时，assets 资源库仍为实时分页。为什么这样处理：频繁拉取大目录不能增加有效信息，重启也不应丢失已发现资源；对用户的影响是离线可浏览与编排，只有首次读取或主动同步产生目录网络请求。远端 `http://100.124.186.117:8189/` 的只读快照及断网重启验证证据在媒体 worktree 的 `.omx/qa/2026-09-07-comfy-snapshot-report.json`，测试未提交生成任务。
- 2026-09-07：用户要求媒体配置布局先以 HTML 交互原型确认，再修改客户端；原型入口为 `.omx/prototypes/media-settings/media-settings.html`，方案见 `.omx/plans/2026-09-07-media-settings-global-list-plan.md`。原型采用生成配置、服务连接、公共工作流三个列表，展示全局默认、独立编辑页、默认停用替换和节点/模型详情；所有数据只在页面内存中，连接检测为模拟，不访问真实服务或业务配置。为什么这样处理：先确认实际点击路径，避免仅凭文字规划再次形成不符合用户预期的页面。对用户的影响是可以离线预览并反馈交互；性能代价仅为约 100 KiB 的自包含 HTML，不新增依赖或后台服务。已通过脚本语法和离线资源结构检查；浏览器工具拒绝本地 file URL，尚未完成浏览器实测与截图验收。
- 2026-09-07：用户要求 ComfyUI 连接编辑页下方直接展示模型、工作节点、工作流、资源库四个页签。官方上游核对发现资产库已存在但属于需显式启用、仍在演进的可选能力；用户保存工作流可从 UserData 读取，UI 工作流与可执行 API 图仍须区分，多用户身份也与 API 认证不同。详细官方源码引用及方案补充保存在 `.omx/plans/2026-09-07-media-settings-global-list-plan.md`。为什么这样处理：四个目录的接口支持范围不同，列出远端资源也不代表现有输入编译器能直接消费。对用户的影响是连接编辑与资源检查可在同页完成，各页签明确显示可用、部分结果或不支持，远端素材经引用/导入适配后供画布与 Agent 共用；性能上采用按页签懒加载、分页和按实例身份缓存，不整库下载或全连接轮询。本轮只完成可行性分析，未修改 HTML 或客户端。
- 2026-09-07：媒体配置 HTML 原型已在 ComfyUI 连接编辑页落地模型、工作节点、工作流、资源库四个页签；连接标题与详情入口统一进入编辑页，切换资源页签保留表单，资源搜索与连接保存分属不同表单边界。增加演示工作流预览/导入、资源库可用/有限记录/接口未开放/认证失败状态，以及内嵌品牌图片、两秒 H.264 视频和 WAV 提示音预览。为什么这样处理：用户可以在一个页面核对连接及其资源，搜索回车不会误保存，修改地址或凭据后也不会复用旧检测结果。对用户的影响是刷新原 HTML 即可体验新交互，全部动作仍仅作用于演示数据；原型约 226 KB，无外部资源请求或后台服务。已通过合并脚本语法、离线打包和媒体格式检查，浏览器实测仍受本地文件访问限制。
- 2026-09-07：用户进一步纠正媒体配置方向：取消图片/视频/音频的全局默认及默认替换流程，“生成配置”改为“媒体模型”，只展示独立 API 模型，ComfyUI 工作流不再包装成模型预设。公共工作流由 Agent 结合素材角色、数量、输入组合、输出目标和实例资源选择，提交前仍须确定性核验节点映射、上传回执和版本；普通 Agent 与画布共用合同。即梦现有 CLI 不冒充 API，原型中的第三方 API 条目标记待配置。为什么这样处理：单一媒体默认无法覆盖多图、图加音频等任务，混合目录会让配置看似可用却无法执行。对用户的影响是原 HTML 已更新为媒体模型、服务连接、公共工作流三个独立列表，保留 ComfyUI 四个资源页签，工作流可查看输入组合；最新方案取代此前默认规则。仅原型与方案更新，约 217 KB，脚本、离线资源及内存逻辑检查通过，浏览器布局与真实服务尚未验证；没有新增依赖、网络请求或客户端运行开销。
- 2026-09-07：用户确认画布顶部采用多选“本画布可用模型范围”，全局媒体配置只维护启用状态，不设默认。HTML 已增加画布入口和按渠道分组、搜索、媒体筛选的多选弹层；首次取全部已启用完整 API 模型，局部勾选转为明确 ID 集合，全部取消保持为空，失效选择可移除。为什么这样处理：启用决定全局候选资格，画布选择决定当前任务范围，二者不能相互覆盖；Agent 后续应取有效目录与画布范围交集，ComfyUI 工作流仍按任务独立匹配。对用户的影响是可以像普通 Agent 选择模型一样在画布导航调整多个候选，切换设置不丢选择，也不改派已准备或运行中的任务。当前仅原型和方案更新，约 236 KB，脚本、离线包及多选边界逻辑检查通过，未验证浏览器布局与真实服务；选择变更不重建媒体预览，不增加轮询或依赖。
- 2026-09-07：用户明确公共工作流的“编辑节点”应从 API 工作流 JSON 中选择真实节点及其 inputs，不能先创建脱离执行图的抽象输入。HTML 已增加节点检索、字段勾选、控件类型/名称/必填/数值约束/排序，并通过 nodeId、class_type、inputKey 和原始类型绑定到画布动态填写框；JSON 改变后检查映射失效，节点连线和复杂输入只读。为什么这样处理：同一份明确映射才能同时支撑用户表单与 Agent 填参，避免表单可填写却无法落到执行节点。对用户的影响是可保存并进入画布查看所选字段，预览精确回填的 JSON；模板版本与任务输入隔离，媒体只生成待上传清单和 null 占位，不伪造远端素材标识。本轮仅原型和方案更新，10 项核心测试及脚本集成检查通过，浏览器布局和真实 ComfyUI 调用未验证；解析和字段数量有界，映射校验直接查询目标字段，本地素材替换或更换工作流时释放对象 URL，不新增依赖或后台轮询。
- 2026-09-07：用户以“可以了”确认当前媒体配置 HTML 原型交互，作为后续客户端实现基准，包括三个配置列表、取消全局默认、画布多选可用 API 模型，以及按 API JSON 真实节点 inputs 绑定画布控件。此确认表示交互方案接受，客户端落地与真实 ComfyUI 联调仍待实施和验收。
- 2026-09-09：Canvas 节点运行卡顿根因是媒体进度约每 500ms 触发 `NativeCanvasGraph` 全图投影与深层三方比较；现对未变化的节点按 ID 局部更新运行态字段，并在仅 viewport/无关父状态变化时跳过节点投影，结构、配置和选区变化仍走完整投影。为什么这样处理：运行态展示不应让 1000 节点画布承担 O(n) 对象创建和比较；对用户的影响是媒体节点运行时交互线程负担显著降低，拖动、缩放、选区和边同步语义不变。性能上只增加节点 ID 索引与受影响节点集合，运行事件通常为 O(k)，结构变更仍为 O(n)；72 项 Canvas 定向测试和全仓类型检查通过。
