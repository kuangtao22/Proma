## 更新内容

- 建立 `<官方版本>-bone.<构建号>` 版本规则，让 Bone 修改与官方基线可以清晰区分。
- 正式版使用 `Proma`，开发版使用独立的 `Proma Dev` 身份；两者隔离 Electron 数据，同时共享 `~/.proma` 业务配置。
- 自动更新固定使用 Bone 发布源，并支持 Bone 预发布 SemVer 的后续升级。
- 统一 macOS、Windows 与 Linux 安装包命名，发布前自动校验标签、应用版本和 Release 标题。
