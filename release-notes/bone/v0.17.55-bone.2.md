## 更新内容

- 修复自定义兼容渠道创建 Agent 会话后标题不自动更新的问题，标题请求现在与当前 Agent 统一使用 Pi 的模型、渠道、认证、协议和代理配置。
- 标题生成使用独立会话，不加载工具且不主动启用推理；短消息直接本地命名，长消息最多返回 50 个 Token，降低额外开销。
- 渠道、凭据、网络或模型响应异常时自动使用首条用户消息作为标题，避免会话长期停留在“新 Agent 会话”。
