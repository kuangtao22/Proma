## 更新内容

- 修复 Windows 数据目录迁移在计划创建后，因退出流程继续写入源目录而被误判为源数据变化的问题。
- 对尚未开始复制且没有断点数据的迁移安全刷新最终容量基线，覆盖旧版本已记录为失败的同类计划。
- 已有复制进度或断点文件时继续执行严格一致性校验，避免在源数据真实变化后错误续传。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.17.55-bone.4-macos-*.dmg
open ~/Downloads/Proma-0.17.55-bone.4-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
