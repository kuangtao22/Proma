## 更新内容

- 同步官方 Proma v0.19.16，保留 Bone 已有功能与本地数据兼容边界。
- 完善 Agent 右侧画布工作区，支持多类型产物、节点引用、版本候选与批量操作。
- 增强 Canvas 图片工作流，支持图片候选批次、比例与上下文传递，以及任务状态和媒体生命周期管理。
- 增加项目数据路径管理与可恢复迁移，强化工作区锁、原子写、断点恢复和完整性校验。
- 完善局域网移动端可信设备、扫码配对、设备撤销、心跳与会话状态同步。
- 增加可配置图片生成模型，并加强渠道配置、凭据解析和远程图片下载安全。
- 修复 Agent 停止、迁移、清理和异步回调中的多类竞态问题。
- 修正 Bone 发布元数据、跨平台安装包命名与 GitHub Release 校验。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.1-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.1-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
