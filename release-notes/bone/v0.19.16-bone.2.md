## 更新内容

- 修复 GitHub Release 工作流中重复定义 Linux 构建任务，避免推送版本标签后工作流在创建 Job 前直接失败。
- 保留 Proma 0.19.16 Bone 1 的全部功能：Agent 画布工作区、Canvas 图片与候选版本、项目数据路径迁移、局域网移动端可信设备和可配置图片生成模型。
- 继续使用统一的跨平台安装包命名、Bone 版本校验和中文发布说明。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.2-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.2-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
