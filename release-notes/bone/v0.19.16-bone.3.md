## 更新内容

- 修复 Electron Builder 配置中重复的顶层 Linux 映射，解决 Linux 打包在加载 YAML 时直接失败的问题。
- 增加发布合同回归检查，确保 Release workflow 和 Electron Builder 配置中的 Linux 构建定义保持唯一。
- 保留 Proma 0.19.16 Bone 2 的全部功能与跨平台打包流程。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.3-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.3-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
