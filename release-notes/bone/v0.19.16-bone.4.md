## 更新内容

- 修复 GitHub Windows runner 中 Visual Studio 开发者命令路径的引号处理，解决稳定目录原生 helper 无法编译的问题。
- 改用临时批处理文件初始化 Visual Studio 编译环境，兼容含空格的安装路径并确保临时文件可靠清理。
- 增加 Windows 构建脚本回归检查，并保留 Bone 3 的 Linux Electron Builder 配置修复。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.4-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.4-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
