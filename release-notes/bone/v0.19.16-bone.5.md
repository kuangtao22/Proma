## 更新内容

- 修复 Windows 稳定目录原生 helper 的相对重命名权限，解决 Canvas 内容、版本正文和事务 intent 无法原子提交的问题。
- 为相对 rename 使用的 transactions、内容根和 entry 目录句柄补充目录遍历权限，同时保留禁止 reparse、容量限制和跨进程锁边界。
- 增加 Windows 目录句柄权限合同检查，并保留 Bone 4 的 Visual Studio 编译环境修复。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.5-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.5-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
