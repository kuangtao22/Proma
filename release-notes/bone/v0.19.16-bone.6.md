## 更新内容

- 将 Windows 稳定目录 helper 的相对重命名切换为 `NtSetInformationFile(FileRenameInformation)`，解决 Win32 `FileRenameInfo` 在 CI runner 上拒绝非空 RootDirectory 的兼容问题。
- Canvas 内容、目录移动和事务 intent 均继续使用已打开的目标目录句柄，不退化为绝对路径重解析。
- 保留 Bone 5 的目录遍历权限、rename 缓冲区边界、reparse 拒绝、容量限制和跨进程锁保护。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.6-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.6-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
