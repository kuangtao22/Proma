## 更新内容

- 修复 Windows SDK 未公开 `FileRenameInformation` 枚举名导致的原生 helper 编译失败。
- 使用 NT `FILE_INFORMATION_CLASS` 的稳定合同值调用相对重命名接口，兼容 GitHub Windows runner 当前 Visual Studio 与 SDK。
- 保留 Bone 6 的 `NtSetInformationFile` 相对目录句柄提交实现和全部路径安全边界。

## macOS 未签名版本安装

当前 Bone macOS 安装包可能未签名。请只从本仓库 GitHub Release 下载，然后在终端执行：

```bash
xattr -cr ~/Downloads/Proma-0.19.16-bone.7-macos-*.dmg
open ~/Downloads/Proma-0.19.16-bone.7-macos-*.dmg
```

将 Proma 拖入“应用程序”后，再执行：

```bash
sudo xattr -cr /Applications/Proma.app
open /Applications/Proma.app
```

`xattr` 会移除 macOS 的下载隔离标记；这不等同于 Apple 代码签名或公证。
