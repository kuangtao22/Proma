## 更新内容

- 合入官网 `v0.19.52` 基线中与 Proma 本地业务兼容的模型侧栏、技能、Vault、Slack、Markdown、模型选择器和 Exa Search 修复。
- 修复历史 Canvas Agent 节点绑定的渠道或模型失效时无法恢复的问题，明确展示重建会话入口，并兼容缺少旧事务凭据的历史数据。
- 延长 GPT Image 2 图片任务终态等待窗口，避免正常生成因超过 30 秒被误报为失败。
- 保留 Canvas、媒体和运维专有模块，未纳入会删除这些能力的官网大架构变更。

## 范围

- 基于官方 `v0.19.52`，Bone 构建号重置为 `1`。
- GitHub Actions 将在本 tag 上构建 macOS、Windows 和 Linux 安装包并创建 Release。
